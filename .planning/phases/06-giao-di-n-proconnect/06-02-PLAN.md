---
phase: 06-giao-di-n-proconnect
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - web/feed.html
autonomous: true
requirements: [UI-02, UI-04, UI-05]
must_haves:
  truths:
    - "feed.html dùng navbar chung qua <div id=\"pronav\"> + proNav('feed')"
    - "Trên lg+ feed có 3 cột: trái thẻ hồ sơ, giữa compose+timeline, phải gợi ý + thẻ thông tin tĩnh"
    - "Trên < lg các cột xếp dọc, cột giữa (timeline) hiển thị trước"
    - "Cột phải 'Người bạn có thể biết' lấy từ /api/connections/suggestions"
    - "Mọi tính năng cũ (compose/react/comment/repost/delete) còn nguyên, endpoint không đổi"
  artifacts:
    - path: "web/feed.html"
      provides: "Trang feed 3 cột responsive mang thương hiệu ProConnect"
      contains: "lg:grid-cols"
  key_links:
    - from: "web/feed.html"
      to: "window.proNav"
      via: "<div id=\"pronav\"></div> + proNav('feed')"
      pattern: "proNav\\('feed'\\)"
    - from: "web/feed.html (cột phải)"
      to: "/api/connections/suggestions"
      via: "api.get suggestions để render People-you-may-know"
      pattern: "connections/suggestions"
---

<objective>
Biến `web/feed.html` thành layout 3 cột kiểu mạng nghề nghiệp (D-03) và gắn navbar dùng chung từ Plan 01.
Cột trái: thẻ hồ sơ tóm tắt (avatar, tên, headline, "Xem hồ sơ"). Cột giữa: compose box + timeline hiện có (GIỮ NGUYÊN logic Alpine). Cột phải: "Người bạn có thể biết" (/api/connections/suggestions) + thẻ tĩnh "Về ProConnect".

Purpose: Đây là màn hình showcase chính trước giảng viên — bố cục 3 cột + thương hiệu navy làm các tính năng Gateway (feed compose, suggestions) trở nên trực quan.
Output: feed.html restyle, responsive, navbar chung, cache-bust replace ph6-01.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/06-giao-di-n-proconnect/06-CONTEXT.md

# Trang đang restyle + contract navbar từ Plan 01
@web/feed.html

<interfaces>
<!-- Từ Plan 01 (web/assets/app.js) -->
window.proNav(active)   // inject navbar dùng chung vào <div id="pronav"> rồi Alpine.initTree(el); active='feed'
window.api, window.auth, window.formatDate, window.notificationBell  // như cũ

<!-- Cache-bust token HIỆN TẠI trên trang (để REPLACE đúng) -->
// app.js?v=ph5-06   styles.css?v1778837733  (token styles.css KHÔNG có dấu '=')

<!-- Endpoint cột trái + cột phải (đã build) -->
GET /api/me                         -> { data: { id, username, display_name, headline?, avatar_url? } }
GET /api/profiles/{id}              -> { data: { id, username, display_name } }   // dùng /me là đủ cho thẻ tóm tắt
GET /api/connections/suggestions    -> { data: [ { user_id, profile:{ display_name, username, avatar_url, headline? } } ] }
GET /api/feed?limit=20              -> timeline (đã dùng trong feedPage hiện tại — KHÔNG đổi)
POST /api/connections/requests {target_id}  -> gửi lời mời (nút Kết nối ở cột phải)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Gắn navbar chung + khung 3 cột responsive vào feed.html</name>
  <read_first>web/feed.html (toàn bộ — đặc biệt khối <nav> dòng 15-67 cần XOÁ, <main> dòng 69-211, và feedPage() script; cả token cache-bust trong <head>), .planning/phases/06-giao-di-n-proconnect/06-CONTEXT.md (D-01, D-03), web/assets/app.js (proNav signature + Alpine.initTree từ Plan 01)</read_first>
  <files>web/feed.html</files>
  <action>
1. **<head> — cache-bust (D-05, thay TOÀN BỘ token, KHÔNG chỉ "bump"):** Trang hiện dùng `app.js?v=ph5-06` và `styles.css?v1778837733` (token styles.css KHÔNG có dấu `=`). Thực hiện REPLACE token đầy đủ:
   - Thay `app.js?v=ph5-06` → `app.js?v=ph6-01`.
   - Thay `styles.css?v1778837733` (và bất kỳ biến thể `styles.css?v=...` nếu có) → `styles.css?v=ph6-01`.
   Sau bước này KHÔNG còn token cũ `v=ph5-06` hay `v1778837733` trong file.
   **<head> — navy Tailwind token (D-01):** NGAY SAU thẻ `<script src="https://cdn.tailwindcss.com"></script>` (dòng ~7), thêm inline config (Play CDN đọc config inline per-page) để `bg-navy`/`text-navy` dùng được:
   ```html
   <script>
     tailwind.config = { theme: { extend: { colors: { navy: { DEFAULT: '#1e3a8a', dark: '#172f6b', light: '#3b5bbd' } } } } };
   </script>
   ```
   Giữ Tailwind Play CDN + Alpine CDN. `<body>` nền `bg-slate-50` (giữ) — ổn.
2. **XOÁ toàn bộ khối `<nav x-data="navbar()">...</nav>`** (dòng ~15-67). Thay bằng đúng một dòng: `<div id="pronav"></div>` rồi gọi `proNav('feed')`. Cách gọi: đặt `<script>document.addEventListener('DOMContentLoaded', () => window.proNav('feed'));</script>` NGAY trước thẻ `</body>`. Vì Alpine load bằng `defer` (chạy sau DOMContentLoaded), proNav thường chạy TRƯỚC Alpine init nên x-data trên #pronav được walk init nhặt; nếu Alpine đã init trước, `proNav` đã tự gọi `Alpine.initTree(#pronav)` (Plan 01) nên reactivity vẫn hoạt động. KHÔNG dựa vào thứ tự — `initTree` trong proNav đảm bảo cả hai chiều.
3. **Khung 3 cột:** đổi `<main>` container thành `max-w-5xl mx-auto px-4 py-6`. Bên trong `<main>` (giữ `x-data="feedPage()" x-init="load()"`), bọc nội dung trong:
```
<div class="grid grid-cols-1 lg:grid-cols-[260px_minmax(0,1fr)_300px] gap-6">
  <aside class="order-2 lg:order-1"> ...thẻ hồ sơ trái... </aside>
  <div class="order-1 lg:order-2"> ...compose + timeline (NGUYÊN BẢN)... </div>
  <aside class="order-3 lg:order-3 hidden lg:block space-y-4"> ...suggestions + thẻ tĩnh... </aside>
</div>
```
LƯU Ý order: cột giữa (timeline) `order-1` để hiện TRƯỚC trên mobile (D-03 "center first"); cột phải `hidden lg:block` (ẩn trên mobile để gọn).
4. **Cột giữa:** di chuyển NGUYÊN khối compose `<section>` + banner degraded + timeline `<template x-if="!loading">...` hiện có vào `<div order-2>`. KHÔNG sửa logic. Bỏ `<h1>Bảng tin</h1>` cũ hoặc thu nhỏ (tuỳ — giữ tiêu đề nhỏ là được). Thêm class thương hiệu: nút "Đăng" và nút primary đổi từ `bg-slate-900 ... hover:bg-slate-700` sang `pro-btn` (class navy từ Plan 01) — áp dụng cho các nút primary trong feed.
5. **Toàn bộ tiếng Việt** giữ nguyên (đã có dấu). Đảm bảo không còn nhãn English.
KHÔNG đụng feedPage() script logic ngoài việc bổ sung cho Task 2. KHÔNG sửa file backend.
  </action>
  <acceptance_criteria>
    - `grep -q 'id="pronav"' web/feed.html` và `grep -q "proNav('feed')" web/feed.html`
    - `! grep -q 'x-data="navbar()"' web/feed.html` (navbar cũ đã xoá)
    - `grep -q 'lg:grid-cols' web/feed.html` và `grep -q 'order-1' web/feed.html`
    - `grep -q 'app.js?v=ph6-01' web/feed.html` và `grep -q 'styles.css?v=ph6-01' web/feed.html`
    - `! grep -q 'v=ph5-06' web/feed.html` và `! grep -q 'v1778837733' web/feed.html` (token cũ đã thay sạch)
    - `grep -q 'tailwind.config' web/feed.html` và `grep -q "navy:" web/feed.html` (navy Tailwind token, D-01)
    - `grep -q 'lang="vi"' web/feed.html` và `! grep -qi 'linkedin' web/feed.html`
  </acceptance_criteria>
  <verify>
    <automated>grep -q 'id="pronav"' web/feed.html && grep -q 'lg:grid-cols' web/feed.html && grep -q 'app.js?v=ph6-01' web/feed.html && grep -q 'styles.css?v=ph6-01' web/feed.html && ! grep -q 'v1778837733' web/feed.html && grep -q 'tailwind.config' web/feed.html && ! grep -q 'x-data="navbar()"' web/feed.html</automated>
  </verify>
  <done>feed.html: navbar cũ thay bằng #pronav + proNav('feed'); main là grid 3 cột lg+ (timeline order-1 trên mobile); cache-bust REPLACE token cũ (ph5-06 + v1778837733) → ?v=ph6-01 cho cả app.js & styles.css; navy Tailwind token inline; tiếng Việt; không LinkedIn; logic feedPage giữ nguyên.</done>
</task>

<task type="auto">
  <name>Task 2: Cột trái (thẻ hồ sơ) + cột phải (gợi ý /suggestions + thẻ tĩnh "Về ProConnect")</name>
  <read_first>web/feed.html (sau Task 1), web/connections.html (mẫu render suggestion card + sendInvite), web/assets/app.js (api/auth)</read_first>
  <files>web/feed.html</files>
  <action>
Mở rộng `feedPage()` (trong `<script>` của feed.html) và điền nội dung 2 cột:

**State mới trong feedPage():** thêm `me: null, suggestions: []`. Trong `load()` (hoặc `x-init`), sau khi tải feed, thêm best-effort:
```
try { this.me = (await api.get('/me')).data; } catch(e){}
try { this.suggestions = ((await api.get('/connections/suggestions')).data || []).slice(0,5); } catch(e){}
```
(bọc try riêng để lỗi suggestions không làm hỏng feed — degrade an toàn).

**Cột TRÁI `<aside order-2 lg:order-1>`** — thẻ hồ sơ tóm tắt:
```
<div class="bg-white border rounded-lg overflow-hidden">
  <div class="h-12 pro-surface"></div>
  <div class="px-4 pb-4 -mt-6">
    <span class="w-14 h-14 rounded-full border-4 border-white bg-slate-300 ...">
      <template x-if="me?.avatar_url"><img :src="me.avatar_url" alt="Ảnh đại diện" class="w-full h-full object-cover rounded-full"></template>
      <template x-if="!me?.avatar_url"><span x-text="(me?.display_name||me?.username||'?').trim().charAt(0).toUpperCase()"></span></template>
    </span>
    <p class="font-semibold mt-2" x-text="me?.display_name || me?.username || ''"></p>
    <p class="subtle text-sm" x-text="me?.headline || ''"></p>
    <a :href="'/profile.html?id='+(me?.id||'')" class="block mt-3 text-sm text-center border rounded py-1.5 hover:bg-slate-50">Xem hồ sơ</a>
  </div>
</div>
```
Mọi field user qua `x-text` (XSS-safe).

**Cột PHẢI `<aside ... hidden lg:block space-y-4>`** — hai thẻ:
1. "Người bạn có thể biết":
```
<div class="bg-white border rounded-lg p-4">
  <h2 class="font-semibold text-sm mb-3">Người bạn có thể biết</h2>
  <template x-if="!suggestions.length"><p class="subtle text-xs">Chưa có gợi ý.</p></template>
  <ul class="space-y-3">
    <template x-for="s in suggestions" :key="s.user_id">
      <li class="flex items-center justify-between gap-2">
        <a :href="'/profile.html?id='+s.user_id" class="flex items-center gap-2 min-w-0 hover:underline">
          <span class="w-9 h-9 rounded-full bg-slate-300 ... shrink-0">
            <template x-if="s.profile?.avatar_url"><img :src="s.profile.avatar_url" alt="Ảnh đại diện" class="w-full h-full object-cover rounded-full"></template>
            <template x-if="!s.profile?.avatar_url"><span x-text="(s.profile?.display_name||s.profile?.username||'?').trim().charAt(0).toUpperCase()"></span></template>
          </span>
          <span class="truncate text-sm" x-text="s.profile?.display_name || s.profile?.username || 'Người dùng'"></span>
        </a>
        <button @click="invite(s.user_id)" :disabled="busy" class="text-xs pro-btn px-2 py-1 rounded disabled:opacity-50 shrink-0">Kết nối</button>
      </li>
    </template>
  </ul>
</div>
```
Thêm method `invite(id)` vào feedPage: `return this._act(() => api.post('/connections/requests', { target_id: id }));` (tái dùng _act đã có). LƯU Ý: _act gọi this.load() sau khi xong — chấp nhận được (refresh feed+suggestions).
2. Thẻ tĩnh "Về ProConnect" (slot quảng cáo demo):
```
<div class="bg-white border rounded-lg p-4 text-sm">
  <h2 class="font-semibold mb-1" style="color:#1e3a8a">Về ProConnect</h2>
  <p class="subtle">Mạng xã hội nghề nghiệp — đồ án SOA / API Gateway (PTIT INT1448). Tất cả dữ liệu demo.</p>
</div>
```
KHÔNG thêm endpoint mới. KHÔNG x-html. KHÔNG sửa file backend.
  </action>
  <acceptance_criteria>
    - `grep -q 'connections/suggestions' web/feed.html` (cột phải)
    - `grep -q 'Người bạn có thể biết' web/feed.html` và `grep -q 'Về ProConnect' web/feed.html`
    - `grep -q 'Xem hồ sơ' web/feed.html` (cột trái) và `grep -q "api.get('/me')" web/feed.html`
    - `! grep -qi 'x-html' web/feed.html`
    - `grep -c 'order-' web/feed.html` >= 2 (cột sắp xếp responsive)
  </acceptance_criteria>
  <verify>
    <automated>grep -q 'connections/suggestions' web/feed.html && grep -q 'Người bạn có thể biết' web/feed.html && grep -q 'Xem hồ sơ' web/feed.html && ! grep -qi 'x-html' web/feed.html</automated>
  </verify>
  <done>Cột trái thẻ hồ sơ (/me, avatar, headline, "Xem hồ sơ") + cột phải gợi ý từ /api/connections/suggestions (nút Kết nối) và thẻ tĩnh "Về ProConnect"; degrade an toàn nếu suggestions lỗi; toàn x-text.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| browser → gateway | feed gọi /me, /feed, /connections/suggestions, /connections/requests (authenticated) |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-06 | Tampering/Info (XSS) | render display_name/headline/suggestion ở 3 cột | mitigate | Toàn bộ x-text (smoke + acceptance assert `! grep x-html`) |
| T-06-07 | Availability (degrade) | suggestions lỗi làm vỡ feed | mitigate | try/catch riêng cho /me và /suggestions; lỗi không chặn timeline |
| T-06-08 | Legal | asset LinkedIn | mitigate | Không thêm asset LinkedIn; guard smoke `! grep -i linkedin` |
| T-06-09 | Surface | đụng backend | accept | Source/production chỉ web/feed.html (KHÔNG gateway/service/db/docker-compose/backend); `.planning/**` meta-docs là bookkeeping GSD bình thường, được miễn |
</threat_model>

<verification>
- `grep` xác nhận #pronav + proNav('feed'), grid 3 cột lg, suggestions, thẻ hồ sơ, thẻ tĩnh, cache-bust REPLACE token mới ph6-01 (không còn token cũ), navy tailwind.config, không x-html, không LinkedIn.
- Mở mắt thường (Plan 04 visual): 3 cột trên desktop, xếp dọc trên mobile với timeline trước.
- Source/production CHỈ web/feed.html (KHÔNG gateway/service/db/docker-compose/backend). `.planning/**` meta-docs là bookkeeping GSD bình thường, được miễn khỏi quy tắc này.

**Phạm vi sửa đổi (làm rõ):** Thay đổi PRODUCTION/source CHỈ giới hạn trong `web/*` + `scripts/*` (KHÔNG gateway/services/db/docker-compose/backend). Các meta-doc `.planning/**` (SUMMARY.md, STATE.md, ROADMAP.md, REQUIREMENTS.md) là artefact bookkeeping GSD bình thường, ĐƯỢC MIỄN khỏi quy tắc source-scope này. Quy tắc nói về source/backend, KHÔNG phải "chỉ web+scripts trong toàn bộ diff".
</verification>

<success_criteria>
- feed.html bố cục 3 cột trên lg+ (trái hồ sơ / giữa compose+timeline / phải suggestions+thẻ tĩnh), responsive xếp dọc < lg với cột giữa trước.
- Navbar dùng chung (proNav), thương hiệu navy (tailwind.config navy token + .pro-* helpers), cache-bust REPLACE token cũ → ?v=ph6-01, tiếng Việt.
- Cột phải nối /api/connections/suggestions; mọi tính năng feed cũ + endpoint giữ nguyên; toàn x-text, không file backend.
</success_criteria>

<output>
After completion, create `.planning/phases/06-giao-di-n-proconnect/06-02-SUMMARY.md`
</output>
