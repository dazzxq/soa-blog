---
phase: 06-giao-di-n-proconnect
plan: 04
type: execute
wave: 3
depends_on: [01, 02, 03]
files_modified:
  - scripts/smoke-phase6.sh
autonomous: false
requirements: [UI-01, UI-02, UI-04, UI-05]
must_haves:
  truths:
    - "smoke-phase6.sh static asserts XANH local sau khi Plan 01/02/03 hoàn tất"
    - "Codex đã APPROVE code (codex-impl-review) trước khi commit (quy trình bắt buộc)"
    - "Sau deploy, 8 trang GET 200 trên https://soa.duyet.vn"
    - "Backend không bị đụng: smoke-phase1..5 vẫn xanh; allowlist guard chặn mọi path ngoài web/ scripts/ .planning/ (gồm docker-compose/file gốc)"
    - "Người dùng (mắt thường) xác nhận navy brand + feed 3 cột (stack trên mobile) + navbar badges hoạt động + tiếng Việt"
  artifacts:
    - path: "scripts/smoke-phase6.sh"
      provides: "Chạy đầy đủ static + RUN_HTTP=1 page-200 sau deploy"
      contains: "smoke6"
  key_links:
    - from: "scripts/smoke-phase6.sh (RUN_HTTP=1)"
      to: "https://soa.duyet.vn/*.html"
      via: "curl page-200 cho 8 trang sau deploy"
      pattern: "RUN_HTTP"
---

<objective>
Đưa giao diện ProConnect lên production: chạy smoke-phase6 static local + regression backend, qua codex-impl-review (bắt buộc), commit + push public GitHub → CI/CD tự deploy lên soa.duyet.vn (web bind-mount → restart web container, KHÔNG migration / không container/secret/grant mới), verify page-200 — RỒI dừng cho con người xác minh thị giác.

Purpose: Đây là plan cutover do orchestrator điều phối — gate cuối cùng trước khi milestone hoàn tất. UI-only nên deploy chỉ serve file tĩnh mới. Tách rõ: Task 1 = mọi việc tự động hoá được (smoke/regression/codex/commit/push/CI-deploy/page-200); Task 2 = checkpoint thị giác con người thuần tuý.
Output: GUI ProConnect sống trên soa.duyet.vn, đã verify tự động + người duyệt thị giác.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/06-giao-di-n-proconnect/06-VALIDATION.md

# Quy trình deploy đã có
@scripts/deploy.sh
@scripts/smoke-phase6.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Smoke static + regression + codex-impl-review, RỒI commit/push → CI deploy → page-200 (tự động)</name>
  <read_first>scripts/smoke-phase6.sh, scripts/deploy.sh (xác nhận deploy chỉ restart web container khi web/ đổi — KHÔNG migration mới), scripts/smoke-phase5.sh</read_first>
  <files>scripts/smoke-phase6.sh</files>
  <action>
TẤT CẢ các bước dưới đều tự động hoá được (CLI/SSH/CI) — executor tự chạy, KHÔNG hỏi con người:

**A. Verify tĩnh + không đụng backend + codex (trước commit):**
1. Chạy `bash scripts/smoke-phase6.sh` (chế độ static, KHÔNG RUN_HTTP) → phải `[smoke6] ALL PASS`. Các assert proNav/initTree/navy/svg/tagline/cache-bust ph6-01/lang=vi/no-linkedin giờ đã xanh vì Plan 01/02/03 hoàn tất. Nếu FAIL, sửa file web tương ứng (vẫn trong web/* — KHÔNG đụng backend) cho tới khi xanh; nếu smoke có assert sai (false negative), sửa nhẹ smoke-phase6.sh.
2. **Allowlist guard (KHÔNG denylist)** — FAIL nếu BẤT KỲ path đã đổi nào nằm NGOÀI `web/`, `scripts/`, `.planning/` (bắt mọi drift backend/config gồm docker-compose.yml, file gốc, v.v.; cho phép UI + bookkeeping GSD):
```sh
# fail nếu có path đã đổi nằm ngoài phạm vi UI/bookkeeping cho phép
if git status --porcelain | sed -E 's/^.{3}//; s/^.* -> //' | grep -qvE '^(web/|scripts/|\.planning/)'; then
  echo "FATAL: Phase 6 đổi file ngoài web/ scripts/ .planning/ (KHÔNG cho phép backend)"; git status --porcelain; exit 1
fi
echo "OK: chỉ web/ + scripts/ + .planning/"
```
(`sed 's/.* -> //'` xử lý dòng rename `old -> new` để chỉ kiểm path đích.) PRODUCTION/source chỉ giới hạn `web/*` + `scripts/*`; `.planning/**` meta-docs là bookkeeping GSD bình thường, được miễn.
3. **codex-impl-review (BẮT BUỘC theo CLAUDE.md)**: gọi `/codex-impl-review` trên toàn bộ thay đổi web/* + scripts/smoke-phase6.sh. Nếu Codex nêu issue hợp lệ → sửa (trong web/*+scripts/*) rồi chạy lại `/codex-impl-review`. CHỈ tiếp tục sau khi Codex APPROVE.

**B. Commit/push → CI/CD deploy → verify page-200 + regression (sau Codex APPROVE):**
4. `git add web/ scripts/smoke-phase6.sh` + commit (message tiếng Anh ngắn theo convention dự án, vd `feat(ui): ProConnect navy rebrand + shared navbar + 3-col feed (phase 6)`) + `git push` lên public GitHub nhánh main.
5. Để CI/CD GitHub Actions tự chạy: lint `php -l` (không đụng PHP → xanh) → deploy SSH VPS → `scripts/deploy.sh` (git pull → docker compose up -d; web bind-mount nên file tĩnh mới được serve sau restart web container) → verify `/api/health`. Theo dõi tới khi deploy xong.
6. Chạy `RUN_HTTP=1 BASE=https://soa.duyet.vn bash scripts/smoke-phase6.sh` → page-200 cho 8 trang phải xanh.
7. Chạy regression: `bash scripts/smoke-phase5.sh` (và phase1..4 nếu nhanh) → vẫn xanh (backend không đổi).

Sau bước 7, mọi xác minh TỰ ĐỘNG đã xong; chuyển sang Task 2 (checkpoint con người). KHÔNG tự gõ "approved" thay con người.
  </action>
  <acceptance_criteria>
    - `bash scripts/smoke-phase6.sh` → `[smoke6] ALL PASS` (exit 0)
    - Allowlist guard XANH: `git status --porcelain | sed -E 's/^.{3}//; s/^.* -> //' | grep -qvE '^(web/|scripts/|\.planning/)'` KHÔNG khớp dòng nào (mọi path đã đổi đều nằm trong web/ scripts/ .planning/ — KHÔNG backend/docker-compose/file gốc nào lọt)
    - Codex impl-review trả APPROVE (ghi lại trong SUMMARY)
    - Đã commit + push; CI deploy xong (`/api/health` xanh)
    - `RUN_HTTP=1 BASE=https://soa.duyet.vn bash scripts/smoke-phase6.sh` → page-200 cho 8 trang xanh
    - `bash scripts/smoke-phase5.sh` → regression xanh
  </acceptance_criteria>
  <verify>
    <automated>bash scripts/smoke-phase6.sh && RUN_HTTP=1 BASE=https://soa.duyet.vn bash scripts/smoke-phase6.sh</automated>
  </verify>
  <done>smoke static xanh; allowlist guard xanh (mọi path đã đổi nằm trong web/ scripts/ .planning/, không backend/docker-compose/file gốc); Codex APPROVE; đã commit/push; CI deploy xong; page-200 cho 8 trang + regression backend (phase5) xanh.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Con người xác minh thị giác production (desktop + mobile)</name>
  <files>(không sửa file — chỉ sign-off thị giác)</files>
  <action>
KHÔNG có bước tự động hoá nào ở task này — toàn bộ smoke/regression/codex/commit/push/CI-deploy/page-200 đã hoàn tất ở Task 1. Executor DỪNG và chờ con người mở https://soa.duyet.vn để duyệt thị giác (việc duy nhất không tự động hoá được). Nếu con người báo lỗi, quay lại sửa trong web/* và lặp lại Task 1 (re-deploy), rồi quay lại checkpoint này.
  </action>
  <what-built>
Toàn bộ GUI ProConnect Phase 6 ĐÃ DEPLOY và verify tự động ở Task 1: thương hiệu navy #1e3a8a + logo mắt xích + tagline qua navbar dùng chung (proNav) trên 8 trang; feed 3 cột responsive; navbar có ô tìm kiếm + chuông thông báo + badge lời mời + menu hồ sơ; toàn bộ tiếng Việt có dấu. Các bước commit/push/deploy/page-200/regression đã XANH trước checkpoint này. Task này CHỈ là sign-off thị giác của con người.
  </what-built>
  <how-to-verify>
Mở https://soa.duyet.vn trên DESKTOP và MOBILE (hoặc thu nhỏ cửa sổ), đăng nhập tài khoản demo (demo / demo@123**), kiểm tra:
1. **Thương hiệu (UI-01):** Mọi trang có navbar navy #1e3a8a với logo mắt xích (inline SVG) + chữ ProConnect + tagline "Kết nối chuyên nghiệp". Không thấy bất kỳ logo/tên LinkedIn.
2. **Feed 3 cột (UI-02):** /feed.html trên desktop hiện 3 cột (trái thẻ hồ sơ, giữa compose+timeline, phải "Người bạn có thể biết" + "Về ProConnect"). Thu nhỏ về mobile → các cột xếp dọc, timeline (cột giữa) hiện trước, cột phải ẩn/gọn.
3. **Navbar wired (UI-04) — đặc biệt kiểm reactivity (BLOCKER cũ):** Gõ vào ô tìm kiếm + Enter → tới /search.html?q=…; chuông thông báo có badge số chưa đọc, bấm mở dropdown; badge lời mời cạnh "Kết nối" HIỆN SỐ nếu có lời mời chờ; menu hồ sơ mở ra "Hồ sơ của tôi / Chỉnh sửa hồ sơ / Đăng xuất"; bấm Đăng xuất → về trang chủ ở trạng thái logged-out (token đã xoá, không vào lại được /feed). Nếu badge/chuông/menu KHÔNG phản ứng (markup chết) → navbar inject thiếu reactivity (Alpine.initTree) — report để sửa.
4. **Tiếng Việt (UI-05):** Mọi nhãn + thông báo lỗi đều tiếng Việt có dấu, không sót English.
5. **Không vỡ chức năng:** đăng bài, thả reaction, bình luận, kết nối, tìm kiếm vẫn hoạt động như trước.
  </how-to-verify>
  <verify>
    <automated>echo "Visual sign-off only — automated checks completed in Task 1 (smoke + RUN_HTTP page-200 + regression)."</automated>
  </verify>
  <done>Con người approve thị giác: brand navy + logo mắt xích + tagline; feed 3 cột stack trên mobile; navbar badges/chuông/menu/Đăng xuất phản ứng đúng (reactivity OK); tiếng Việt; chức năng cũ không vỡ.</done>
  <resume-signal>Gõ "approved" nếu mọi mục đạt, hoặc mô tả vấn đề (sẽ sửa trong web/* và lặp lại deploy ở Task 1).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| GitHub → VPS (CI/CD) | push main kích deploy; chỉ pull + restart web container (không secret/migration mới) |
| internet → Cloudflare → origin | 8 trang tĩnh phục vụ qua edge hiện có |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-14 | Tampering | deploy đụng backend/config ngoài ý muốn | mitigate | Task 1 allowlist guard FAIL nếu bất kỳ path đã đổi nằm ngoài web/ scripts/ .planning/ (bắt cả docker-compose.yml + file gốc) trước khi commit |
| T-06-15 | Availability | regression vỡ tính năng cũ | mitigate | Task 1 chạy smoke-phase5 (và 1..4) sau deploy; Task 2 checkpoint con người kiểm chức năng |
| T-06-16 | Legal | LinkedIn asset lọt production | mitigate | smoke `! grep -i linkedin` (Task 1) + checkpoint thị giác mục 1 (Task 2) |
| T-06-17 | Repudiation | đăng xuất không xoá phiên trên prod | mitigate | checkpoint mục 3 kiểm Đăng xuất → logged-out thật |
| T-06-18 | Process | commit khi Codex chưa duyệt | mitigate | Task 1 chặn: chỉ commit sau Codex APPROVE (CLAUDE.md) |
| T-06-19 | DoS/UX (inert UI) | navbar production thiếu reactivity (badge/bell/menu chết) | mitigate | proNav gọi Alpine.initTree (Plan 01) + smoke assert initTree (Task 1) + checkpoint mục 3 (Task 2) |
</threat_model>

<verification>
- Task 1 (auto): smoke-phase6 static ALL PASS; `RUN_HTTP=1 ... soa.duyet.vn` page-200 cho 8 trang; smoke-phase5 (+1..4) regression xanh; git status chỉ web/* + scripts/*; Codex APPROVE; commit/push/CI-deploy xong.
- Task 2 (checkpoint con người): brand navy + feed 3 cột responsive + navbar badges/reactivity + tiếng Việt + chức năng không vỡ.
</verification>

<success_criteria>
- GUI ProConnect sống trên soa.duyet.vn: navy brand + logo mắt xích + tagline (UI-01), feed 3 cột responsive (UI-02), navbar search/bell/invite-badge/profile-menu phản ứng (UI-04), tiếng Việt có dấu toàn bộ (UI-05).
- Page-200 cho 8 trang; regression backend xanh; không file backend đụng tới; Codex đã duyệt; con người approve thị giác.
</success_criteria>

<output>
After completion, create `.planning/phases/06-giao-di-n-proconnect/06-04-SUMMARY.md`
</output>
