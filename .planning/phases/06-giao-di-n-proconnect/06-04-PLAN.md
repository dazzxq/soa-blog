---
phase: 06-giao-di-n-proconnect
plan: 04
type: execute
wave: 3
depends_on: [01, 02, 03]
files_modified:
  - scripts/smoke-phase6.sh
autonomous: false
requirements: [UI-01, UI-02, UI-04, UI-05]
must_haves:
  truths:
    - "smoke-phase6.sh static asserts XANH local sau khi Plan 01/02/03 hoàn tất"
    - "Codex đã APPROVE code (codex-impl-review) trước khi commit (quy trình bắt buộc)"
    - "Sau deploy, 8 trang GET 200 trên https://soa.duyet.vn"
    - "Backend không bị đụng: smoke-phase1..5 vẫn xanh (không file service/gateway/db nào đổi)"
    - "Người dùng (mắt thường) xác nhận navy brand + feed 3 cột (stack trên mobile) + navbar badges hoạt động + tiếng Việt"
  artifacts:
    - path: "scripts/smoke-phase6.sh"
      provides: "Chạy đầy đủ static + RUN_HTTP=1 page-200 sau deploy"
      contains: "smoke6"
  key_links:
    - from: "scripts/smoke-phase6.sh (RUN_HTTP=1)"
      to: "https://soa.duyet.vn/*.html"
      via: "curl page-200 cho 8 trang sau deploy"
      pattern: "RUN_HTTP"
---

<objective>
Đưa giao diện ProConnect lên production: chạy smoke-phase6 static local, qua codex-impl-review (bắt buộc), commit + push public GitHub → CI/CD tự deploy lên soa.duyet.vn (web bind-mount → restart web container, KHÔNG migration / không container/secret/grant mới), rồi xác minh: static + page-200 + regression backend + checkpoint thị giác con người.

Purpose: Đây là plan cutover do orchestrator điều phối — gate cuối cùng trước khi milestone hoàn tất. UI-only nên deploy chỉ serve file tĩnh mới.
Output: GUI ProConnect sống trên soa.duyet.vn, đã verify.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/06-giao-di-n-proconnect/06-VALIDATION.md

# Quy trình deploy đã có
@scripts/deploy.sh
@scripts/smoke-phase6.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Smoke static local + regression backend xanh, rồi codex-impl-review</name>
  <read_first>scripts/smoke-phase6.sh, scripts/deploy.sh (xác nhận deploy chỉ restart web container khi web/ đổi — KHÔNG migration mới)</read_first>
  <files>scripts/smoke-phase6.sh</files>
  <action>
1. Chạy `bash scripts/smoke-phase6.sh` (chế độ static, KHÔNG RUN_HTTP) → phải `[smoke6] ALL PASS`. Các assert proNav/navy/svg/tagline/cache-bust ph6/lang=vi/no-linkedin giờ đã xanh vì Plan 01/02/03 hoàn tất. Nếu FAIL, sửa file web tương ứng (vẫn trong web/* — KHÔNG đụng backend) cho tới khi xanh; nếu smoke có assert sai (false negative), sửa nhẹ smoke-phase6.sh.
2. Khẳng định KHÔNG file backend bị đổi trong phase: `git status --porcelain | grep -E '^.{1,2} (gateway/|services/|db/)' && echo "VI PHẠM: backend bị đụng" || echo "OK: chỉ web/ + scripts/"`. Phải in "OK".
3. **codex-impl-review (BẮT BUỘC theo CLAUDE.md)**: gọi `/codex-impl-review` trên toàn bộ thay đổi web/* + scripts/smoke-phase6.sh. Nếu Codex nêu issue hợp lệ → sửa (trong web/*+scripts/*) rồi chạy lại `/codex-impl-review`. CHỈ tiếp tục sang Task 2 sau khi Codex APPROVE.
KHÔNG commit ở task này (commit nằm ở Task 2 sau khi Codex duyệt).
  </action>
  <acceptance_criteria>
    - `bash scripts/smoke-phase6.sh` → `[smoke6] ALL PASS` (exit 0)
    - `git status --porcelain` KHÔNG có dòng nào chạm `gateway/`, `services/`, `db/`
    - Codex impl-review trả APPROVE (ghi lại trong SUMMARY)
  </acceptance_criteria>
  <verify>
    <automated>bash scripts/smoke-phase6.sh</automated>
  </verify>
  <done>smoke static xanh; xác nhận không file backend đổi; Codex APPROVE code.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Commit + push (CI/CD deploy) + xác minh thị giác production</name>
  <files>scripts/smoke-phase6.sh</files>
  <action>
Sau khi Codex APPROVE ở Task 1, executor tự động (KHÔNG hỏi cho các bước CLI vì đều tự động hoá được):
1. `git add web/ scripts/smoke-phase6.sh` + commit (message tiếng Anh ngắn theo convention dự án, vd `feat(ui): ProConnect navy rebrand + shared navbar + 3-col feed (phase 6)`) + `git push` lên public GitHub nhánh main.
2. Để CI/CD GitHub Actions tự chạy: lint `php -l` (không đụng PHP → xanh) → deploy SSH VPS → `scripts/deploy.sh` (git pull → docker compose up -d; web bind-mount nên file tĩnh mới được serve sau restart web container) → verify `/api/health`. Theo dõi tới khi deploy xong.
3. Chạy `RUN_HTTP=1 BASE=https://soa.duyet.vn bash scripts/smoke-phase6.sh` → page-200 cho 8 trang phải xanh.
4. Chạy regression: `bash scripts/smoke-phase5.sh` (và phase1..4 nếu nhanh) → vẫn xanh (backend không đổi).
5. SAU đó dừng cho con người xác minh thị giác (mục how-to-verify) — đây là gate UI thật.
  </action>
  <what-built>
Toàn bộ GUI ProConnect Phase 6: thương hiệu navy #1e3a8a + logo mắt xích + tagline qua navbar dùng chung (proNav) trên 8 trang; feed 3 cột responsive; navbar có ô tìm kiếm + chuông thông báo + badge lời mời + menu hồ sơ; toàn bộ tiếng Việt có dấu. Các bước commit/push/deploy/smoke ở phần <action> đã chạy xong trước checkpoint này.
  </what-built>
  <how-to-verify>
Mở https://soa.duyet.vn trên DESKTOP và MOBILE (hoặc thu nhỏ cửa sổ), đăng nhập tài khoản demo (demo / demo@123**), kiểm tra:
1. **Thương hiệu (UI-01):** Mọi trang có navbar navy #1e3a8a với logo mắt xích (inline SVG) + chữ ProConnect + tagline "Kết nối chuyên nghiệp". Không thấy bất kỳ logo/tên LinkedIn.
2. **Feed 3 cột (UI-02):** /feed.html trên desktop hiện 3 cột (trái thẻ hồ sơ, giữa compose+timeline, phải "Người bạn có thể biết" + "Về ProConnect"). Thu nhỏ về mobile → các cột xếp dọc, timeline (cột giữa) hiện trước, cột phải ẩn/gọn.
3. **Navbar wired (UI-04):** Gõ vào ô tìm kiếm + Enter → tới /search.html?q=…; chuông thông báo có badge số chưa đọc, bấm mở dropdown; badge lời mời cạnh "Kết nối" hiện số nếu có lời mời chờ; menu hồ sơ mở ra "Hồ sơ của tôi / Chỉnh sửa hồ sơ / Đăng xuất"; bấm Đăng xuất → về trang chủ ở trạng thái logged-out (token đã xoá, không vào lại được /feed).
4. **Tiếng Việt (UI-05):** Mọi nhãn + thông báo lỗi đều tiếng Việt có dấu, không sót English.
5. **Không vỡ chức năng:** đăng bài, thả reaction, bình luận, kết nối, tìm kiếm vẫn hoạt động như trước.
  </how-to-verify>
  <verify>
    <automated>RUN_HTTP=1 BASE=https://soa.duyet.vn bash scripts/smoke-phase6.sh</automated>
  </verify>
  <done>Đã commit/push qua Codex-approved; CI deploy xong; page-200 cho 8 trang + regression backend xanh; con người approve thị giác (brand/3-cột/navbar/tiếng Việt/chức năng).</done>
  <resume-signal>Gõ "approved" nếu mọi mục đạt, hoặc mô tả vấn đề (sẽ sửa trong web/* và lặp lại deploy).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| GitHub → VPS (CI/CD) | push main kích deploy; chỉ pull + restart web container (không secret/migration mới) |
| internet → Cloudflare → origin | 8 trang tĩnh phục vụ qua edge hiện có |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-14 | Tampering | deploy đụng backend ngoài ý muốn | mitigate | Task 1 assert git status không chạm gateway/services/db trước khi commit |
| T-06-15 | Availability | regression vỡ tính năng cũ | mitigate | Chạy smoke-phase5 (và 1..4) sau deploy; checkpoint con người kiểm chức năng |
| T-06-16 | Legal | LinkedIn asset lọt production | mitigate | smoke `! grep -i linkedin` + checkpoint thị giác mục 1 |
| T-06-17 | Repudiation | đăng xuất không xoá phiên trên prod | mitigate | checkpoint mục 3 kiểm Đăng xuất → logged-out thật |
| T-06-18 | Process | commit khi Codex chưa duyệt | mitigate | Task 1 chặn: chỉ commit sau Codex APPROVE (CLAUDE.md) |
</threat_model>

<verification>
- smoke-phase6 static (local) ALL PASS; `RUN_HTTP=1 ... soa.duyet.vn` page-200 cho 8 trang.
- smoke-phase5 (+1..4) regression xanh → backend không đổi.
- git status xác nhận chỉ web/* + scripts/* thay đổi.
- Checkpoint con người: brand navy + feed 3 cột responsive + navbar badges + tiếng Việt + chức năng không vỡ.
</verification>

<success_criteria>
- GUI ProConnect sống trên soa.duyet.vn: navy brand + logo mắt xích + tagline (UI-01), feed 3 cột responsive (UI-02), navbar search/bell/invite-badge/profile-menu (UI-04), tiếng Việt có dấu toàn bộ (UI-05).
- Page-200 cho 8 trang; regression backend xanh; không file backend đụng tới; Codex đã duyệt; con người approve thị giác.
</success_criteria>

<output>
After completion, create `.planning/phases/06-giao-di-n-proconnect/06-04-SUMMARY.md`
</output>
