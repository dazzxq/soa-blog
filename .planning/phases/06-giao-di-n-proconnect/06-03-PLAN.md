---
phase: 06-giao-di-n-proconnect
plan: 03
type: execute
wave: 2
depends_on: [01]
files_modified:
  - web/index.html
  - web/login.html
  - web/register.html
  - web/profile.html
  - web/profile-edit.html
  - web/connections.html
  - web/search.html
autonomous: true
requirements: [UI-01, UI-04, UI-05]
must_haves:
  truths:
    - "7 trang còn lại (index, login, register, profile, profile-edit, connections, search) dùng navbar chung qua <div id=\"pronav\"> + proNav(active)"
    - "Mọi trang mang thương hiệu navy #1e3a8a (navy Tailwind token + nút primary .pro-btn), tagline + logo qua navbar chung"
    - "Trang đăng nhập/đăng ký dùng navbar gọn (logo + tagline), logged-out hiển thị Đăng nhập/Đăng ký"
    - "Mọi tính năng + endpoint cũ giữ nguyên; cache-bust REPLACE token cũ (ph5-06 + v1778837733) → ?v=ph6-01 cho app.js + styles.css"
    - "Mọi chữ tiếng Việt có dấu, không nhãn English; không asset LinkedIn"
  artifacts:
    - path: "web/connections.html"
      provides: "Trang kết nối dùng navbar chung + brand navy"
      contains: "id=\"pronav\""
    - path: "web/profile.html"
      provides: "Trang hồ sơ dùng navbar chung + brand navy"
      contains: "id=\"pronav\""
    - path: "web/search.html"
      provides: "Trang tìm kiếm dùng navbar chung + brand navy"
      contains: "id=\"pronav\""
  key_links:
    - from: "web/connections.html"
      to: "window.proNav"
      via: "<div id=\"pronav\"></div> + proNav('connections')"
      pattern: "proNav\\('connections'\\)"
    - from: "web/search.html"
      to: "window.proNav"
      via: "<div id=\"pronav\"></div> + proNav('search')"
      pattern: "proNav\\('search'\\)"
---

<objective>
Áp navbar dùng chung (proNav từ Plan 01) + thương hiệu navy lên 7 trang còn lại để toàn ứng dụng nhất quán (D-04): index, login, register, profile, profile-edit, connections, search.
Mỗi trang: thay khối `<nav>` cũ bằng `<div id="pronav">` + gọi `proNav(active)`, thêm navy Tailwind token (inline tailwind.config) + đổi nút primary sang navy `.pro-btn`, REPLACE token cache-bust cũ → `?v=ph6-01`, audit tiếng Việt.

Purpose: Nhất quán thương hiệu trên mọi màn hình là tiêu chí UI-01/UI-05; navbar chung loại bỏ ~50 dòng markup lặp trên mỗi file (D-02). Chạy song song Plan 02 (file rời nhau: Plan 02 sở hữu feed.html).
Output: 7 trang restyle, navbar chung, navy token, cache-bust replace ph6-01.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/06-giao-di-n-proconnect/06-CONTEXT.md

# Các trang đang restyle + contract navbar Plan 01
@web/connections.html
@web/profile.html
@web/search.html
@web/login.html

<interfaces>
<!-- Từ Plan 01 (web/assets/app.js) -->
window.proNav(active)  // inject navbar dùng chung vào <div id="pronav"> rồi Alpine.initTree(el); active ∈ 'feed'|'connections'|'search'|'profile'|''
// Các trang giữ NGUYÊN x-data page component (connectionsPage, profilePage, searchPage, loginPage, registerPage, indexPage) — chỉ thay <nav> + brand + navy token + cache-bust.
// login/register/index logged-out: proNav('') vẫn render đúng (logged-out → Đăng nhập/Đăng ký).

<!-- Cache-bust token HIỆN TẠI trên cả 7 trang (để REPLACE đúng) -->
// app.js?v=ph5-06   styles.css?v1778837733  (token styles.css KHÔNG có dấu '=')

<!-- Navy Tailwind token (inline, per-page — Play CDN đọc config inline mỗi trang) -->
// <script>tailwind.config = { theme:{ extend:{ colors:{ navy:{ DEFAULT:'#1e3a8a', dark:'#172f6b', light:'#3b5bbd' } } } } };</script>
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: index + login + register — navbar chung + navy token + brand navy + cache-bust replace</name>
  <read_first>web/index.html, web/login.html, web/register.html (toàn bộ — đặc biệt khối <nav>, token cache-bust trong <head>, và các nút bg-slate-900), web/assets/app.js (proNav + Alpine.initTree từ Plan 01)</read_first>
  <files>web/index.html, web/login.html, web/register.html</files>
  <action>
Với CẢ BA trang:
1. **<head> — cache-bust (D-05, REPLACE token đầy đủ):** Trang hiện dùng `app.js?v=ph5-06` và `styles.css?v1778837733` (token styles.css KHÔNG có `=`). Thay `app.js?v=ph5-06` → `app.js?v=ph6-01`; thay `styles.css?v1778837733` (và mọi biến thể `styles.css?v=...`) → `styles.css?v=ph6-01`. Sau bước này KHÔNG còn token cũ.
   **<head> — navy Tailwind token (D-01):** NGAY SAU `<script src="https://cdn.tailwindcss.com"></script>`, thêm `<script>tailwind.config = { theme:{ extend:{ colors:{ navy:{ DEFAULT:'#1e3a8a', dark:'#172f6b', light:'#3b5bbd' } } } } };</script>`. Giữ Tailwind/Alpine CDN, `lang="vi"`.
2. **Thay <nav>:** XOÁ khối `<nav ...>...</nav>` hiện có; thay bằng `<div id="pronav"></div>`. Thêm `<script>document.addEventListener('DOMContentLoaded', () => window.proNav(''));</script>` ngay trước `</body>`. proNav tự gọi `Alpine.initTree(#pronav)` (Plan 01) nên reactivity hoạt động bất kể proNav chạy trước hay sau Alpine init. ACTIVE = `''` cho cả ba (logged-out hiển thị Đăng nhập/Đăng ký; logged-in vẫn render menu đầy đủ).
   - login.html / register.html navbar cũ chỉ là logo + 1 link — proNav thay thế đầy đủ. Có thể giữ link chéo (login↔register) trong THÂN trang, không bắt buộc trong navbar.
3. **Nút primary navy:** đổi mọi nút submit/CTA `class="...bg-slate-900...hover:bg-slate-700..."` sang dùng `pro-btn` (thêm class `pro-btn`, bỏ `bg-slate-900 hover:bg-slate-700`, GIỮ padding/rounded/text-white/disabled). Áp cho: nút Đăng nhập (login), Đăng ký (register), CTA primary trên index ("Đăng nhập"/"Hồ sơ của tôi"); nút "Đăng ký" secondary giữ border.
4. **index.html:** đổi tiêu đề hero "ProConnect — nền tảng đang được xây dựng" thành thông điệp hoàn thiện, vd "Chào mừng tới ProConnect" + mô tả mạng nghề nghiệp (tiếng Việt có dấu). Giữ section "Hồ sơ của bạn" + logic indexPage.
5. **Audit tiếng Việt:** không nhãn English; giữ chuỗi demo account như cũ.
KHÔNG sửa script page-component logic (loginPage/registerPage/indexPage). KHÔNG file backend. KHÔNG x-html.
  </action>
  <acceptance_criteria>
    - 3 trang: `grep -q 'id="pronav"'` và `grep -q 'window.proNav'`
    - 3 trang: `grep -q 'app.js?v=ph6-01'` và `grep -q 'styles.css?v=ph6-01'` và `! grep -q 'v1778837733'` (token cũ đã thay sạch)
    - 3 trang: `grep -q 'tailwind.config'` và `grep -q "navy:"` (navy token, D-01)
    - 3 trang: `! grep -q '<nav '` (navbar cũ đã xoá)
    - 3 trang: `grep -q 'pro-btn'` và `grep -q 'lang="vi"'` và `! grep -qi 'linkedin'`
  </acceptance_criteria>
  <verify>
    <automated>for f in index login register; do grep -q 'id="pronav"' web/$f.html && grep -q 'app.js?v=ph6-01' web/$f.html && grep -q 'styles.css?v=ph6-01' web/$f.html && ! grep -q 'v1778837733' web/$f.html && grep -q 'tailwind.config' web/$f.html && grep -q 'pro-btn' web/$f.html && ! grep -q '<nav ' web/$f.html || { echo "FAIL $f"; exit 1; }; done</automated>
  </verify>
  <done>index/login/register: navbar cũ thay bằng #pronav + proNav(''); navy Tailwind token inline; nút primary navy .pro-btn; cache-bust REPLACE token cũ → ?v=ph6-01; tiếng Việt; không LinkedIn; logic giữ nguyên.</done>
</task>

<task type="auto">
  <name>Task 2: profile + profile-edit — navbar chung + navy token + brand navy + cache-bust replace</name>
  <read_first>web/profile.html (toàn bộ — <nav> dòng 15-33 cũ chỉ có Xin chào/Đăng xuất, token cache-bust, các nút bg-slate-900), web/profile-edit.html (có x-data="navbar()" + bg-slate-900 + token cache-bust), web/assets/app.js (proNav)</read_first>
  <files>web/profile.html, web/profile-edit.html</files>
  <action>
Với CẢ HAI trang:
1. **<head> — cache-bust (REPLACE token đầy đủ):** Thay `app.js?v=ph5-06` → `app.js?v=ph6-01`; thay `styles.css?v1778837733` (và mọi `styles.css?v=...`) → `styles.css?v=ph6-01`. Không còn token cũ.
   **<head> — navy Tailwind token (D-01):** NGAY SAU `<script src="https://cdn.tailwindcss.com"></script>`, thêm `<script>tailwind.config = { theme:{ extend:{ colors:{ navy:{ DEFAULT:'#1e3a8a', dark:'#172f6b', light:'#3b5bbd' } } } } };</script>`. Giữ CDN, `lang="vi"`.
2. **Thay <nav>:** XOÁ `<nav x-data="navbar()">...</nav>` (profile.html dùng navbar rút gọn — thay luôn bằng navbar đầy đủ). Thay bằng `<div id="pronav"></div>` + `<script>document.addEventListener('DOMContentLoaded', () => window.proNav('profile'));</script>` trước `</body>`. proNav tự gọi `Alpine.initTree(#pronav)` (Plan 01) nên reactivity hoạt động bất kể thứ tự.
3. **Nút navy:** đổi nút primary (Kết nối / Chấp nhận / nút Lưu của profile-edit / các CTA `bg-slate-900 hover:bg-slate-700`) sang `pro-btn`. Các badge trạng thái quan hệ (Đã kết nối/Đã gửi lời mời/Chờ phản hồi…) GIỮ màu hiện tại (amber/green/slate) — chúng có ngữ nghĩa riêng. Tuỳ chọn: dải ảnh bìa dùng `pro-surface` cho nhất quán.
4. **profile.html:** giữ container `max-w-3xl` (trang hồ sơ KHÔNG cần 3 cột — chỉ feed cần, D-03). Dữ liệu user vẫn x-text (đã đúng — giữ).
5. **profile-edit.html:** giữ toàn bộ form + logic; chỉ thay nav + token + nút + cache-bust + audit tiếng Việt.
KHÔNG sửa profilePage()/script logic ngoài thay nav. KHÔNG file backend. KHÔNG x-html.
  </action>
  <acceptance_criteria>
    - 2 trang: `grep -q 'id="pronav"'` và `grep -q "proNav('profile')"`
    - 2 trang: `grep -q 'app.js?v=ph6-01'` và `grep -q 'styles.css?v=ph6-01'` và `! grep -q 'v1778837733'`
    - 2 trang: `grep -q 'tailwind.config'` và `grep -q "navy:"` và `! grep -q 'x-data="navbar()"'`
    - 2 trang: `grep -q 'pro-btn'` và `! grep -qi 'x-html'` và `! grep -qi 'linkedin'` và `grep -q 'lang="vi"'`
  </acceptance_criteria>
  <verify>
    <automated>for f in profile profile-edit; do grep -q 'id="pronav"' web/$f.html && grep -q 'app.js?v=ph6-01' web/$f.html && grep -q 'styles.css?v=ph6-01' web/$f.html && ! grep -q 'v1778837733' web/$f.html && grep -q 'tailwind.config' web/$f.html && ! grep -q 'x-data="navbar()"' web/$f.html && ! grep -qi 'x-html' web/$f.html || { echo "FAIL $f"; exit 1; }; done</automated>
  </verify>
  <done>profile/profile-edit: navbar đầy đủ dùng chung (proNav('profile')); navy Tailwind token inline; nút primary navy; cache-bust REPLACE → ?v=ph6-01; badge trạng thái giữ màu ngữ nghĩa; tiếng Việt; không x-html/LinkedIn/backend.</done>
</task>

<task type="auto">
  <name>Task 3: connections + search — navbar chung + navy token + brand navy + cache-bust replace</name>
  <read_first>web/connections.html (toàn bộ — <nav> dòng 15-67 đầy đủ, token cache-bust, nhiều nút bg-slate-900), web/search.html (toàn bộ — <nav> tương tự, token cache-bust, nút Tìm + Kết nối), web/assets/app.js (proNav)</read_first>
  <files>web/connections.html, web/search.html</files>
  <action>
Với CẢ HAI trang:
1. **<head> — cache-bust (REPLACE token đầy đủ):** Thay `app.js?v=ph5-06` → `app.js?v=ph6-01`; thay `styles.css?v1778837733` (và mọi `styles.css?v=...`) → `styles.css?v=ph6-01`. Không còn token cũ.
   **<head> — navy Tailwind token (D-01):** NGAY SAU `<script src="https://cdn.tailwindcss.com"></script>`, thêm `<script>tailwind.config = { theme:{ extend:{ colors:{ navy:{ DEFAULT:'#1e3a8a', dark:'#172f6b', light:'#3b5bbd' } } } } };</script>`. Giữ CDN, `lang="vi"`.
2. **Thay <nav>:** XOÁ khối `<nav x-data="navbar()">...</nav>` đầy đủ (đã chứa search/bell/Kết nối/đăng xuất — giờ navbar chung lo việc đó, tránh trùng lặp). Thay bằng `<div id="pronav"></div>` + `<script>document.addEventListener('DOMContentLoaded', () => window.proNav(ACTIVE));</script>` trước `</body>`. ACTIVE: connections → `'connections'`, search → `'search'`. proNav tự gọi `Alpine.initTree(#pronav)` (Plan 01) nên reactivity hoạt động bất kể thứ tự.
   - LƯU Ý: navbar chung đã có ô tìm kiếm + chuông + badge lời mời, nên xoá nav cũ KHÔNG mất chức năng — chỉ chuyển vào proNav.
3. **Nút navy:** đổi mọi nút primary `bg-slate-900 hover:bg-slate-700` (Chấp nhận / Kết nối / Tìm / sendInvite) sang `pro-btn`. Nút secondary (border: Từ chối/Huỷ lời mời/Xoá kết nối) giữ nguyên.
4. **search.html:** ô tìm kiếm chính trong THÂN trang giữ nguyên (đây là trang kết quả — vẫn cần ô tìm lớn); nút "Tìm" → pro-btn.
5. **Audit tiếng Việt** + không nhãn English; KHÔNG x-html (dữ liệu user đã x-text — giữ).
KHÔNG sửa connectionsPage()/searchPage() logic ngoài thay nav. KHÔNG file backend.
  </action>
  <acceptance_criteria>
    - connections: `grep -q "proNav('connections')"` ; search: `grep -q "proNav('search')"`
    - 2 trang: `grep -q 'id="pronav"'` và `grep -q 'app.js?v=ph6-01'` và `grep -q 'styles.css?v=ph6-01'` và `! grep -q 'v1778837733'` và `! grep -q 'x-data="navbar()"'`
    - 2 trang: `grep -q 'tailwind.config'` và `grep -q "navy:"`
    - 2 trang: `grep -q 'pro-btn'` và `! grep -qi 'x-html'` và `! grep -qi 'linkedin'` và `grep -q 'lang="vi"'`
    - search vẫn nối đúng: `grep -q "api.get('/search" web/search.html` ; connections vẫn: `grep -q 'connections/suggestions' web/connections.html`
  </acceptance_criteria>
  <verify>
    <automated>grep -q "proNav('connections')" web/connections.html && grep -q "proNav('search')" web/search.html && for f in connections search; do grep -q 'app.js?v=ph6-01' web/$f.html && grep -q 'styles.css?v=ph6-01' web/$f.html && ! grep -q 'v1778837733' web/$f.html && grep -q 'tailwind.config' web/$f.html && ! grep -q 'x-data="navbar()"' web/$f.html && ! grep -qi 'x-html' web/$f.html || { echo "FAIL $f"; exit 1; }; done</automated>
  </verify>
  <done>connections/search: navbar đầy đủ dùng chung (proNav('connections'/'search')); navy Tailwind token inline; nút primary navy; cache-bust REPLACE → ?v=ph6-01; endpoint cũ (/search, /connections/*) giữ nguyên; tiếng Việt; không x-html/LinkedIn/backend.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| browser → gateway | 7 trang gọi endpoint authenticated qua api wrapper (token chỉ ở header) |
| dev markup → DOM | navbar chung inject 1 lần qua proNav (markup tĩnh) + Alpine.initTree |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-10 | Tampering/Info (XSS) | render display_name/headline/kết quả tìm kiếm/profile | mitigate | Toàn bộ x-text (acceptance assert `! grep x-html` mỗi trang) |
| T-06-11 | Repudiation/Spoofing | Đăng xuất | mitigate | Đăng xuất nằm trong proNav (auth.logout xoá token) — đồng nhất 7 trang |
| T-06-12 | Legal | asset LinkedIn | mitigate | Logo tự thiết kế qua proNav; guard `! grep -i linkedin` mỗi trang |
| T-06-13 | Surface | đụng backend | accept | Source/production chỉ web/*.html (KHÔNG gateway/service/db/docker-compose/backend); `.planning/**` meta-docs là bookkeeping GSD bình thường, được miễn |
</threat_model>

<verification>
- `grep` xác nhận 7 trang: #pronav + proNav(active đúng), navy tailwind.config token, cache-bust REPLACE token mới ph6-01 (không còn token cũ v1778837733/ph5-06) cho cả app.js & styles.css, nút pro-btn, không `<nav>`/`navbar()` cũ, không x-html, không LinkedIn, lang="vi".
- Endpoint cũ (search, connections/suggestions, profile/full…) còn nguyên.
- Source/production CHỈ 7 file web/*.html (KHÔNG gateway/service/db/docker-compose/backend). `.planning/**` meta-docs là bookkeeping GSD bình thường, được miễn khỏi quy tắc này.

**Phạm vi sửa đổi (làm rõ):** Thay đổi PRODUCTION/source CHỈ giới hạn trong `web/*` + `scripts/*` (KHÔNG gateway/services/db/docker-compose/backend). Các meta-doc `.planning/**` (SUMMARY.md, STATE.md, ROADMAP.md, REQUIREMENTS.md) là artefact bookkeeping GSD bình thường, ĐƯỢC MIỄN khỏi quy tắc source-scope này. Quy tắc nói về source/backend, KHÔNG phải "chỉ web+scripts trong toàn bộ diff".
</verification>

<success_criteria>
- 7 trang dùng navbar chung (proNav) với active đúng, thương hiệu navy nhất quán (navy Tailwind token + .pro-btn), tagline+logo qua navbar.
- Cache-bust REPLACE token cũ → ?v=ph6-01 cho app.js + styles.css trên cả 7 trang; mọi chữ tiếng Việt có dấu.
- Mọi tính năng + endpoint cũ giữ nguyên; toàn x-text; không asset LinkedIn; không file backend.
</success_criteria>

<output>
After completion, create `.planning/phases/06-giao-di-n-proconnect/06-03-SUMMARY.md`
</output>
