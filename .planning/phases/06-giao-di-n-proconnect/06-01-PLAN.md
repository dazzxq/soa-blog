---
phase: 06-giao-di-n-proconnect
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/smoke-phase6.sh
  - web/assets/app.js
  - web/assets/styles.css
autonomous: true
requirements: [UI-01, UI-04, UI-05]
must_haves:
  truths:
    - "Mọi trang dùng MỘT navbar chung do window.proNav() render, không lặp markup"
    - "Logo mắt xích (inline SVG navy) + tagline tiếng Việt xuất hiện trong navbar"
    - "Navbar có ô tìm kiếm (Enter → /search.html?q=), chuông thông báo, badge lời mời, menu hồ sơ; logged-out → Đăng nhập/Đăng ký"
    - "Badge lời mời đếm GET /api/connections/requests?direction=incoming, poll ~15s"
    - "Menu hồ sơ Đăng xuất thực sự xoá token rồi điều hướng về /"
    - "Mọi dữ liệu người dùng render bằng x-text (không x-html), không lộ token/email trong markup"
  artifacts:
    - path: "scripts/smoke-phase6.sh"
      provides: "Static markup asserts + curl page-200 cho 8 trang"
      contains: "smoke6"
    - path: "web/assets/app.js"
      provides: "window.proNav(active) shared navbar + invite badge poll"
      contains: "window.proNav"
    - path: "web/assets/styles.css"
      provides: "Brand navy tokens + .pro-* helper classes"
      contains: "1e3a8a"
  key_links:
    - from: "web/assets/app.js (proNav)"
      to: "/api/connections/requests?direction=incoming"
      via: "api.get poll ~15s for invite badge count"
      pattern: "connections/requests\\?direction=incoming"
    - from: "web/assets/app.js (proNav notification bell)"
      to: "window.notificationBell"
      via: "reuse Phase-5 bell inside the shared navbar"
      pattern: "notificationBell"
---

<objective>
Xây nền tảng thương hiệu ProConnect và thanh điều hướng dùng chung — không build step.
Tạo `window.proNav(active)` (một navbar duy nhất, inject vào `<div id="pronav">` trên mọi trang ở Plan 02/03), thêm token màu navy `#1e3a8a` + logo mắt xích inline-SVG + tagline tiếng Việt vào `web/assets/app.js` / `web/assets/styles.css`, và viết hạ tầng kiểm thử Wave 0 `scripts/smoke-phase6.sh`.

Purpose: Đây là contract dùng chung. Plan 02 (feed) và Plan 03 (7 trang còn lại) chỉ việc đặt `<div id="pronav">` và gọi `proNav()` — không lặp lại 50 dòng markup navbar trên 8 file (D-02 DRY).
Output: smoke-phase6.sh + proNav() trong app.js + brand tokens trong styles.css.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-giao-di-n-proconnect/06-CONTEXT.md
@.planning/phases/06-giao-di-n-proconnect/06-VALIDATION.md

# Reference: cấu trúc navbar + notificationBell hiện có (sẽ chuyển vào proNav)
@web/assets/app.js
@web/feed.html

<interfaces>
<!-- Các global đã có trong web/assets/app.js (executor dùng trực tiếp, KHÔNG khám phá lại) -->
window.api    = { get(p), post(p,body), patch(p,body), delete(p) };   // wrap fetch, tự gắn Bearer token, ném lỗi có .status/.code
window.auth   = { token(), user(), isLoggedIn(), save(user,token), logout() };  // logout() xoá soa_blog_token + soa_blog_user khỏi localStorage
window.formatDate(iso) -> string (vi-VN)
window.notificationBell = function(){...}  // Alpine data: { open, items, unread, start(), markOne(id), markAll(), message(n) }; poll GET /api/notifications ~15s
// auth.user() trả { id, username, display_name, ... } từ localStorage (đã lưu khi login)

<!-- Endpoint navbar nối tới (đã build, Phase 3/5) -->
GET /api/connections/requests?direction=incoming  -> { data: [ { request_id, user_id, profile:{display_name,...} } ] }   // length = số lời mời chờ
GET /api/notifications -> dùng qua notificationBell
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wave 0 — scripts/smoke-phase6.sh (static markup asserts + page-200)</name>
  <read_first>scripts/smoke-phase5.sh (mẫu pass/fail + login + cấu trúc), web/feed.html (markup hiện tại), .planning/phases/06-giao-di-n-proconnect/06-VALIDATION.md (Per-Requirement Verification Map)</read_first>
  <files>scripts/smoke-phase6.sh</files>
  <action>
Tạo `scripts/smoke-phase6.sh` (bash, `set -euo pipefail`, hàm `pass()/fail()` đếm `FAILURES` y hệt smoke-phase5.sh). KHÔNG cần login/token cho phần static. Hai khối kiểm tra:

**A. STATIC MARKUP (chạy local, grep trên web/) — gate chính của Wave 0:**
Định nghĩa `PAGES="index login register feed profile profile-edit connections search"` (đường dẫn `web/$p.html`).
- UI-01 brand: với MỖI trang `web/$p.html`, assert `grep -q 'id="pronav"'` (placeholder navbar dùng chung). Assert `web/assets/app.js` chứa `window.proNav` VÀ `1e3a8a` (navy) VÀ một chuỗi SVG logo (`grep -q '<svg' web/assets/app.js`) VÀ tagline (`grep -qi 'Kết nối' web/assets/app.js`). Assert `web/assets/styles.css` chứa `1e3a8a`.
- UI-04 navbar wired: assert `web/assets/app.js` chứa `connections/requests?direction=incoming` (badge lời mời, D-06), `search.html?q=` (ô tìm kiếm), `notificationBell` (chuông), và `auth.logout` (menu hồ sơ đăng xuất).
- UI-05 tiếng Việt: với mỗi trang assert `grep -q 'lang="vi"' web/$p.html`. Assert KHÔNG có nhãn English lộ liễu: `! grep -qiE '>(Login|Logout|Sign in|Sign up|Search|Home|Profile|Settings)<' web/$p.html`.
- No-LinkedIn (legal): với mỗi trang VÀ app.js/styles.css assert `! grep -qi 'linkedin' "$f"`.
- Integrity: assert `! grep -qE '/api/(posts|comments)/?"' web/$p.html` không sinh ra (giữ nguyên — chỉ cảnh báo nếu xuất hiện endpoint chết); assert cache-bust đã bump: mỗi trang `grep -qE 'app\.js\?v=ph6' web/$p.html` và `grep -qE 'styles\.css\?v=ph6' web/$p.html` (Plan 02/03 sẽ bump; ở Wave 0 các assert này có thể FAIL — đó là kỳ vọng, smoke chạy thật ở Plan 04).
- Cân bằng thẻ HTML thô: mỗi trang số `<body` == số `</body>` (1==1).

**B. PAGE-200 (chạy trên VPS, cần biến môi trường) — chạy ở Plan 04 cutover:**
`BASE="${BASE:-https://soa.duyet.vn}"`; với mỗi trang `curl -s -o /dev/null -w '%{http_code}' "$BASE/$p.html"` (và `/` cho index) phải `200`. Bọc khối này trong `if [ "${RUN_HTTP:-0}" = "1" ]` để chạy local (static-only) không cần mạng; Plan 04 gọi `RUN_HTTP=1 bash scripts/smoke-phase6.sh`.

Kết thúc: in `[smoke6] ALL PASS` + `exit 0` nếu `FAILURES==0`, ngược lại `[smoke6] $FAILURES FAILURES` + `exit 1`. Đặt comment đầu file giải thích: static asserts là gate Wave 0; một số assert cache-bust/proNav sẽ chỉ xanh SAU khi Plan 02/03 hoàn tất (giống smoke-phase5 viết trước, xanh sau).
  </action>
  <acceptance_criteria>
    - `bash -n scripts/smoke-phase6.sh` (syntax OK)
    - `grep -q 'id="pronav"' scripts/smoke-phase6.sh` và `grep -q 'direction=incoming' scripts/smoke-phase6.sh` và `grep -q 'linkedin' scripts/smoke-phase6.sh` (có guard) và `grep -q 'lang="vi"' scripts/smoke-phase6.sh`
    - `grep -q 'RUN_HTTP' scripts/smoke-phase6.sh` (page-200 tách riêng cho cutover)
  </acceptance_criteria>
  <verify>
    <automated>bash -n scripts/smoke-phase6.sh && grep -q 'smoke6' scripts/smoke-phase6.sh && grep -q 'direction=incoming' scripts/smoke-phase6.sh</automated>
  </verify>
  <done>smoke-phase6.sh tồn tại, syntax hợp lệ, có static asserts (pronav/navy/svg/tagline/vi/no-linkedin/cache-bust) + page-200 gated bởi RUN_HTTP.</done>
</task>

<task type="auto">
  <name>Task 2: Brand tokens (navy #1e3a8a) + .pro-* helpers trong styles.css</name>
  <read_first>web/assets/styles.css (file hiện tại, chỉ 7 dòng), .planning/phases/06-giao-di-n-proconnect/06-CONTEXT.md (D-01)</read_first>
  <files>web/assets/styles.css</files>
  <action>
Thêm vào `web/assets/styles.css` (GIỮ nội dung cũ: font Inter, .prose-vn, .subtle, .fade-in):
- CSS vars `:root { --navy:#1e3a8a; --navy-hover:#172f6b; --navy-accent:#3b5bbd; --navy-surface:#eef2ff; }`
- Helper classes (vì Tailwind Play CDN không thấy được các class tuỳ biến trong file CSS — đây là các class CSS thuần dùng kèm Tailwind):
  - `.pro-btn { background:var(--navy); color:#fff; } .pro-btn:hover { background:var(--navy-hover); }`
  - `.pro-link-active { color:var(--navy); font-weight:600; }`
  - `.pro-badge { background:#dc2626; color:#fff; }` (badge đỏ cho số đếm — giữ tương phản)
  - `.pro-surface { background:var(--navy-surface); }`
- `[x-cloak]{display:none!important;}` (tránh nháy dropdown trước khi Alpine init).
KHÔNG dùng `@apply` (không có build). Đây là CSS thuần.
  </action>
  <acceptance_criteria>
    - `grep -q '1e3a8a' web/assets/styles.css` và `grep -q 'pro-btn' web/assets/styles.css` và `grep -q 'x-cloak' web/assets/styles.css`
    - Nội dung cũ còn nguyên: `grep -q 'prose-vn' web/assets/styles.css` và `grep -q 'fade-in' web/assets/styles.css`
  </acceptance_criteria>
  <verify>
    <automated>grep -q '1e3a8a' web/assets/styles.css && grep -q 'pro-btn' web/assets/styles.css && grep -q 'prose-vn' web/assets/styles.css</automated>
  </verify>
  <done>styles.css có navy tokens + .pro-* helpers + x-cloak, giữ toàn bộ style cũ.</done>
</task>

<task type="auto">
  <name>Task 3: window.proNav(active) — shared navbar (logo SVG, tagline, search, bell, invite badge, profile menu)</name>
  <read_first>web/assets/app.js (toàn bộ — đặc biệt notificationBell, auth, api, navbar cũ), web/feed.html dòng 15-67 (markup navbar hiện tại để bê nguyên hành vi), .planning/phases/06-giao-di-n-proconnect/06-CONTEXT.md (D-02, D-06)</read_first>
  <files>web/assets/app.js</files>
  <action>
Thêm vào CUỐI IIFE trong `web/assets/app.js` (trước `})();`), một hàm `window.proNav = function (active) {...}` render navbar dùng chung. Cách inject (no-build, no innerHTML cho dữ liệu người dùng): proNav trả về MỘT chuỗi HTML TĨNH (chỉ chứa markup Alpine `x-data/x-text/x-if`, KHÔNG nội suy dữ liệu user vào string), và một initializer gắn vào `<div id="pronav">`. Mẫu cụ thể:

```js
window.proNav = function (active) {
  // active: 'feed' | 'connections' | 'search' | 'profile' | '' (để tô đậm link hiện tại)
  var el = document.getElementById('pronav');
  if (!el) return;
  el.setAttribute('x-data', 'proNavData("' + (active || '') + '")');
  el.setAttribute('x-init', 'init()');
  el.innerHTML = PRONAV_HTML;   // markup TĨNH — mọi dữ liệu user render qua x-text bên trong, KHÔNG nội suy chuỗi
};
```

`PRONAV_HTML` (hằng chuỗi trong app.js) gồm:
- `<nav class="bg-white border-b sticky top-0 z-20">` chứa `<div class="max-w-5xl mx-auto px-4 py-2.5 flex items-center justify-between gap-4">`.
- TRÁI: `<a href="/feed.html" class="flex items-center gap-2">` + INLINE SVG logo mắt xích navy (hai vòng lồng nhau, dùng `stroke="#1e3a8a"` — ví dụ hai `<circle>`/`<ellipse>` hoặc hai `<rect rx>` lồng nhau, viewBox 0 0 24 24, w-7 h-7) + `<span class="font-bold text-lg" style="color:#1e3a8a">ProConnect</span>` + `<span class="hidden md:inline text-xs subtle">Kết nối chuyên nghiệp</span>` (tagline tiếng Việt).
- GIỮA (chỉ khi đăng nhập): `<form @submit.prevent="...search.html?q=...">` với input `x-ref="nq"` placeholder "Tìm người, kỹ năng…" (bê từ navbar cũ, dùng `encodeURIComponent($refs.nq.value)`).
- PHẢI (logged-in, `<template x-if="isLoggedIn">`):
  - Link "Bảng tin" `:class="active==='feed' ? 'pro-link-active' : 'hover:underline'"`, "Kết nối" tương tự với badge lời mời: `<a href="/connections.html" class="relative ...">Kết nối <span x-show="invites>0" class="pro-badge absolute -top-2 -right-3 text-xs rounded-full min-w-[18px] h-[18px] px-1 inline-flex items-center justify-center" x-text="invites"></span></a>`.
  - Chuông thông báo: bê NGUYÊN khối `<div x-data="notificationBell()" x-init="start()">...` từ feed.html (giữ x-text, dropdown). (notificationBell đã tồn tại — KHÔNG sửa.)
  - Menu hồ sơ: `<div class="relative" x-data="{m:false}">` nút avatar/tên (`<span x-text="me.display_name || me.username"></span>` — `me` lấy từ proNavData) → dropdown `<a :href="'/profile.html?id='+me.id">Hồ sơ của tôi</a>`, `<a href="/profile-edit.html">Chỉnh sửa hồ sơ</a>`, `<button @click="logout()">Đăng xuất</button>`.
- PHẢI (logged-out, `<template x-if="!isLoggedIn">`): `<a href="/login.html">Đăng nhập</a>` + `<a href="/register.html" class="pro-btn px-3 py-1.5 rounded">Đăng ký</a>`.

Thêm cũng `window.proNavData = function (active) {...}` (Alpine component):
```js
window.proNavData = function (active) {
  return {
    active: active,
    isLoggedIn: auth.isLoggedIn(),
    me: auth.user() || {},
    invites: 0,
    timer: null,
    init() {
      if (!this.isLoggedIn) return;
      this.loadInvites();
      this.timer = setInterval(() => this.loadInvites(), 15000); // D-06 ~15s
    },
    async loadInvites() {
      try {
        const r = await api.get('/connections/requests?direction=incoming'); // D-06
        this.invites = (r.data || []).length;
      } catch (e) { /* degrade: giữ badge cũ */ }
    },
    logout() { auth.logout(); window.location.href = '/'; },  // xoá token thật rồi điều hướng
  };
};
```

QUAN TRỌNG (XSS / threat model): MỌI dữ liệu user (display_name, tagline động nếu có, message thông báo) phải render bằng `x-text`, KHÔNG `x-html`, KHÔNG nội suy vào `innerHTML` string. `PRONAV_HTML` là markup tĩnh do dev viết — an toàn. KHÔNG đặt token vào markup. KHÔNG sửa file gateway/service/db nào.

Giữ `window.navbar` cũ (các trang chưa migrate vẫn dùng được tới khi Plan 02/03 thay) — hoặc xoá nếu chắc chắn 8 trang đều chuyển sang proNav ở Plan 02/03; để an toàn GIỮ lại, vô hại.
  </action>
  <acceptance_criteria>
    - `grep -q 'window.proNav' web/assets/app.js` và `grep -q 'window.proNavData' web/assets/app.js`
    - `grep -q '<svg' web/assets/app.js` và `grep -q '1e3a8a' web/assets/app.js` và `grep -qi 'Kết nối chuyên nghiệp' web/assets/app.js`
    - `grep -q 'direction=incoming' web/assets/app.js` và `grep -q '15000' web/assets/app.js` (poll ~15s)
    - `grep -q 'auth.logout' web/assets/app.js` (menu hồ sơ đăng xuất xoá token)
    - `! grep -qi 'x-html' web/assets/app.js` và `! grep -qi 'linkedin' web/assets/app.js`
    - `node -e "new Function(require('fs').readFileSync('web/assets/app.js','utf8'))"` (parse OK — không lỗi cú pháp JS)
  </acceptance_criteria>
  <verify>
    <automated>node -e "new Function(require('fs').readFileSync('web/assets/app.js','utf8'))" && grep -q 'window.proNav' web/assets/app.js && grep -q 'direction=incoming' web/assets/app.js && ! grep -qi 'x-html' web/assets/app.js</automated>
  </verify>
  <done>proNav() + proNavData() có trong app.js: logo SVG navy + tagline, search→/search.html?q=, chuông (reuse notificationBell), badge lời mời poll ~15s GET /api/connections/requests?direction=incoming, menu hồ sơ (Hồ sơ của tôi/Chỉnh sửa/Đăng xuất xoá token); logged-out → Đăng nhập/Đăng ký; toàn bộ x-text, không x-html, không LinkedIn, không file backend.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| browser → gateway API | proNav badge/bell gọi endpoint authenticated; token ở localStorage, chỉ gắn qua header bởi api wrapper |
| dev markup → DOM | PRONAV_HTML là chuỗi tĩnh do dev viết, inject 1 lần qua innerHTML |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Tampering/Info (XSS) | proNav render display_name + notif message | mitigate | Mọi dữ liệu user render bằng x-text; PRONAV_HTML tĩnh không nội suy dữ liệu; KHÔNG x-html (smoke assert `! grep x-html`) |
| T-06-02 | Info Disclosure | token trong markup | mitigate | Token chỉ ở localStorage + header Bearer (api wrapper); KHÔNG in token vào HTML |
| T-06-03 | Repudiation/Spoofing | Đăng xuất không xoá phiên | mitigate | proNavData.logout() gọi auth.logout() (xoá soa_blog_token+user) rồi điều hướng; smoke assert `auth.logout` hiện diện |
| T-06-04 | Legal (trademark) | dùng asset LinkedIn | mitigate | Logo tự thiết kế inline SVG mắt xích; smoke guard `! grep -i linkedin` trên mọi file |
| T-06-05 | Elevation/Surface | thêm bề mặt backend | accept | Phase chỉ sửa web/* + scripts/*; KHÔNG đụng gateway/service/db (files_modified khẳng định điều này) |
</threat_model>

<verification>
- `bash -n scripts/smoke-phase6.sh` OK; `node -e "new Function(...app.js)"` parse OK.
- `grep` xác nhận proNav/proNavData/navy/SVG/tagline/invite-poll/logout có trong app.js; navy token + .pro-* trong styles.css.
- Khẳng định files_modified CHỈ gồm scripts/* + web/assets/* (KHÔNG gateway/services/db).
</verification>

<success_criteria>
- window.proNav(active) render navbar dùng chung đầy đủ (logo SVG navy + tagline VN, search→/search.html?q=, chuông reuse notificationBell, badge lời mời poll ~15s GET /api/connections/requests?direction=incoming, menu hồ sơ với Đăng xuất xoá token; logged-out Đăng nhập/Đăng ký).
- Brand navy #1e3a8a tokens + .pro-* helpers trong styles.css.
- scripts/smoke-phase6.sh (Wave 0) sẵn sàng: static asserts + page-200 (gated RUN_HTTP).
- Toàn bộ x-text (không x-html), không LinkedIn asset, không file backend nào bị đụng.
</success_criteria>

<output>
After completion, create `.planning/phases/06-giao-di-n-proconnect/06-01-SUMMARY.md`
</output>
