---
phase: 02-h-s-ngh-nghi-p
plan: 02
type: execute
wave: 1
depends_on: [01]
files_modified:
  - services/profile-service/src/routes.php
  - services/profile-service/src/Controllers/UserController.php
  - services/profile-service/src/Controllers/ProfileController.php
autonomous: true
requirements: [PROF-02, PROF-03, PROF-04, PROF-05, PROF-06]
must_haves:
  truths:
    - "profile-service assembles GET /users/{id}/full (basic + experience + education + skills) in its own bounded context"
    - "GET /users/{id}/full never returns email or password_hash (public-safe projection)"
    - "Owner can create/update/delete experience + education scoped strictly by X-User-Id"
    - "Owner can add/remove skills; duplicate skill returns 409"
    - "PATCH /users/{id} accepts the 4 new basic fields (cover_url, headline, location, about)"
    - "Every write is scoped by X-User-Id; a caller can never mutate another user's rows"
  artifacts:
    - path: "services/profile-service/src/Controllers/ProfileController.php"
      provides: "Experience/education/skills CRUD + full() assembly, all X-User-Id scoped"
      min_lines: 120
    - path: "services/profile-service/src/routes.php"
      provides: "Mirror routes /users/{id}/full|experience|education|skills"
      contains: "/users/{id:[0-9]+}/full"
  key_links:
    - from: "services/profile-service/src/Controllers/ProfileController.php"
      to: "experience/education/skills tables"
      via: "PDO prepared statements scoped WHERE user_id = X-User-Id"
      pattern: "X-User-Id"
    - from: "ProfileController::full"
      to: "users + child tables"
      via: "public-column SELECT (no email/password_hash) + child fetches"
      pattern: "experience|education|skills"
---

<objective>
Build the profile-service data layer (D-05..D-08, D-11, D-12): extend basic-profile editing with the 4 new fields, add owner-scoped CRUD for experience/education/skills, and add the intra-service `GET /users/{id}/full` assembly that the gateway will compose across services. profile-service owns its bounded context; the gateway (Plan 03) composes ACROSS services.

Purpose: the live migration (Plan 01) created the schema; this plan makes profile-service read/write it. Every write is scoped by the gateway-trusted `X-User-Id` header (never `user_id` from body) — the IDOR mitigation. `/full` emits a public-safe projection so the gateway's public route can never leak email.
Output: ProfileController.php (new), UserController.php (extended update), routes.php (mirror routes).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-h-s-ngh-nghi-p/02-RESEARCH.md

<interfaces>
<!-- Contracts the executor needs; extracted from the codebase. Use directly, no exploration. -->

profile-service helpers (services/profile-service/src/):
- `App\Db::pdo(): PDO` — lazy singleton, ATTR_EMULATE_PREPARES=false (real prepared statements; bind each placeholder once).
- `App\Json::ok(Response $res, mixed $data, int $status=200): Response`
- `App\Json::list(Response $res, array $items, array $meta): Response`
- `App\DomainError(int $status, string $code, string $message)` — Vietnamese message; rendered by JsonErrorHandler as `{error:{code,message}}`.

Existing UserController precedents (services/profile-service/src/Controllers/UserController.php):
- `private find(int $id): ?array` — currently SELECTs id, username, EMAIL, display_name, avatar_url, created_at. (Do NOT reuse this for /full — it leaks email.)
- `update()` owner-scope guard (lines 168-172): `$callerId=(int)($req->getHeaderLine('X-User-Id')?:0); if($callerId===0||$callerId!==$id) throw new DomainError(403,'FORBIDDEN','Bạn chỉ có thể chỉnh sửa hồ sơ của chính mình.');`
- 409 duplicate precedent (create lines 57-61): USER_EXISTS pattern.

Schema (from Plan 01 migration):
- users + cover_url VARCHAR(512), headline VARCHAR(160), location VARCHAR(128), about TEXT
- experience(id,user_id,company,title,start_date DATE,end_date DATE NULL,description TEXT NULL)
- education(id,user_id,school,degree NULL,field NULL,start_year SMALLINT NULL,end_year SMALLINT NULL)
- skills(id,user_id,name) UNIQUE(user_id,name)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend UserController::update with 4 new basic fields + add public full() projection</name>
  <read_first>
    - services/profile-service/src/Controllers/UserController.php (FULL — update() lines 165-207 conditional-set pattern; find() lines 209-218 leaks email)
    - 02-RESEARCH.md Pattern 5 (full assembly) + Pitfall 2 (email leak) + Pitfall 6 (date validation)
  </read_first>
  <files>services/profile-service/src/Controllers/UserController.php</files>
  <action>
    Extend `update()` (D-05, PROF-02): add four MORE `array_key_exists` branches mirroring the existing `display_name`/`avatar_url` conditional-set style. For each, validate then append to `$sets`/`$params`:
    - `cover_url`: nullable; if non-null, `strlen <= 512 && filter_var(..., FILTER_VALIDATE_URL)` else `400 VALIDATION_FAILED 'Cover URL không hợp lệ.'`
    - `headline`: nullable; if non-null `mb_strlen <= 160` else `400 ... 'Chức danh tối đa 160 ký tự.'`
    - `location`: nullable; if non-null `mb_strlen <= 128` else `400 ... 'Vị trí tối đa 128 ký tự.'`
    - `about`: nullable; if non-null `mb_strlen <= 5000` else `400 ... 'Giới thiệu quá dài.'`
    Keep the existing owner-scope guard and the "no fields → return existing" early-out. After update, return the refreshed row.

    Add a NEW public method `full(Request $req, Response $res, array $args): Response` (D-12, PROF-06) — the intra-service assembly used by the gateway:
    - `$id = (int)$args['id'];`
    - PUBLIC projection (Pitfall 2 — NO email, NO password_hash): `$stmt = Db::pdo()->prepare('SELECT id, username, display_name, avatar_url, cover_url, headline, location, about, created_at FROM users WHERE id = :id LIMIT 1'); $stmt->execute([':id'=>$id]); $user = $stmt->fetch();`
    - if `$user === false` → `throw new DomainError(404,'USER_NOT_FOUND','Không tìm thấy người dùng.')`
    - fetch children (ORDER per RESEARCH Pattern 5):
      `experience`: `SELECT id,company,title,start_date,end_date,description FROM experience WHERE user_id=:u ORDER BY start_date DESC`
      `education`: `SELECT id,school,degree,field,start_year,end_year FROM education WHERE user_id=:u ORDER BY start_year DESC`
      `skills`: `SELECT id,name FROM skills WHERE user_id=:u ORDER BY name`
    - assign `$user['experience']`, `$user['education']`, `$user['skills']` from `fetchAll()`; `return Json::ok($res, $user);`

    Also update `find()` only if needed by callers — DO NOT add email to /full. Leave find() as-is (it backs the email-trimmed public /api/profiles/{id} basic route at the gateway).
  </action>
  <verify>
    <automated>php -l services/profile-service/src/Controllers/UserController.php</automated>
  </verify>
  <acceptance_criteria>
    - `php -l services/profile-service/src/Controllers/UserController.php` → "No syntax errors detected"
    - `grep -q "function full" services/profile-service/src/Controllers/UserController.php`
    - full() projection excludes email/password_hash: `grep -A3 "function full" services/profile-service/src/Controllers/UserController.php` (manual: SELECT list has NO `email`); machine check: the SELECT inside full must not list email — `awk '/function full/,/return Json::ok/' services/profile-service/src/Controllers/UserController.php | grep -q 'SELECT id, username, display_name, avatar_url, cover_url, headline, location, about'`
    - `grep -cE "array_key_exists\('(cover_url|headline|location|about)'" services/profile-service/src/Controllers/UserController.php` equals 4
    - `grep -q "FROM experience" services/profile-service/src/Controllers/UserController.php && grep -q "FROM education" services/profile-service/src/Controllers/UserController.php && grep -q "FROM skills" services/profile-service/src/Controllers/UserController.php`
  </acceptance_criteria>
  <done>update() accepts and validates the 4 new fields; full() assembles a public-safe (no email) basic + experience + education + skills body; php -l clean.</done>
</task>

<task type="auto">
  <name>Task 2: New ProfileController — experience/education/skills CRUD, X-User-Id scoped</name>
  <read_first>
    - services/profile-service/src/Controllers/UserController.php (owner-scope guard lines 168-172; 409 USER_EXISTS lines 57-61; conditional-set validation style)
    - 02-RESEARCH.md Pattern 6 (owner-scoped writes), §"Skills duplicate handling" code, Pitfall 6 (date/year validation)
  </read_first>
  <files>services/profile-service/src/Controllers/ProfileController.php</files>
  <action>
    Create `services/profile-service/src/Controllers/ProfileController.php` (namespace `App\Controllers`, `final class ProfileController`, `declare(strict_types=1)`). Use `App\Db`, `App\Json`, `App\DomainError`, PDO. NO constructor needed (uses Db singleton, like UserController). Every method reads `$callerId = (int)($req->getHeaderLine('X-User-Id') ?: 0)` and the path `$id = (int)$args['id']`; reject if `$callerId === 0 || $callerId !== $id` with `403 FORBIDDEN 'Bạn chỉ có thể chỉnh sửa hồ sơ của chính mình.'` (D-11 — NEVER trust user_id from body). This is the IDOR mitigation (T-02-01).

    EXPERIENCE (PROF-03):
    - `addExperience(req,res,args)`: validate body — `company` (1-160, required), `title` (1-160, required), `start_date` required `YYYY-MM-DD` (`preg_match('/^\d{4}-\d{2}-\d{2}$/')` else 400 VALIDATION_FAILED 'Ngày bắt đầu phải dạng YYYY-MM-DD.'), `end_date` either null/'' (→NULL) or valid YYYY-MM-DD, `description` optional (<=5000). INSERT with `user_id = :caller` (NOT from body). Return `Json::ok($res, [...inserted row with lastInsertId...], 201)`.
    - `updateExperience(req,res,args)` (path `{id}` = user, `{eid}` = row): conditional-set on company/title/start_date/end_date/description with same validation; UPDATE `... WHERE id=:eid AND user_id=:caller`; if `rowCount===0` and the row does not belong to caller → fetch to distinguish; simplest: if affected 0 and no such row for caller → `404 EXPERIENCE_NOT_FOUND 'Không tìm thấy mục kinh nghiệm.'`. Return updated row.
    - `deleteExperience(req,res,args)`: `DELETE FROM experience WHERE id=:eid AND user_id=:caller`; if `rowCount===0` → `404 EXPERIENCE_NOT_FOUND`. Return `Json::ok($res, ['deleted'=>true])`.

    EDUCATION (PROF-04): analogous methods `addEducation`/`updateEducation`/`deleteEducation`. Fields: `school` (1-160 required), `degree` (<=160 nullable), `field` (<=160 nullable), `start_year`/`end_year` (nullable SMALLINT; if present validate integer 1900-2100 else 400 'Năm không hợp lệ.'). Same `WHERE id=:eid AND user_id=:caller` scoping; `404 EDUCATION_NOT_FOUND 'Không tìm thấy mục học vấn.'`.

    SKILLS (PROF-05 — add/remove only, NO update, D-08/D-10):
    - `addSkill(req,res,args)`: `$name = trim((string)($b['name']??''));` validate `$name !== '' && mb_strlen($name) <= 80` else `400 VALIDATION_FAILED 'Tên kỹ năng từ 1-80 ký tự.'`. INSERT `(user_id, name) VALUES (:caller,:name)` wrapped in try/catch: on `\PDOException` with `getCode()==='23000'` → `409 SKILL_EXISTS 'Kỹ năng đã tồn tại.'` (dedupe, T-02-04); else rethrow. Return inserted `{id,name}` 201.
    - `deleteSkill(req,res,args)` (`{sid}`): `DELETE FROM skills WHERE id=:sid AND user_id=:caller`; `rowCount===0` → `404 SKILL_NOT_FOUND 'Không tìm thấy kỹ năng.'`. Return `{deleted:true}`.

    Use prepared statements everywhere (bind each placeholder once — EMULATE_PREPARES=false). Vietnamese messages.
  </action>
  <verify>
    <automated>php -l services/profile-service/src/Controllers/ProfileController.php</automated>
  </verify>
  <acceptance_criteria>
    - `php -l services/profile-service/src/Controllers/ProfileController.php` → "No syntax errors detected"
    - every write method scopes by caller: `grep -c "X-User-Id" services/profile-service/src/Controllers/ProfileController.php` >= 1 AND `grep -c "WHERE id=:eid AND user_id=:caller\|WHERE id=:sid AND user_id=:caller\|user_id = :caller\|user_id=:caller" services/profile-service/src/Controllers/ProfileController.php` >= 6
    - no body user_id trust: `! grep -q "\['user_id'\]" services/profile-service/src/Controllers/ProfileController.php`
    - skills 409: `grep -q "SKILL_EXISTS" services/profile-service/src/Controllers/ProfileController.php && grep -q "23000" services/profile-service/src/Controllers/ProfileController.php`
    - methods present: `grep -cE "function (addExperience|updateExperience|deleteExperience|addEducation|updateEducation|deleteEducation|addSkill|deleteSkill)" services/profile-service/src/Controllers/ProfileController.php` equals 8
  </acceptance_criteria>
  <done>ProfileController has 8 CRUD methods, all writes scoped by X-User-Id (never body user_id), skills dedupe returns 409, php -l clean.</done>
</task>

<task type="auto">
  <name>Task 3: Register mirror routes in profile-service routes.php</name>
  <read_first>
    - services/profile-service/src/routes.php (FULL — existing /users routes, `{id:[0-9]+}` constraint)
    - 02-RESEARCH.md Pattern 5 (route names) + D-10/D-12
  </read_first>
  <files>services/profile-service/src/routes.php</files>
  <action>
    Add `use App\Controllers\ProfileController;` and register the mirror routes (D-12). Keep `{id:[0-9]+}` numeric constraints. Add AFTER the existing `/users/{id:[0-9]+}` routes:

    ```php
    // Phase 2 — full assembly (intra-service; gateway composes across services)
    $app->get   ('/users/{id:[0-9]+}/full',                       [UserController::class,    'full']);

    // Experience (owner-scoped by X-User-Id at the controller)
    $app->post  ('/users/{id:[0-9]+}/experience',                 [ProfileController::class, 'addExperience']);
    $app->patch ('/users/{id:[0-9]+}/experience/{eid:[0-9]+}',    [ProfileController::class, 'updateExperience']);
    $app->delete('/users/{id:[0-9]+}/experience/{eid:[0-9]+}',    [ProfileController::class, 'deleteExperience']);

    // Education
    $app->post  ('/users/{id:[0-9]+}/education',                  [ProfileController::class, 'addEducation']);
    $app->patch ('/users/{id:[0-9]+}/education/{eid:[0-9]+}',     [ProfileController::class, 'updateEducation']);
    $app->delete('/users/{id:[0-9]+}/education/{eid:[0-9]+}',     [ProfileController::class, 'deleteEducation']);

    // Skills (add / remove only)
    $app->post  ('/users/{id:[0-9]+}/skills',                     [ProfileController::class, 'addSkill']);
    $app->delete('/users/{id:[0-9]+}/skills/{sid:[0-9]+}',        [ProfileController::class, 'deleteSkill']);
    ```

    profile-service has NO container (manual wiring) — Slim resolves `[Controller::class,'method']` to `new Controller()` via its default callable resolver, matching how UserController works today. No DI edit needed.
  </action>
  <verify>
    <automated>php -l services/profile-service/src/routes.php</automated>
  </verify>
  <acceptance_criteria>
    - `php -l services/profile-service/src/routes.php` → "No syntax errors detected"
    - `grep -q "/users/{id:\[0-9\]+}/full" services/profile-service/src/routes.php`
    - all 8 CRUD routes present: `grep -cE "/users/\{id:\[0-9\]\+\}/(experience|education|skills)" services/profile-service/src/routes.php` >= 8
    - `grep -q "use App\\\\Controllers\\\\ProfileController;" services/profile-service/src/routes.php`
  </acceptance_criteria>
  <done>routes.php registers /users/{id}/full + 8 experience/education/skills CRUD mirror routes with numeric constraints; php -l clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| gateway → profile-service (X-User-Id header) | Services trust the gateway-set X-User-Id on the isolated network; they never verify JWT |
| request body → SQL | Untrusted field values reach prepared statements |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Elevation/Tampering (IDOR) | ProfileController writes | mitigate | Every write scoped by `X-User-Id`; `WHERE id=:eid AND user_id=:caller`; never read `user_id` from body. Acceptance grep forbids `$b['user_id']`. |
| T-02-02 | Information disclosure | UserController::full projection | mitigate | full() SELECTs a public column set (no email/password_hash). Gateway adds defense-in-depth allowlist in Plan 03. |
| T-02-03 | Tampering (SQLi) | all queries | mitigate | PDO prepared statements, EMULATE_PREPARES=false; never interpolate. |
| T-02-04 | Tampering (dup skill) | addSkill | mitigate | UNIQUE(user_id,name) + catch SQLSTATE 23000 → 409 SKILL_EXISTS. |
</threat_model>

<verification>
- `php -l` clean on all 3 files.
- Runtime CRUD round-trips verified by scripts/smoke-phase2.sh on the deployed stack (Plan 05).
- This service layer is consumed by the gateway in Plan 03.
</verification>

<success_criteria>
- profile-service exposes /users/{id}/full (public-safe) + owner-scoped experience/education/skills CRUD + extended basic update.
- All writes scoped by X-User-Id; skills dedupe 409.
</success_criteria>

<output>
After completion, create `.planning/phases/02-h-s-ngh-nghi-p/02-02-SUMMARY.md`
</output>
