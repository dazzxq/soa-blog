---
phase: 02-h-s-ngh-nghi-p
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - db/02-migrate-phase2.sql
  - db/01-schema-profile.sql
  - db/99-seed.sql
  - scripts/deploy.sh
  - scripts/smoke-phase2.sh
autonomous: true
requirements: [PROF-02, PROF-03, PROF-04, PROF-05, PROF-07]
must_haves:
  truths:
    - "The live MariaDB volume gains the 4 new user columns + 3 new tables without dropping existing data"
    - "A fresh-volume docker compose up produces the SAME end schema as the migrated live volume"
    - "deploy.sh applies db/02-migrate-phase2.sql against the running mariadb after the Phase-1 migrate step"
    - "5 demo accounts have sample experience/education/skills content idempotently"
    - "scripts/smoke-phase2.sh exists and encodes every PROF-02..07 + email-leak + owner-scope + degrade assertion"
  artifacts:
    - path: "db/02-migrate-phase2.sql"
      provides: "Idempotent live-volume migration (ADD COLUMN IF NOT EXISTS / CREATE TABLE IF NOT EXISTS) + idempotent demo seed"
      contains: "ADD COLUMN IF NOT EXISTS cover_url"
    - path: "scripts/smoke-phase2.sh"
      provides: "Bash/curl smoke harness for Phase 2"
      min_lines: 80
    - path: "scripts/deploy.sh"
      provides: "Wired phase-2 migration apply step"
      contains: "02-migrate-phase2.sql"
  key_links:
    - from: "scripts/deploy.sh"
      to: "db/02-migrate-phase2.sql"
      via: "docker compose exec -T mariadb mysql ... < db/02-migrate-phase2.sql"
      pattern: "02-migrate-phase2.sql"
---

<objective>
Wave 0 BLOCKING infrastructure: ship the idempotent live-DB migration (the single highest-risk item — Phase 2's repeat of the Phase-1 fresh-volume pitfall) and the Phase 2 smoke harness. NO runtime endpoint can be verified until the migration is wired and the smoke script exists.

Purpose: profile-service's DB is ALREADY LIVE on the VPS (proconnect_profile.users, 5 rows). `db/*.sql` + `db/00-init.sh` run ONLY on a fresh MariaDB volume, so a repo-only schema edit deploys green but 500s at runtime on `SELECT cover_url ...` / `SELECT ... FROM experience`. This plan fixes that with a non-destructive idempotent migration applied via deploy.sh, keeps the fresh-volume schema file in sync, and seeds demo content so the presentation has data.
Output: db/02-migrate-phase2.sql, db/01-schema-profile.sql (kept in sync), db/99-seed.sql (kept in sync), scripts/deploy.sh (wired), scripts/smoke-phase2.sh.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-h-s-ngh-nghi-p/02-RESEARCH.md
@.planning/phases/02-h-s-ngh-nghi-p/02-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Idempotent live migration + fresh-volume schema sync + demo seed</name>
  <read_first>
    - db/01-schema-profile.sql (current users table — 6 columns, to extend)
    - db/99-seed.sql (5 demo accounts id 1..5; INSERT IGNORE pattern to mirror)
    - db/migrate-phase1.sql.tmpl (idempotent ALTER/CREATE precedent + the `.tmpl` rationale — Phase 2 needs PLAIN `.sql`, no envsubst, because it has NO secret placeholders)
    - 02-RESEARCH.md §"Idempotent live migration (db/02-migrate-phase2.sql)" (the exact verified DDL) + Pitfall 1 + Open Question 2 (seed)
  </read_first>
  <files>db/02-migrate-phase2.sql, db/01-schema-profile.sql, db/99-seed.sql</files>
  <action>
    Create `db/02-migrate-phase2.sql` (PLAIN .sql — NOT .tmpl, no envsubst placeholders, no secrets) starting with `USE proconnect_profile;` then, idempotently:

    1. Extend `users` (D-05):
       `ALTER TABLE users ADD COLUMN IF NOT EXISTS cover_url VARCHAR(512) NULL AFTER avatar_url, ADD COLUMN IF NOT EXISTS headline VARCHAR(160) NULL, ADD COLUMN IF NOT EXISTS location VARCHAR(128) NULL, ADD COLUMN IF NOT EXISTS about TEXT NULL;`
    2. `CREATE TABLE IF NOT EXISTS experience` (D-06): id BIGINT UNSIGNED PK AUTO_INCREMENT, user_id BIGINT UNSIGNED NOT NULL, company VARCHAR(160) NOT NULL, title VARCHAR(160) NOT NULL, start_date DATE NOT NULL, end_date DATE NULL, description TEXT NULL, created_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP, INDEX idx_exp_user (user_id). ENGINE=InnoDB CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci.
    3. `CREATE TABLE IF NOT EXISTS education` (D-07): id PK AUTO_INCREMENT, user_id NOT NULL, school VARCHAR(160) NOT NULL, degree VARCHAR(160) NULL, field VARCHAR(160) NULL, start_year SMALLINT NULL, end_year SMALLINT NULL, created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP, INDEX idx_edu_user (user_id). Same engine/charset.
    4. `CREATE TABLE IF NOT EXISTS skills` (D-08): id PK AUTO_INCREMENT, user_id NOT NULL, name VARCHAR(80) NOT NULL, created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP, UNIQUE KEY uq_skill_user_name (user_id, name), INDEX idx_skill_user (user_id). Same engine/charset.
    5. Idempotent DEMO SEED (CONTEXT "Specific Ideas" + RESEARCH Open Q2) so the LIVE volume gets content (99-seed.sql will NOT reach the existing volume — same Pitfall-1 reason): use deterministic explicit ids so re-runs do not duplicate. For users 1 (demo) and 2 (duyet): `UPDATE users SET headline=..., location=..., about=... WHERE id IN (1,2) AND headline IS NULL;` (Vietnamese, with diacritics, e.g. duyet headline "Sinh viên PTIT — Software Engineer"). Insert a few experience/education/skills rows with explicit ids guarded so reruns are no-ops, e.g. `INSERT INTO experience (id,user_id,company,title,start_date,end_date,description) SELECT 1,2,'PTIT','Sinh viên CNTT','2022-09-01',NULL,'Học SOA' WHERE NOT EXISTS (SELECT 1 FROM experience WHERE id=1);` (repeat for 2-3 exp, 1-2 edu, 3-4 skills across users 1 and 2). All Vietnamese with diacritics. Keep it SMALL (a handful of rows).

    Then MIRROR the end-state into the fresh-volume files so a from-scratch `docker compose up` converges to the same schema:
    - `db/01-schema-profile.sql`: add the 4 columns to the `CREATE TABLE users` definition AND append the 3 `CREATE TABLE IF NOT EXISTS` blocks (experience/education/skills) identical to the migration.
    - `db/99-seed.sql`: append the same demo exp/edu/skills seed rows using `INSERT IGNORE` (matching the existing file's INSERT IGNORE style) so fresh volumes also get demo content.

    NOTE: MariaDB 10.11 supports `ALTER TABLE ... ADD COLUMN IF NOT EXISTS` (since 10.0.2). Add a SQL comment `-- requires MariaDB >= 10.0.2 for ADD COLUMN IF NOT EXISTS; deploy target is 10.11` at the top.
  </action>
  <verify>
    <automated>grep -c 'ADD COLUMN IF NOT EXISTS' db/02-migrate-phase2.sql && grep -cE 'CREATE TABLE IF NOT EXISTS (experience|education|skills)' db/02-migrate-phase2.sql && grep -q 'uq_skill_user_name' db/02-migrate-phase2.sql && ! grep -qi 'drop ' db/02-migrate-phase2.sql && grep -q 'cover_url' db/01-schema-profile.sql</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'ADD COLUMN IF NOT EXISTS' db/02-migrate-phase2.sql` >= 4
    - `grep -E 'CREATE TABLE IF NOT EXISTS (experience|education|skills)' db/02-migrate-phase2.sql | wc -l` equals 3
    - `grep -q 'uq_skill_user_name' db/02-migrate-phase2.sql` (skills UNIQUE constraint present — backs PROF-05 dedupe)
    - `! ls db/02-migrate-phase2.sql.tmpl 2>/dev/null` (file is plain .sql, NOT .tmpl) AND `! grep -q '\${' db/02-migrate-phase2.sql` (no envsubst placeholders)
    - `grep -q 'cover_url' db/01-schema-profile.sql` AND `grep -cE 'CREATE TABLE IF NOT EXISTS (experience|education|skills)' db/01-schema-profile.sql` equals 3 (fresh-volume in sync)
    - `grep -qi 'WHERE NOT EXISTS\|INSERT IGNORE' db/02-migrate-phase2.sql` (seed is idempotent)
    - migration contains NO `DROP ` token: `! grep -qi 'drop ' db/02-migrate-phase2.sql` (non-destructive — threat T-02-05)
  </acceptance_criteria>
  <done>db/02-migrate-phase2.sql is idempotent, non-destructive, has 4 columns + 3 tables + small demo seed; db/01-schema-profile.sql and db/99-seed.sql mirror the same end state for fresh volumes.</done>
</task>

<task type="auto">
  <name>Task 2: Wire migration into deploy.sh (after Phase-1 step)</name>
  <read_first>
    - scripts/deploy.sh lines 107-131 (Phase-1 envsubst migrate at ~124, full-topology up at ~130)
    - 02-RESEARCH.md Pitfall 1 step 3 (exact deploy snippet — plain .sql, no envsubst)
  </read_first>
  <files>scripts/deploy.sh</files>
  <action>
    Insert a NEW step in `scripts/deploy.sh` AFTER the Phase-1 cutover apply (after line 124 `echo "[deploy] phase-1 DB cutover applied"`) and BEFORE the full-topology up (line ~126 `# 8) FULL-TOPOLOGY UP`). It applies the idempotent Phase-2 migration to the already-running mariadb container. NO envsubst (no placeholders). The migration is idempotent so re-runs are safe; do NOT add `|| true` (a migration failure should surface), but because mariadb is already confirmed healthy at this point this is safe:

    ```bash
    # 7b) PHASE-2 ADDITIVE MIGRATION (idempotent; RESEARCH Pitfall 1) -------------
    # db/*.sql run ONLY on a fresh volume; the live volume already exists, so the
    # new columns/tables + demo seed are applied here against the running DB.
    # Non-destructive (ADD/CREATE IF NOT EXISTS) and idempotent — safe to re-run.
    echo "[deploy] applying db/02-migrate-phase2.sql (additive)"
    docker compose exec -T mariadb mysql -uroot -p"$DB_ROOT_PASSWORD" proconnect_profile < db/02-migrate-phase2.sql
    echo "[deploy] phase-2 additive migration applied"
    ```

    Use `proconnect_profile` as the explicit database arg (the migration also has `USE proconnect_profile;` as defense-in-depth). `$DB_ROOT_PASSWORD` is already sourced from .env earlier in the script (line 25-27).
  </action>
  <verify>
    <automated>bash -n scripts/deploy.sh && grep -q '02-migrate-phase2.sql' scripts/deploy.sh && awk '/phase-1 DB cutover applied/{a=NR} /02-migrate-phase2.sql/{b=NR} /FULL-TOPOLOGY UP/{c=NR} END{exit !(a<b && b<c)}' scripts/deploy.sh</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n '02-migrate-phase2.sql' scripts/deploy.sh` shows the apply line
    - the new block appears AFTER `phase-1 DB cutover applied` and BEFORE `FULL-TOPOLOGY UP`: `awk '/phase-1 DB cutover applied/{a=NR} /02-migrate-phase2.sql/{b=NR} /FULL-TOPOLOGY UP/{c=NR} END{exit !(a<b && b<c)}' scripts/deploy.sh` exits 0
    - `bash -n scripts/deploy.sh` (syntax OK)
    - `grep -q 'envsubst' <(grep -A2 '02-migrate-phase2.sql' scripts/deploy.sh)` returns nothing (no envsubst on the phase-2 apply): `grep -A2 '02-migrate-phase2.sql' scripts/deploy.sh | grep -qv envsubst`
  </acceptance_criteria>
  <done>deploy.sh applies the phase-2 migration against the running mariadb, ordered after the phase-1 cutover and before full-topology up; syntax valid.</done>
</task>

<task type="auto">
  <name>Task 3: Author scripts/smoke-phase2.sh (the Phase 2 gate)</name>
  <read_first>
    - scripts/smoke-phase1.sh (FULL — clone its structure: `set -euo pipefail`, `GW` env, `pass`/`fail`/`FAILURES`, login pattern, verdict block)
    - 02-VALIDATION.md §Per-Task Verification Map (every assertion this script must encode)
    - 02-RESEARCH.md §"Phase Requirements → Test Map"
  </read_first>
  <files>scripts/smoke-phase2.sh</files>
  <action>
    Create `scripts/smoke-phase2.sh` cloning the smoke-phase1.sh skeleton (`#!/usr/bin/env bash`, `set -euo pipefail`, `GW="${GW:-http://127.0.0.1:8000}"`, `FAILURES=0`, `pass()/fail()`, verdict `ALL PASS`/`$FAILURES FAILURES` with exit code). Demo password `demo@123**`. It runs against a RUNNING stack (VPS or local). Encode these checks (Vietnamese echo labels OK):

    1. Login `duyet` (id 2) → capture token: `TOK=$(curl -s -XPOST "$GW/api/auth/login" -H 'Content-Type: application/json' -d '{"login":"duyet","password":"demo@123**"}' | grep -o '"token":"[^"]*"' | cut -d'"' -f4)`; fail if empty.
    2. PROF-06 public view: `curl -s "$GW/api/profiles/2/full"` with NO Authorization → HTTP 200 AND body contains `"experience"`, `"education"`, `"skills"`, `"connection_status"`. Assert `connection_status` is `null` when anonymous: `grep -q '"connection_status":null'`.
    3. Security email-leak: the no-token `/full` body MUST NOT contain `email` or `@`: `if echo "$body" | grep -qE '"email"|@'; then fail ...`.
    4. PROF-07 + degrade: the `/full` body has `"meta"` with `"degraded":true` (connection-service is a stub → 404 → degrade). Assert `grep -q '"degraded":true'` AND `grep -q '"parts"'` (or `connection` listed).
    5. PROF-04/D-04 auth-aware: `curl -s -H "Authorization: Bearer $TOK" "$GW/api/profiles/2/full"` → `connection_status` is `"none"` (NOT null) — `grep -q '"connection_status":"none"'`.
    6. PROF-02 edit basic (NON-DESTRUCTIVE + NULL-SAFE — smoke MUST fully restore demo data and must NOT crash under `set -euo pipefail`; Codex plan-review fix, final round 4). **To eliminate the nullable-field restore problem entirely, the smoke test mutates ONLY `headline`** — which the demo seed (Task 1) guarantees is non-null for user 2 (duyet). PROF-02 "edit basic works" is fully proven by editing headline; we deliberately do NOT mutate location/about/cover_url here so there is no null field to revert.
       a. **Init gate + var FIRST:** `RESTORE_READY=0; ORIG_HL=""`.
       b. **Define gated JSON-safe restore + register trap BEFORE capture:** `jesc(){ printf '%s' "$1" | sed 's/\\\\/\\\\\\\\/g; s/"/\\\\"/g'; }`; `restore(){ [ "$RESTORE_READY" = 1 ] && [ -n "$TOK" ] || return 0; curl -s -XPATCH "$GW/api/profiles/me" -H "Authorization: Bearer $TOK" -H 'Content-Type: application/json' -d "{\"headline\":\"$(jesc "$ORIG_HL")\"}" >/dev/null 2>&1 || true; }`; then `trap restore EXIT`. (Restore touches ONLY headline — the single field the smoke test changed; since seed guarantees it non-null, the restored value is always the real original, never empty/garbage.)
       c. **Capture null-safe, then mark ready:** `FULL2=$(curl -s "$GW/api/profiles/2/full" || true); if [ -n "$FULL2" ]; then ORIG_HL=$(printf '%s' "$FULL2" | grep -o '"headline":"[^"]*"' | head -1 | cut -d'"' -f4 || true); [ -n "$ORIG_HL" ] && RESTORE_READY=1; fi`. (RESTORE_READY=1 only when a real original headline was captured → restore never writes empty.)
       d. **Mutate ONLY headline:** `PATCH /api/profiles/me` with token + `{"headline":"SMOKE-HL"}` → 200; CAPTURE the response body and assert no email leak — `if echo "$patchBody" | grep -qE '"email"|@'; then fail "PATCH /me leaked email"; fi` (gateway allowlist must strip it). Then `/api/profiles/2/full` (anon) contains `SMOKE-HL`.
       The `trap restore EXIT` writes the original headline back at script end (even on early failure), so repeated runs leave the demo profile clean and unchanged. (Experience/education/skills checks already delete what they create — keep that.)
    7. PROF-03 experience round-trip: `POST /api/profiles/me/experience` `{"company":"SmokeCo","title":"Dev","start_date":"2020-01-01"}` → capture id from `data.id`; `/full` contains `SmokeCo`; `DELETE /api/profiles/me/experience/{id}` → 200/204; `/full` no longer contains `SmokeCo`.
    8. PROF-04 education round-trip: same shape `POST /api/profiles/me/education` `{"school":"SmokeUni","degree":"CNTT"}` → id → in `/full` → DELETE → gone.
    9. PROF-05 skills add/remove + dedupe: `POST /api/profiles/me/skills` `{"name":"SmokeSkill"}` → 200/201; `/full` contains `SmokeSkill`; POST same again → expect HTTP 409 (`curl -s -o /dev/null -w '%{http_code}'` == 409); `DELETE /api/profiles/me/skills/{id}` → gone from `/full`.
    10. Owner-scope / no-IDOR: confirm gateway exposes NO numeric-id write path — `curl -s -o /dev/null -w '%{http_code}' -XPATCH "$GW/api/profiles/3" -H "Authorization: Bearer $TOK" -d '{"headline":"HACK"}'` returns 404/405 (route does not exist); AND user 3's `/full` headline is unchanged (does not contain `HACK`).
    11. Mutations require auth: `PATCH /api/profiles/me` with NO token → 401.

    Each check uses `pass`/`fail`. End with the verdict block + exit. Make the file executable-friendly (it will be run with `bash scripts/smoke-phase2.sh`).
  </action>
  <verify>
    <automated>bash -n scripts/smoke-phase2.sh && grep -q 'set -euo pipefail' scripts/smoke-phase2.sh && grep -q '/full' scripts/smoke-phase2.sh && grep -q '"degraded":true' scripts/smoke-phase2.sh && grep -q 409 scripts/smoke-phase2.sh</automated>
  </verify>
  <acceptance_criteria>
    - `bash -n scripts/smoke-phase2.sh` (syntax OK)
    - `grep -q 'set -euo pipefail' scripts/smoke-phase2.sh` AND `grep -q 'FAILURES' scripts/smoke-phase2.sh`
    - all required behaviors present (grep all in one shot): `grep -q '/full' scripts/smoke-phase2.sh && grep -q '"degraded":true' scripts/smoke-phase2.sh && grep -q '"connection_status":null' scripts/smoke-phase2.sh && grep -q '"connection_status":"none"' scripts/smoke-phase2.sh && grep -q '/profiles/me/experience' scripts/smoke-phase2.sh && grep -q '/profiles/me/education' scripts/smoke-phase2.sh && grep -q '/profiles/me/skills' scripts/smoke-phase2.sh && grep -q '409' scripts/smoke-phase2.sh`
    - email-leak guards present (BOTH the /full anon body AND the PATCH /me response): `[ "$(grep -cE '"email"\|@' scripts/smoke-phase2.sh)" -ge 2 ]`
    - non-destructive restore present (Codex plan-review fix): `grep -q 'trap restore EXIT' scripts/smoke-phase2.sh && grep -q 'ORIG_HL' scripts/smoke-phase2.sh` (smoke restores user 2's basic fields so repeated runs don't dirty demo data)
  </acceptance_criteria>
  <done>scripts/smoke-phase2.sh exists, syntax-valid, encodes every PROF-02..07 + email-leak + degrade + auth-aware + owner-scope assertion from VALIDATION.md, mirroring the smoke-phase1.sh structure.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| deploy script → live MariaDB volume | DDL crosses into production data; must be non-destructive |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-05 | Tampering/DoS | db/02-migrate-phase2.sql against live volume | mitigate | Only `ADD COLUMN IF NOT EXISTS` / `CREATE TABLE IF NOT EXISTS` + idempotent guarded seed; NO `DROP`/`ALTER ... DROP`; acceptance grep asserts no `drop`. Re-runnable safely. |
| T-02-06 | DoS (deploy) | deploy.sh ordering | accept | Migration runs after mariadb is confirmed healthy (existing step 5); idempotent so a partial re-run converges. |
</threat_model>

<verification>
- `bash -n scripts/deploy.sh && bash -n scripts/smoke-phase2.sh`
- migration grep assertions pass (Task 1 acceptance)
- deploy ordering assertion passes (Task 2 acceptance)
- Runtime application of the migration + smoke run happens on the VPS in Plan 05 (live deploy).
</verification>

<success_criteria>
- db/02-migrate-phase2.sql idempotent + non-destructive, with demo seed; fresh-volume files in sync.
- deploy.sh applies it after the phase-1 step, before full-topology up.
- scripts/smoke-phase2.sh encodes all Phase 2 runtime assertions and is the gate for later waves.
</success_criteria>

<output>
After completion, create `.planning/phases/02-h-s-ngh-nghi-p/02-01-SUMMARY.md`
</output>
