---
phase: 02-h-s-ngh-nghi-p
plan: 05
type: execute
wave: 4
depends_on: [01, 02, 03, 04]
files_modified: []
autonomous: false
requirements: [PROF-02, PROF-03, PROF-04, PROF-05, PROF-06, PROF-07, UI-03]
must_haves:
  truths:
    - "The Phase-2 additive migration is applied to the LIVE proconnect_profile volume (4 columns + 3 tables + demo seed) without data loss"
    - "scripts/smoke-phase2.sh passes green on the deployed soa.duyet.vn stack"
    - "scripts/smoke-phase1.sh still passes (no Phase-1 regression)"
    - "/api/profiles/{id}/full composes + degrades correctly on the live stack; no email leak"
    - "Container count stays <= 9 (no new container added in Phase 2)"
  artifacts: []
  key_links:
    - from: "git push main"
      to: "GitHub Actions → VPS git pull --ff-only → scripts/deploy.sh"
      via: "CI/CD"
      pattern: "deploy"
    - from: "deploy.sh phase-2 migration step"
      to: "live proconnect_profile volume"
      via: "docker compose exec mariadb mysql < db/02-migrate-phase2.sql"
      pattern: "02-migrate-phase2.sql"
---

<objective>
Live cutover + phase verification (PROF-02..07, UI-03; ROADMAP success criteria 1-4). Push Phase 2 to `main`, let CI/CD deploy to soa.duyet.vn, confirm the idempotent additive migration applied to the LIVE volume, and run the full smoke suite (Phase 1 + Phase 2) green on the deployed stack.

Purpose: Phase 2's runtime risk is the live-volume migration (Pitfall 1 — repo schema edits do NOT reach the existing volume; deploy.sh applies the migration). The migration is non-destructive (ADD/CREATE IF NOT EXISTS) so this is lower-risk than the Phase-1 destructive cutover, but it is still a live-production step and must be verified.
Output: deployed Phase 2 on soa.duyet.vn, green smoke suite, no Phase-1 regression. No new files.

NOTE: This plan contains the mandatory CLAUDE.md review gates and the live-deploy checkpoint. autonomous: false.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-h-s-ngh-nghi-p/02-RESEARCH.md
@.planning/phases/02-h-s-ngh-nghi-p/02-VALIDATION.md
@.planning/phases/01-n-n-t-ng-gateway/01-VERIFICATION.md
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Mandatory pre-commit code review (CLAUDE.md gate)</name>
  <decision>Has Codex APPROVED the Phase 2 code changes via /codex-impl-review?</decision>
  <context>
    CLAUDE.md (project + ~/.claude) mandates: before ANY commit, run /codex-impl-review; commit ONLY after Codex approves; fix valid issues and re-review. This gate covers the diffs from Plans 01-04 (migration, profile-service CRUD, gateway composition, UI). Reviewer focus: IDOR scoping (X-User-Id, no body user_id), email-leak allowlist on /full, settle-vs-unwrap degrade correctness, migration idempotency/non-destructiveness, no x-html on user text.
  </context>
  <options>
    <option id="approved"><name>Approved</name><pros>Cleared to commit + push</pros><cons>none</cons></option>
    <option id="changes"><name>Changes requested</name><pros>Fix valid issues, re-run /codex-impl-review</pros><cons>Iterate before commit</cons></option>
  </options>
  <resume-signal>Type "approved" once Codex approves (or describe the fixes applied and re-review)</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Static lint gate + commit + push to main (triggers CI/CD)</name>
  <read_first>
    - .github/workflows/deploy.yml (CI: php -l lint job → SSH deploy job runs scripts/deploy.sh, verifies /api/health)
    - scripts/deploy.sh (the phase-2 migration step wired in Plan 01)
  </read_first>
  <files>(no source files — git operations only)</files>
  <action>
    Local static gate mirroring CI lint: run `find gateway services -name '*.php' -newer /dev/null -print0 | xargs -0 -n1 php -l` (or simply `php -l` over the files changed in Plans 02-03) — all must report "No syntax errors detected". Also `bash -n scripts/deploy.sh scripts/smoke-phase2.sh`.

    Then stage Phase 2 changes and commit (the working dir is NOT a git repo per env note — confirm `git status` works; if the project uses git elsewhere, operate in the repo root). Follow the project deploy flow: local commit → push public GitHub `main` → GitHub Actions runs lint then SSHes to the VPS, does `git pull --ff-only`, runs `scripts/deploy.sh` (which applies db/02-migrate-phase2.sql to the live volume), and verifies `https://soa.duyet.vn/api/health`.

    Commit message: `feat(02): hồ sơ nghề nghiệp — composition /full + experience/education/skills CRUD + live migration` ending with the required Co-Authored-By trailer.

    Do NOT push until Task 1 (Codex approval) is cleared.
  </action>
  <verify>
    <automated>bash -n scripts/deploy.sh && bash -n scripts/smoke-phase2.sh</automated>
  </verify>
  <acceptance_criteria>
    - all changed `*.php` pass `php -l`
    - `bash -n scripts/deploy.sh && bash -n scripts/smoke-phase2.sh` exit 0
    - commit created on `main` (or feature branch per repo convention) and pushed; CI lint job green
  </acceptance_criteria>
  <done>Phase 2 committed and pushed; CI lint passes; deploy job triggered.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Live deploy verification — migration applied + full smoke green</name>
  <what-built>
    CI/CD deployed Phase 2 to soa.duyet.vn: gateway + profile-service images rebuilt with the new composition/CRUD code (automatic via docker compose build), web/* pages shipped via bind mount, and deploy.sh applied the idempotent additive migration (db/02-migrate-phase2.sql) to the LIVE proconnect_profile volume — adding 4 columns + 3 tables + demo seed WITHOUT touching existing rows.
  </what-built>
  <how-to-verify>
    On the VPS (SSH alias vps-root), in the project dir:
    1. Confirm the migration landed (non-destructive): `docker compose exec -T mariadb mysql -uroot -p"$DB_ROOT_PASSWORD" -e "DESCRIBE proconnect_profile.users;"` shows cover_url/headline/location/about; `SHOW TABLES IN proconnect_profile;` lists experience, education, skills; the 5 demo users still exist (`SELECT id,username FROM proconnect_profile.users;`).
    2. Run the full smoke suite on the deployed stack: `bash scripts/smoke-phase1.sh && bash scripts/smoke-phase2.sh` → BOTH "ALL PASS". This asserts: /full composes experience/education/skills + connection_status; anonymous /full → 200 + connection_status null + NO email/@; token /full → connection_status "none"; meta.degraded:true (connection stub — EXPECTED); PATCH /me basic reflected in /full; experience/education round-trips; skills add → 409 on dup → delete; no numeric-id write route (IDOR closed).
    3. Container budget: `docker compose ps --services | wc -l` ≤ 9 (no new container — Phase 2 only extended existing services).
    4. Browser spot-check (folds in Plan 04's UI checkpoint): open https://soa.duyet.vn/profile.html?id=2 and the edit flow as logged-in duyet.
    If anything fails, the migration is idempotent + non-destructive — safe to re-run deploy.sh; check `docker compose logs gateway profile-service` for 500s indicating a missing column (would mean the migration step did not run).
  </how-to-verify>
  <resume-signal>Type "approved" once both smoke suites are green on soa.duyet.vn and the migration is confirmed, or describe failures</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI/CD → live VPS + production DB volume | Deploy applies DDL to production data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-05 | Tampering/DoS | live-volume migration | mitigate | Migration is non-destructive (ADD/CREATE IF NOT EXISTS), idempotent, re-runnable; verified by DESCRIBE/SHOW TABLES + existing-rows check at the checkpoint. Lower risk than the Phase-1 destructive cutover (no mysqldump pre-wipe needed). |
| T-02-11 | Availability | live deploy regression | mitigate | Phase-1 smoke must still pass (no regression); gateway /api/health gate in deploy.sh; rollback = revert commit + redeploy (migration columns/tables are additive, harmless if unused). |
</threat_model>

<verification>
- Migration applied to live volume (DESCRIBE/SHOW TABLES), demo rows intact.
- `scripts/smoke-phase1.sh && scripts/smoke-phase2.sh` green on soa.duyet.vn.
- Container count ≤ 9.
- All 4 ROADMAP Phase-2 success criteria observable: (1) edit basic, (2) exp/edu/skills CRUD reflected, (3) public view of others with sections, (4) gateway composition + degrade.
</verification>

<success_criteria>
- Phase 2 live on soa.duyet.vn; full smoke suite green; no Phase-1 regression; no data loss; ≤ 9 containers.
</success_criteria>

<output>
After completion, create `.planning/phases/02-h-s-ngh-nghi-p/02-05-SUMMARY.md`
</output>
