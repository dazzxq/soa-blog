---
phase: 04-news-feed
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - db/04-migrate-phase4.sql
  - db/01-schema-feed.sql
  - scripts/deploy.sh
  - scripts/smoke-phase4.sh
autonomous: true
requirements: [FEED-01, FEED-02, FEED-03, FEED-04, FEED-05, FEED-06]
tags: [database, migration, smoke, feed]

must_haves:
  truths:
    - "posts/reactions/comments tables exist in proconnect_feed on the live VPS volume after deploy"
    - "Re-running the phase-4 migration is a no-op (idempotent CREATE IF NOT EXISTS + guarded seed)"
    - "Demo feed fixtures exist with an ASYMMETRIC fan-trap canary: post 1 by duyet(2) has 2 reactions + 1 comment, a post by long(3), 1 repost by demo(1) of post 1"
    - "smoke-phase4.sh runs against a running stack and asserts every FEED req + feed composition + EXACT asymmetric count correctness (post1 = reaction_count 2 AND comment_count 1) + my_reaction + email-leak + 404-invariant + owner-scope + newest-first"
  artifacts:
    - path: "db/04-migrate-phase4.sql"
      provides: "Idempotent live-volume posts/reactions/comments tables + asymmetric-count demo seed"
      contains: "CREATE TABLE IF NOT EXISTS posts"
    - path: "db/01-schema-feed.sql"
      provides: "Fresh-volume schema convergence for feed-service"
      contains: "CREATE TABLE IF NOT EXISTS posts"
    - path: "scripts/deploy.sh"
      provides: "Blocking phase-4 migration step after the phase-3 step"
      contains: "04-migrate-phase4.sql"
    - path: "scripts/smoke-phase4.sh"
      provides: "Full FEED-01..06 + composition + exact-count + PII + 404 + ownership smoke harness"
      contains: "smoke4"
  key_links:
    - from: "scripts/deploy.sh"
      to: "db/04-migrate-phase4.sql"
      via: "docker compose exec mariadb mysql proconnect_feed < db/04-migrate-phase4.sql"
      pattern: "04-migrate-phase4\\.sql"
---

<objective>
Wave 0 infrastructure for Phase 4: ship the idempotent live-volume migration that
creates `posts`/`reactions`/`comments` in `proconnect_feed` + a demo feed seed, wire it
BLOCKING into deploy.sh after the phase-3 step, add the fresh-volume schema file for
from-scratch convergence, and create the `scripts/smoke-phase4.sh` harness that every
later wave's acceptance criteria reference.

Purpose: Without the live migration, every feed endpoint 500s on `SELECT ... FROM posts`
(RESEARCH Pitfall 1 — `db/*.sql` only run on a fresh volume; the live VPS volume already
exists, and `proconnect_feed` is EMPTY). Without the smoke harness, no later task is
verifiable. This is a clone of the proven Phase 2/3 migration + smoke pattern. The demo
seed deliberately gives post 1 ASYMMETRIC counts (2 reactions + 1 comment) so the smoke
fan-trap guard can actually fail on a double-JOIN count query (a symmetric 1/1 post would
pass even when broken, since 1×1 = 1).

Output: db/04-migrate-phase4.sql, db/01-schema-feed.sql, deploy.sh step 7d, scripts/smoke-phase4.sh.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-news-feed/04-CONTEXT.md
@.planning/phases/04-news-feed/04-RESEARCH.md
@.planning/phases/04-news-feed/04-VALIDATION.md

<interfaces>
<!-- Templates to clone. Do NOT invent new shapes — these are proven and shipped. -->

Demo user ids (db/migrate-phase1.sql.tmpl seed lines 93-98, CONFIRMED):
1=demo ("Tài khoản demo"), 2=duyet ("Nguyễn Thế Duyệt"), 3=long ("Đinh Ngọc Long"),
4=diep ("Vũ Duy Điệp"), 5=tai ("Tạ Ngọc Tài"). Shared demo password: `demo@123**`.
Seed demo graph (db/03-migrate-phase3.sql): duyet(2)<->long(3) ACCEPTED; demo(1)->duyet(2) pending.
=> A post by long(3) appearing in duyet(2)'s feed proves FEED-02 (timeline = self + connections).

deploy.sh step 7c (the slot AFTER which the phase-4 step goes) — scripts/deploy.sh lines 142-151:
```bash
# 7c) PHASE-3 ADDITIVE MIGRATION (idempotent; RESEARCH Pitfall 1) -------------
echo "[deploy] applying db/03-migrate-phase3.sql (additive)"
docker compose exec -T mariadb mysql -uroot -p"$DB_ROOT_PASSWORD" proconnect_connection < db/03-migrate-phase3.sql
echo "[deploy] phase-3 additive migration applied"

# 8) FULL-TOPOLOGY UP (ISSUE-3 step 4) -----------------------------------------
echo "[deploy] docker compose up -d --remove-orphans (full topology)"
docker compose up -d --remove-orphans
```
The phase-4 step MUST go between the `phase-3 additive migration applied` echo and the
`# 8) FULL-TOPOLOGY UP` comment, so feed-service boots against existing tables.

LOCKED DDL (D-01..D-04 + RESEARCH §"Idempotent live migration", verbatim):
```sql
CREATE TABLE IF NOT EXISTS posts (
  id BIGINT UNSIGNED PRIMARY KEY AUTO_INCREMENT,
  author_id BIGINT UNSIGNED NOT NULL,
  content TEXT NOT NULL,
  image_url VARCHAR(512) NULL,
  repost_of BIGINT UNSIGNED NULL,
  created_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
  INDEX idx_posts_author_created (author_id, created_at),
  INDEX idx_posts_created (created_at),
  INDEX idx_posts_repost (repost_of)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;

CREATE TABLE IF NOT EXISTS reactions (
  id BIGINT UNSIGNED PRIMARY KEY AUTO_INCREMENT,
  post_id BIGINT UNSIGNED NOT NULL,
  user_id BIGINT UNSIGNED NOT NULL,
  type ENUM('like','love','haha','wow','sad','angry') NOT NULL DEFAULT 'like',
  created_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
  UNIQUE KEY uq_reaction_post_user (post_id, user_id),
  INDEX idx_reaction_post (post_id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;

CREATE TABLE IF NOT EXISTS comments (
  id BIGINT UNSIGNED PRIMARY KEY AUTO_INCREMENT,
  post_id BIGINT UNSIGNED NOT NULL,
  author_id BIGINT UNSIGNED NOT NULL,
  body TEXT NOT NULL,
  created_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
  INDEX idx_comment_post_created (post_id, created_at)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;
```
NOTE: `content TEXT NOT NULL` — a repost stores `content=''` (D-04, A6); the original is
shown via `repost_of`. `repost_of` is a logical FK only (no physical constraint, matching
the database-per-service doctrine).

FAN-TRAP CANARY (why post 1 carries 2 reactions + 1 comment, NOT 1+1): the timeline count
query (Plan 02) MUST use correlated subqueries, not a double JOIN. A broken double-JOIN
returns the cross-product — on a symmetric 1-reaction/1-comment post it computes 1×1 = 1 for
both counts and the bug is INVISIBLE. With post 1 at 2 reactions × 1 comment, a double-JOIN
yields comment_count = 2 (and/or reaction_count multiplied), so the smoke EXACT-count assert
(`reaction_count`==2 AND `comment_count`==1) fails loudly. The asymmetry is what makes the
guard real.

smoke-phase3.sh scaffold to clone (scripts/smoke-phase3.sh, 350 lines): `set -euo pipefail`,
`GW="${GW:-http://127.0.0.1:8000}"`, `PW='demo@123**'`, `FAILURES=0`, `pass()`/`fail()` with a
`[smoke4]` prefix, login via `grep -o '"token":"[^"]*"' | cut -d'"' -f4`, parallel CLEANUP
arrays + `trap restore EXIT` registered BEFORE creating anything, final `ALL PASS`/`N FAILURES`
verdict. app.js/api envelope: success object `{ "data": ... }`, list `{ "data": [...], "meta": {...} }`,
error `{ "error": { "code", "message" } }`; 204 → empty body.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: db/04-migrate-phase4.sql (idempotent live migration + asymmetric-count demo seed)</name>
  <read_first>db/03-migrate-phase3.sql (the EXACT idempotent-migration template + header rationale + guarded-seed `INSERT ... SELECT ... WHERE NOT EXISTS` pattern); 04-RESEARCH.md §"Idempotent live migration (db/04-migrate-phase4.sql)" (lines 560-621, the verbatim DDL + seed) + §Pitfall 1 (lines 512-526) + §Pitfall 3 (the fan-trap rationale).</read_first>
  <files>db/04-migrate-phase4.sql</files>
  <action>
Create db/04-migrate-phase4.sql cloning the header + structure of db/03-migrate-phase3.sql.
Top line MUST be `USE proconnect_feed;` (the deploy step also passes the DB name).
NON-DESTRUCTIVE: only CREATE TABLE IF NOT EXISTS + guarded INSERT — NO DROP/ALTER/envsubst.

Add the three CREATE TABLE IF NOT EXISTS statements EXACTLY as in the LOCKED DDL block in
<interfaces> above (posts, reactions, comments — D-01/D-02/D-03 with all indexes and the
reactions `UNIQUE KEY uq_reaction_post_user (post_id, user_id)`).

Idempotent demo seed (D-08) — explicit ids + `WHERE NOT EXISTS` so re-runs are no-ops.
Use the `INSERT INTO posts (id, author_id, content, image_url, repost_of) SELECT ... WHERE NOT EXISTS (SELECT 1 FROM posts WHERE id = N)` form (mirror 03-migrate-phase3.sql guarded seed):
- post id 1: author 2 (duyet), content 'Chào mừng đến với ProConnect! Đây là bài viết demo đầu tiên.', image_url NULL, repost_of NULL.
- post id 2: author 3 (long),  content 'Vừa hoàn thành một dự án microservices thú vị.', image_url NULL, repost_of NULL.
- post id 3: author 1 (demo),  content '' , image_url NULL, repost_of 1  (demo reposts duyet's post 1 — proves FEED-05).
- reaction id 1: post 1, user 3 (long), type 'love'.
- reaction id 2: post 2, user 2 (duyet), type 'like'.
- reaction id 3: post 1, user 4 (diep), type 'like'.  ← SECOND reaction on post 1 → post 1 = 2 reactions, the fan-trap canary (asymmetric vs its 1 comment). Uses a distinct user so the (post,user) UNIQUE key holds.
- comment id 1: post 1, author 3 (long), body 'Chúc mừng bạn!'.  ← post 1 = exactly 1 comment.
Net: post 1 carries an ASYMMETRIC 2 reactions + 1 comment (Pitfall 3 canary); each guarded with its own `WHERE NOT EXISTS (SELECT 1 FROM reactions/comments WHERE id = N)`.
Add a header comment block explaining Pitfall 1 (db/*.sql only run on a fresh volume; the live
proconnect_feed schema exists but is EMPTY), the plain-.sql/no-envsubst rationale (feed_svc
already has GRANT ALL on proconnect_feed from Phase 1), that re-runs are no-ops, AND that post 1's
2-reactions/1-comment asymmetry is the deliberate fan-trap canary for the smoke EXACT-count assert
(symmetric 1/1 would mask a double-JOIN bug) — exactly as 03-migrate-phase3.sql documents its seed.
  </action>
  <verify>
    <automated>cd /Users/theduyet/Documents/Code/SOA/soa-blog && grep -q "USE proconnect_feed" db/04-migrate-phase4.sql && grep -q "CREATE TABLE IF NOT EXISTS posts" db/04-migrate-phase4.sql && grep -q "CREATE TABLE IF NOT EXISTS reactions" db/04-migrate-phase4.sql && grep -q "CREATE TABLE IF NOT EXISTS comments" db/04-migrate-phase4.sql && grep -q "uq_reaction_post_user (post_id, user_id)" db/04-migrate-phase4.sql && grep -q "ENUM('like','love','haha','wow','sad','angry')" db/04-migrate-phase4.sql && grep -q "repost_of BIGINT UNSIGNED NULL" db/04-migrate-phase4.sql && grep -qE "SELECT 3, 1, '', NULL, 1" db/04-migrate-phase4.sql && grep -qE "SELECT 1, 1, 3," db/04-migrate-phase4.sql && grep -qE "SELECT 3, 1, 4," db/04-migrate-phase4.sql && test "$(grep -cE "WHERE NOT EXISTS \(SELECT 1 FROM reactions WHERE id" db/04-migrate-phase4.sql)" = "3" && test "$(grep -cE "WHERE NOT EXISTS \(SELECT 1 FROM comments WHERE id" db/04-migrate-phase4.sql)" = "1" && ! grep -qiE "DROP TABLE|ALTER TABLE|envsubst|\\$\\{" db/04-migrate-phase4.sql && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep "USE proconnect_feed" db/04-migrate-phase4.sql` matches (correct DB).
    - All three `CREATE TABLE IF NOT EXISTS posts|reactions|comments` match.
    - `grep "uq_reaction_post_user (post_id, user_id)"` matches (upsert key + dedupe backstop present).
    - `grep "ENUM('like','love','haha','wow','sad','angry')"` matches (D-02 reaction enum).
    - `grep "repost_of BIGINT UNSIGNED NULL"` matches (D-04 repost column).
    - The repost seed row `SELECT 3, 1, '', NULL, 1` matches (demo reposts post 1).
    - THREE guarded reaction seed rows total (ids 1/2/3 — verifier asserts the `WHERE NOT EXISTS (SELECT 1 FROM reactions WHERE id` guard count is EXACTLY 3), of which TWO are on post 1: reaction id 1 (`SELECT 1, 1, 3,` = id 1, post 1, user 3) AND reaction id 3 (`SELECT 3, 1, 4,` = id 3, post 1, user 4); reaction id 2 is on post 2. So post 1 has EXACTLY 2 reactions + its 1 comment (the asymmetric fan-trap canary).
    - EXACTLY ONE guarded comment seed row (comment id 1 on post 1 — verifier asserts the `WHERE NOT EXISTS (SELECT 1 FROM comments WHERE id` guard count is EXACTLY 1).
    - `grep -iE "DROP TABLE|ALTER TABLE|envsubst|\${"` returns NOTHING (non-destructive, plain .sql).
  </acceptance_criteria>
  <done>db/04-migrate-phase4.sql exists, idempotent, non-destructive, all three tables + demo seed (incl. 1 repost, post 1 carrying an asymmetric 2 reactions + 1 comment fan-trap canary), no envsubst/DROP/ALTER.</done>
</task>

<task type="auto">
  <name>Task 2: db/01-schema-feed.sql (fresh-volume convergence)</name>
  <read_first>db/00-init.sh (creates proconnect_feed DB + feed_svc grant on a fresh volume); db/01-schema-connection.sql (the fresh-volume schema-file pattern that USEs its own DB, structure-only, no seed); 04-RESEARCH.md Pitfall 1 step 2 (line 517 — fresh-volume schema file mirrors the migration DDL verbatim).</read_first>
  <files>db/01-schema-feed.sql</files>
  <action>
Create db/01-schema-feed.sql so a from-scratch `docker compose up` (fresh volume) converges to
the same schema. Top line `USE proconnect_feed;`. Contains the SAME three
`CREATE TABLE IF NOT EXISTS posts|reactions|comments (...)` DDL as Task 1 (copy them VERBATIM —
including the reactions UNIQUE key and all indexes, so fresh and live volumes converge identically).
Do NOT include the demo seed here (seed belongs in the migration; keep the fresh-volume schema
file structure-only, mirroring how db/01-schema-connection.sql is structure-only). Add a one-line
header: this is the fresh-volume schema; the live VPS volume gets the same tables via
db/04-migrate-phase4.sql (Pitfall 1). Confirm `ls db/ | sort` shows 00-init.sh before 01-schema-feed.sql.
  </action>
  <verify>
    <automated>cd /Users/theduyet/Documents/Code/SOA/soa-blog && ls db/ | sort | grep -q "01-schema-feed.sql" && grep -q "USE proconnect_feed" db/01-schema-feed.sql && grep -q "CREATE TABLE IF NOT EXISTS posts" db/01-schema-feed.sql && grep -q "CREATE TABLE IF NOT EXISTS reactions" db/01-schema-feed.sql && grep -q "CREATE TABLE IF NOT EXISTS comments" db/01-schema-feed.sql && grep -q "uq_reaction_post_user (post_id, user_id)" db/01-schema-feed.sql && test "$(grep -c INSERT db/01-schema-feed.sql)" = "0" && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `db/01-schema-feed.sql` exists with `USE proconnect_feed;` + the three CREATE TABLE statements.
    - The DDL is identical to Task 1 (the reactions UNIQUE key + indexes grep-match).
    - `ls db/ | sort` shows `00-init.sh` before `01-schema-feed.sql` (fresh-volume order safe).
    - File contains NO demo seed INSERTs (`grep -c "INSERT" db/01-schema-feed.sql` is 0).
  </acceptance_criteria>
  <done>Fresh-volume schema file exists, ordered correctly, structure-only, identical DDL to the live migration.</done>
</task>

<task type="auto">
  <name>Task 3: wire migration into deploy.sh (blocking) + create smoke-phase4.sh harness</name>
  <read_first>scripts/deploy.sh lines 142-151 (phase-3 migration step 7c + the `# 8) FULL-TOPOLOGY UP` slot — phase-4 step slots BETWEEN them); scripts/smoke-phase3.sh (the FULL smoke scaffold to clone: set -euo pipefail, GW/PW/FAILURES, pass/fail, parallel CLEANUP arrays + trap restore EXIT, login token extraction, final verdict); 04-VALIDATION.md §"Per-Requirement Verification Map"; 04-RESEARCH.md §"Phase Requirements → Test Map" (lines 722-733, the exact assertions) + §Pitfall 3 (fan-trap).</read_first>
  <files>scripts/deploy.sh, scripts/smoke-phase4.sh</files>
  <action>
PART A — deploy.sh: Insert a phase-4 step IMMEDIATELY after the phase-3 step (after the line
`echo "[deploy] phase-3 additive migration applied"`), BEFORE the `# 8) FULL-TOPOLOGY UP` comment.
Plain .sql, no envsubst, no `|| true` (a failure must surface and block deploy). Mirror 7c exactly:
```bash
# 7d) PHASE-4 ADDITIVE MIGRATION (idempotent; RESEARCH Pitfall 1) -------------
# Adds posts/reactions/comments + demo feed seed to the live proconnect_feed
# volume. Non-destructive (CREATE IF NOT EXISTS + guarded seed), safe to re-run.
echo "[deploy] applying db/04-migrate-phase4.sql (additive)"
docker compose exec -T mariadb mysql -uroot -p"$DB_ROOT_PASSWORD" proconnect_feed < db/04-migrate-phase4.sql
echo "[deploy] phase-4 additive migration applied"
```

PART B — scripts/smoke-phase4.sh: Clone smoke-phase3.sh scaffold (`set -euo pipefail`,
`GW="${GW:-http://127.0.0.1:8000}"`, `PW='demo@123**'`, `FAILURES=0`, `pass()`/`fail()` with a
`[smoke4]` prefix, login helper, final `ALL PASS`/`N FAILURES` verdict). Make it executable (`chmod +x`).
NON-DESTRUCTIVE: any post/reaction/comment the script CREATES it must DELETE in cleanup; register a
`trap restore EXIT` BEFORE creating anything; it MUST NOT delete the demo-seed fixtures
(posts 1/2/3, the seed reactions/comment) — or must leave them intact. Login-by-username (not hardcoded id).

Implement these assertions (gateway routes go live in Plan 03; assertions run on the deployed
stack in Plan 05). Use `grep -qE` for body asserts, `curl -s -o /dev/null -w '%{http_code}'` for codes,
and capture ids via `grep -o '"id":[0-9]*' | head -1 | cut -d: -f2`:
1. login duyet(2), long(3), diep(4) → tokens.
2. FEED-01 create: as duyet `POST /api/posts {content:"smoke bài viết X-<rand>"}` → 200/201 + id; capture POST_ID; clean up (DELETE) at end.
3. FEED-01 with image: `POST /api/posts {content, image_url:"https://example.com/a.png"}` → 201; body has image_url; clean up.
4. FEED-02 timeline self+connections newest-first: as duyet `GET /api/feed?limit=20` → 200; body contains the just-created POST_ID AND a post by long(3) (seed post 2, content "Vừa hoàn thành" — duyet↔long connected); assert the timeline is newest-first (the just-created post appears before the older seed post 2 in body order).
5. FEED-03 reaction set/change/remove + count: `POST /api/posts/{POST_ID}/reactions {type:"like"}` → ok; re-GET feed → that item has `"reaction_count":1` and `"my_reaction":"like"`; change `POST .../reactions {type:"love"}` → still `reaction_count":1` and `my_reaction":"love"` (upsert, not a 2nd row); `DELETE .../reactions` → `reaction_count":0` and `my_reaction` null.
6. FEED-04 comment add/list/delete + count: `POST /api/posts/{POST_ID}/comments {body:"smoke bình luận"}` → 201 + capture CID; `GET /api/posts/{POST_ID}/comments` lists it AND the comment author card has display_name but NO `@`/email; feed item shows `comment_count":1`; `DELETE /api/comments/{CID}` (owner duyet) → gone; comment_count back to 0.
7. FEED-05 repost shows origin: `POST /api/posts/{POST_ID}/repost` as long → 201 + capture REPOST_ID; `GET /api/feed` as long → the repost item has `repost_of` == POST_ID and `original` with `author` + `content`; clean up (DELETE REPOST_ID).
8. FEED-06 composition shape: every `/api/feed` item has keys `author` (with `display_name`), `reaction_count`, `comment_count`, `my_reaction`, `repost_of`; the WHOLE feed body has NO `@`/`"email"` (email-leak guard).
9. FEED-06 EXACT-count correctness (ASYMMETRIC fan-trap guard, Pitfall 3): isolate the seed post-1 item from a feed fetched as duyet (e.g. with `python3 -c`/`jq`-free awk or a tolerant `grep -oE` on the `"id":1,` object slice) and assert it reports `reaction_count":2` AND `comment_count":1` EXACTLY (seed: long+diep reactions, long's 1 comment). This is the canary: a double-JOIN count query would multiply to `comment_count":2` (2 reactions × 1 comment) or otherwise inflate, and this EXACT 2-AND-1 assert (NOT >=1) fails on it. A symmetric 1/1 post could NOT catch the bug — that is why the seed is asymmetric. Fail with a clear `[smoke4] FAIL fan-trap: post1 expected react=2 comment=1` message.
10. Invariant 404: `POST /api/posts/999999/reactions {type:"like"}`, `POST /api/posts/999999/comments {body:"x"}`, `POST /api/posts/999999/repost` each → 404.
11. Ownership: diep(4) tries `DELETE /api/posts/{POST_ID}` (duyet's) → 403/404; diep tries `DELETE /api/comments/{a duyet comment id}` → 403/404 (only owner deletes).
12. Security: combined assert NO `@`/`"email"` across `/api/feed`, `/api/posts/{POST_ID}` and `/api/posts/{POST_ID}/comments` bodies.
13. DEGRADE (documented, asserted indirectly): add an inline comment that when connection-service is down the gateway degrades to own-posts + `meta.degraded:["connections"]` (feed-service is the only hard dep) — a fault-injection path not exercised locally; the own-posts path is covered by the create→feed assertion (step 4).
  </action>
  <verify>
    <automated>cd /Users/theduyet/Documents/Code/SOA/soa-blog && grep -q "04-migrate-phase4.sql" scripts/deploy.sh && grep -q "proconnect_feed < db/04-migrate-phase4.sql" scripts/deploy.sh && ! grep -q "04-migrate-phase4.sql.*|| true" scripts/deploy.sh && bash -n scripts/smoke-phase4.sh && test -x scripts/smoke-phase4.sh && grep -q "reaction_count" scripts/smoke-phase4.sh && grep -q "my_reaction" scripts/smoke-phase4.sh && grep -q "comment_count" scripts/smoke-phase4.sh && grep -q "repost_of" scripts/smoke-phase4.sh && grep -qE 'reaction_count":2' scripts/smoke-phase4.sh && grep -qE 'comment_count":1' scripts/smoke-phase4.sh && grep -qi "fan-trap" scripts/smoke-phase4.sh && grep -qE 'email|@' scripts/smoke-phase4.sh && grep -q "999999" scripts/smoke-phase4.sh && grep -q "trap" scripts/smoke-phase4.sh && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep "04-migrate-phase4.sql" scripts/deploy.sh` matches; the line uses `proconnect_feed` and has NO `|| true`.
    - The phase-4 block appears AFTER `phase-3 additive migration applied` and BEFORE `# 8) FULL-TOPOLOGY UP` / `docker compose up -d --remove-orphans`.
    - `bash -n scripts/smoke-phase4.sh` exits 0; `test -x scripts/smoke-phase4.sh` (executable).
    - smoke-phase4.sh asserts `reaction_count`, `comment_count`, `my_reaction`, `repost_of`, the 999999 404-invariant, and an `@`/email PII-leak guard.
    - smoke-phase4.sh contains an EXACT asymmetric fan-trap assert on seed post 1: `reaction_count":2` AND `comment_count":1` (grep both literals + a `fan-trap` label match) — NOT a `>=1` check, so a double-JOIN inflation is caught.
    - smoke-phase4.sh registers a `trap` cleanup and does not delete the demo-seed fixtures.
  </acceptance_criteria>
  <done>deploy.sh applies the phase-4 migration blocking after the phase-3 step; smoke-phase4.sh is a syntactically-valid, executable, non-destructive harness covering FEED-01..06 + EXACT asymmetric count-correctness (post1 react=2/comment=1 fan-trap canary) + my_reaction + PII + 404-invariant + ownership + newest-first.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| deploy host → mariadb container | Migration SQL piped into the running DB as root; must be non-destructive |
| fresh-volume initdb glob → schema | db/*.sql run alphabetically only on a fresh volume; live volume already initialized |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering/Denial | db/04-migrate-phase4.sql | mitigate | Only CREATE IF NOT EXISTS + guarded INSERT; NO DROP/ALTER; idempotent so re-runs are no-ops (grep guard in verify). |
| T-04-02 | Denial | deploy.sh phase-4 step | mitigate | No `|| true` — a migration failure surfaces and blocks deploy before full-topology up, so feed-service never boots against missing tables (Pitfall 1). |
| T-04-03 | Information disclosure | smoke-phase4.sh | mitigate | The harness asserts no `@`/email leaks in feed/post/comment bodies — turning the email allowlist into a CI gate (Pitfall 2). |
| T-04-04 | Tampering/Integrity | reaction_count/comment_count correctness | mitigate | Smoke asserts the ASYMMETRIC seed post 1 (2 reactions + 1 comment) reports EXACTLY reaction_count=2 AND comment_count=1 — a double-JOIN fan-trap would inflate comment_count to 2 (=2×1), so the exact assert fails on it. A symmetric 1/1 post could not catch this (Pitfall 3). |
| T-04-05 | Tampering | fresh-volume ordering | accept | 01-schema-feed.sql sorts after 00-init.sh; each 01-* file USEs its own DB so inter-order is irrelevant. Low risk. |
</threat_model>

<verification>
- `grep` asserts on the two SQL files + deploy.sh wiring (see per-task automated checks).
- `bash -n scripts/smoke-phase4.sh` for syntax.
- Runtime verification of the migration + smoke assertions (incl. the asymmetric fan-trap canary) happens in Plan 05 (live cutover) against the deployed VPS stack — Docker is not available locally (04-RESEARCH §Environment Availability).
</verification>

<success_criteria>
- posts/reactions/comments will exist on the live proconnect_feed volume after deploy (migration wired blocking).
- Migration is idempotent + non-destructive + carries the demo feed seed (incl. 1 repost; post 1 with an asymmetric 2 reactions + 1 comment fan-trap canary).
- smoke-phase4.sh is a valid, executable, non-destructive harness covering FEED-01..06 + EXACT asymmetric count-correctness + my_reaction + PII + 404 + ownership + newest-first, referenced by every later wave.
</success_criteria>

<output>
After completion, create `.planning/phases/04-news-feed/04-01-SUMMARY.md`
</output>
</content>
</invoke>
