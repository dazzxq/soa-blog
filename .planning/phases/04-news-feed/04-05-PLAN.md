---
phase: 04-news-feed
plan: 05
type: execute
wave: 4
depends_on: ["01", "02", "03", "04"]
files_modified: []
autonomous: false
requirements: [FEED-01, FEED-02, FEED-03, FEED-04, FEED-05, FEED-06]
tags: [deploy, cutover, smoke, verification]

must_haves:
  truths:
    - "Codex impl-review approves the Phase 4 code before any commit (CLAUDE.md gate)"
    - "Push to main triggers CI/CD; deploy.sh applies db/04-migrate-phase4.sql to the live proconnect_feed volume (posts/reactions/comments + demo seed)"
    - "smoke-phase4.sh passes on the deployed soa.duyet.vn stack (FEED-01..06 + composition + count-correctness + my_reaction + email-leak + 404-invariant + ownership + newest-first)"
    - "smoke-phase1/2/3 still pass (no regression); the production site stays live"
  artifacts: []
  key_links:
    - from: "GitHub Actions deploy"
      to: "live proconnect_feed volume"
      via: "scripts/deploy.sh phase-4 migration step"
      pattern: "04-migrate-phase4"
---

<objective>
Live cutover for Phase 4: run the mandatory Codex impl-review, commit + push to main, let
CI/CD deploy to soa.duyet.vn (applying the idempotent posts/reactions/comments migration to
the live proconnect_feed volume), then verify the full smoke suite green on the deployed stack
and confirm the FEED-06 /api/feed composition payoff is live. Mirrors the Phase 1/2/3 cutover.

Purpose: Phase 4 code is only "done" when it runs on the live VPS with no regression and the
migration has materialized posts/reactions/comments on the existing (empty) proconnect_feed
volume (Pitfall 1). Docker is not available locally, so runtime verification happens here.

Output: deployed Phase 4 on soa.duyet.vn; smoke-phase1/2/3/4 all green; no code files changed
in this plan (orchestration + verification only).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-news-feed/04-VALIDATION.md

<interfaces>
Deploy flow (Phase 1/2/3 precedent): push main -> GitHub Actions (.github/workflows/deploy.yml):
  (1) lint job = php -l over all *.php; (2) deploy job = SSH to VPS -> scripts/deploy.sh
  (git pull --ff-only -> docker compose build --pull -> phase-2/3/4 migration steps -> up -d
  --remove-orphans -> gateway health wait) -> verify https://soa.duyet.vn/api/health.
The phase-4 migration step (Plan 01) is applied to proconnect_feed BEFORE full-topology up so
feed-service boots against existing tables.

CLAUDE.md mandatory gates: /codex-impl-review BEFORE commit; only commit after Codex APPROVE.
(The /codex-plan-review gate runs on THIS plan set before any Phase 4 coding begins.)

Smoke entry points: GW=https://soa.duyet.vn bash scripts/smoke-phaseN.sh (the scripts accept a
GW override). Demo creds: any of demo/duyet/long/diep/tai with password demo@123**.
Seed (Plan 01): post 1 by duyet, post 2 by long, post 3 = demo's repost of post 1; reaction on
post 1 (long, love) + on post 2 (duyet, like); 1 comment on post 1 (long).
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Codex impl-review gate (mandatory, before commit)</name>
  <what-built>The full Phase 4 changeset across Plans 01-04: db/04-migrate-phase4.sql + db/01-schema-feed.sql, deploy.sh phase-4 step, scripts/smoke-phase4.sh, feed-service PostController + CommentController + routes, gateway FeedClient + FeedController + routes + DI, web/feed.html + index nav link.</what-built>
  <action>Present the full Phase 4 changeset to Codex via /codex-impl-review and iterate until APPROVE. Human-gated checkpoint: the executor runs the review command, applies any valid fixes (especially the timeline count query / fan-trap, the email allowlist, the no-N+1 repost batch, and the X-User-Id owner-scoping), and re-runs until Codex approves. No commit happens before approval.</action>
  <how-to-verify>
    1. Run `/codex-impl-review` on the staged Phase 4 changes (CLAUDE.md mandatory gate).
    2. If Codex raises valid issues, fix them and re-run `/codex-impl-review` until APPROVE.
    3. Confirm `php -l` is clean across all changed *.php and `bash -n scripts/smoke-phase4.sh` is clean.
    Do NOT commit until Codex APPROVES.
  </how-to-verify>
  <resume-signal>Type "approved" once Codex has approved the implementation, or describe the issues to fix.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Commit + push to main (triggers CI/CD deploy)</name>
  <read_first>scripts/deploy.sh (the full deploy flow incl. the phase-4 step added in Plan 01); .github/workflows/deploy.yml (lint + deploy jobs).</read_first>
  <files></files>
  <action>
After Codex APPROVE (Task 1): per the SOA workflow rule (local git -> review -> push public GitHub
-> VPS pull -> CI/CD auto-deploy), stage the Phase 4 files, commit with a descriptive message, and
push to main so GitHub Actions deploys. Use the gsd commit helper / git as appropriate. The commit
message ends with the Co-Authored-By trailer. Push to the default branch only after review per
CLAUDE.md. Then monitor the GitHub Actions run to green (lint + deploy jobs).
  </action>
  <verify>
    <automated>cd /Users/theduyet/Documents/Code/SOA/soa-blog && git log --oneline -1 | grep -iE "feed|phase 4|bảng tin|news" && gh run list --limit 1 --json conclusion --jq '.[0].conclusion' | grep -q success</automated>
  </verify>
  <done>Phase 4 committed and pushed; GitHub Actions deploy run concludes success.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify live deploy + full smoke suite (no regression) + FEED-06 payoff</name>
  <what-built>Phase 4 deployed to soa.duyet.vn: feed-service real routes (posts/reactions/comments/repost + timeline query), gateway /api/feed 3-service composition + posts/reactions/comments passthrough, web/feed.html, and the live-volume posts/reactions/comments tables + demo seed via the deploy.sh phase-4 step.</what-built>
  <action>Human-gated verification checkpoint: run the full smoke suite (smoke-phase1/2/3/4) against https://soa.duyet.vn, confirm SHOW TABLES lists posts/reactions/comments with the demo seed, confirm the /api/feed composition payoff (author + counts + my_reaction, no email, newest-first, degrade-safe), and eyeball the browser feed UX. The executor runs the commands; the human confirms the results.</action>
  <how-to-verify>
    1. Confirm migration applied: on the VPS, `docker compose exec -T mariadb mysql -uroot -p... proconnect_feed -e "SHOW TABLES;"` lists `posts`, `reactions`, `comments`, and `SELECT id,author_id,repost_of FROM posts;` shows the demo seed (post 1 duyet, post 2 long, post 3 repost_of=1).
    2. Full smoke, no regression:
       `GW=https://soa.duyet.vn bash scripts/smoke-phase1.sh && ...phase2.sh && ...phase3.sh && ...phase4.sh`
       -> all four print ALL PASS.
    3. FEED-06 payoff (the headline composition): as duyet, `curl -H "Authorization: Bearer <duyet-token>" "https://soa.duyet.vn/api/feed?limit=20"` returns a newest-first timeline where each item has `author` (with display_name), `reaction_count`, `comment_count`, `my_reaction`, `repost_of`; the repost item carries `original` with its author+content; and the WHOLE body contains NO email/`@` (allowlist working). long's post appears in duyet's feed (self+connections).
    4. Count-correctness: the seed post 1 (1 reaction + 1 comment) reports `reaction_count:1` + `comment_count:1` (not inflated — fan-trap check).
    5. Browser UX (manual-only per 04-VALIDATION.md): open https://soa.duyet.vn/feed.html — compose a post, react/change/remove, comment + delete, repost (shows origin), delete own post; confirm Vietnamese + no email on cards. (This is the Plan 04 human-verify, re-confirmed on the deployed stack.)
    6. Production site still serves normally (no 5xx on /api/health).
  </how-to-verify>
  <resume-signal>Type "approved" once all four smoke suites pass, the /api/feed composition payoff checks out (author+counts+my_reaction, no email, newest-first, correct counts), and the browser UX is good; otherwise describe the failure for gap closure.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub Actions → VPS | SSH-keyed deploy; runs scripts/deploy.sh as configured |
| deploy.sh → live mariadb volume | Phase-4 migration applied as root; non-destructive + idempotent (Plan 01) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-22 | Denial (broken prod) | live deploy | mitigate | Migration is additive/idempotent + applied before full-topology up; deploy.sh waits for gateway health and aborts on failure; smoke-phase1/2/3 confirm no regression before sign-off. |
| T-04-23 | Integrity (review gate) | commit | mitigate | Mandatory /codex-impl-review APPROVE before commit (CLAUDE.md); CI php -l lint gate. |
| T-04-24 | Information disclosure (PII) | live feed/comment cards | mitigate | smoke-phase4 asserts no `@`/email in feed/post/comment bodies on the deployed stack; human-verify re-confirms. |
| T-04-25 | Integrity (count fan-trap) | live timeline counts | mitigate | smoke-phase4 asserts the seed post's reaction_count/comment_count are exact (not inflated) on the deployed stack. |
</threat_model>

<verification>
- GitHub Actions deploy run = success (lint + deploy).
- smoke-phase1/2/3/4 all ALL PASS against https://soa.duyet.vn.
- SHOW TABLES confirms posts/reactions/comments + demo seed on the live proconnect_feed volume.
- GET /api/feed as duyet shows the 3-service composition payoff (author+counts+my_reaction, no email, newest-first, correct counts) live (FEED-06).
</verification>

<success_criteria>
- Phase 4 is deployed on soa.duyet.vn with posts/reactions/comments materialized on the live volume.
- The full smoke suite is green (no regression) and the FEED-06 /api/feed composition payoff is live.
- The mandatory Codex review gate was satisfied before commit.
</success_criteria>

<output>
After completion, create `.planning/phases/04-news-feed/04-05-SUMMARY.md`
</output>
