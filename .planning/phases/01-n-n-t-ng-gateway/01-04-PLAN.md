---
phase: 01-n-n-t-ng-gateway
plan: 04
type: execute
wave: 3
depends_on: [02, 03, 05]
files_modified:
  - docker-compose.yml
  - .env.example
  - gateway/public/index.php
  - gateway/src/routes.php
  - gateway/src/Controllers/HealthController.php
  - gateway/src/Controllers/ProfilesController.php
  - gateway/src/Controllers/AuthController.php
  - gateway/src/Services/HttpClient.php
  - gateway/src/Middleware/RequestIdMiddleware.php
  - gateway/src/Services/UserClient.php
  - gateway/src/Services/PostClient.php
  - gateway/src/Services/CommentClient.php
  - gateway/src/Controllers/UsersController.php
  - gateway/src/Controllers/PostsController.php
  - gateway/src/Controllers/AggregateController.php
autonomous: true
requirements: [PLAT-01, PLAT-02, PLAT-03, PLAT-04, PLAT-05, PROF-01]
must_haves:
  truths:
    - "docker compose up boots an 8-container stack (mariadb, gateway, web, profile, connection, feed, search, notification); container count <=9; post/comment removed"
    - "Gateway routes /api/profiles/{id} to profile-service, keeps /api/auth/register, /api/auth/login, /api/me; old /api/posts and /api/comments routes removed"
    - "GET /api/profiles/{id} returns ONLY id, username, display_name (D-07 whitelist); email/avatar_url are NOT exposed on this public route"
    - "Gateway /api/health fans out to all 5 services and is 200 only when all 5 report ok (D-10)"
    - "Every gateway->service call forwards the X-Request-Id header (D-12); a downstream stub echoes it back as `rid`, and the gateway /api/health body surfaces that echoed rid (provable via smoke-phase1.sh)"
    - "JWT verify stays gateway-only; services receive only X-User-Id (PLAT-03)"
  artifacts:
    - path: "docker-compose.yml"
      provides: "8-service topology, no post/comment, 5 *_SVC_DB_PASS in mariadb env, 5 *_SERVICE_URL in gateway env"
      contains: "proconnect_profile"
    - path: "gateway/src/Controllers/HealthController.php"
      provides: "5-way health fan-out"
      contains: "notification"
    - path: "gateway/src/Controllers/ProfilesController.php"
      provides: "/api/profiles/{id} -> profile-service /users/{id}"
      contains: "ProfileClient"
  key_links:
    - from: "gateway/src/routes.php"
      to: "ProfilesController"
      via: "/api/profiles/{id} route"
      pattern: "profiles"
    - from: "gateway/src/Services/HttpClient.php"
      to: "downstream services"
      via: "X-Request-Id default header"
      pattern: "X-Request-Id"
    - from: "docker-compose.yml gateway depends_on"
      to: "5 services healthy"
      via: "depends_on service_healthy"
      pattern: "service_healthy"
---

<objective>
The convergence wave: assemble docker-compose.yml into the 8-container topology, rewire the gateway (DI, routes, controllers), add X-Request-Id downstream forwarding (D-12), retire post/comment (D-01), and expose /api/profiles/{id} (D-07) + 5-way /api/health fan-out (D-10). This is the single shared-file bottleneck — it consumes the ProfileClient (Plan 02), the 4 stub clients (Plan 03), and the proconnect_* schema + 5 password env-var naming (Plan 05).

Purpose: PLAT-01..05 (topology + routing + central auth + central cross-cutting + clean boot) and PROF-01 (auth survives rename). Depends on 02 (ProfileClient + profile-service exist), 03 (4 stub clients + services exist, stub /health echoes X-Request-Id as rid), 05 (proconnect_* DB naming + 5 password vars exist).
Output: rewritten compose + gateway wiring + new ProfilesController + 5-way HealthController + X-Request-Id forwarding.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-n-n-t-ng-gateway/01-CONTEXT.md
@.planning/phases/01-n-n-t-ng-gateway/01-RESEARCH.md
@.planning/phases/01-n-n-t-ng-gateway/01-VALIDATION.md

<interfaces>
<!-- gateway/public/index.php DI registers UserClient/PostClient/CommentClient + AuthController(UserClient,jwt) + HealthController(User,Post,Comment) + UsersController + PostsController + AggregateController. Middleware add order (LIFO) Logging->CORS->RateLimit->RequestId MUST stay untouched (RESEARCH Pitfall 4). -->
<!-- AuthController: __construct(private UserClient $users, private string $secret, private int $ttlSeconds=86400). Calls $this->users->create/verifyCredentials/get — ProfileClient has identical signatures, so swap the type to ProfileClient, keep property name. -->
<!-- UsersController::show (clone template for ProfilesController): $upstream = $this->users->get((int)$args['id']); return Json::raw($res, decode($upstream), $upstream->getStatusCode()); -->
<!-- HealthController::check current 3-way settle pattern -> extend to 5 keys profile/connection/feed/search/notification. Returns {status, services, ts}, 200 iff all ok. The `services` map MUST carry each downstream /health BODY verbatim (so the stub-echoed `rid` is visible to the smoke test — D-12 receipt proof). -->
<!-- HttpClient::create(string $baseUri): headers Accept + X-Forwarded-By. Add X-Request-Id sourced from a per-request value. -->
<!-- RequestIdMiddleware sets request attribute 'request_id' + response header X-Request-Id. -->
<!-- docker-compose.yml: anchor *svc-healthcheck (wget /health). mariadb env: MARIADB_ROOT_PASSWORD + USER/POST/COMMENT_SVC_DB_PASS. gateway env: JWT_SECRET + USER/POST/COMMENT_SERVICE_URL + RATE_LIMIT_PER_MIN; gateway depends_on user/post/comment service_healthy; gateway ports 127.0.0.1:8000:80. user-service block: build ./services/user-service, env DB_HOST/DB_NAME/DB_USER/DB_PASS, NO ports, healthcheck *svc-healthcheck. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: X-Request-Id downstream forwarding + ProfilesController + 5-way HealthController + AuthController retype</name>
  <read_first>
    - gateway/src/Middleware/RequestIdMiddleware.php (where the rid is generated/stored)
    - gateway/src/Services/HttpClient.php (default headers sent on every downstream call)
    - gateway/src/Controllers/UsersController.php (clone template for ProfilesController)
    - gateway/src/Controllers/HealthController.php (3-way fan-out to extend to 5; confirm it returns each service's body in the `services` map)
    - gateway/src/Controllers/AuthController.php (constructor type UserClient -> ProfileClient)
    - gateway/src/Services/ProfileClient.php (Plan 02 — confirm get(int)/create/verifyCredentials signatures)
    - gateway/src/Services/ConnectionClient.php, FeedClient.php, SearchClient.php, NotificationClient.php (Plan 03 — confirm healthAsync())
    - services/connection-service/src/Controllers/HealthController.php (Plan 03 — confirm stub /health echoes inbound X-Request-Id as `rid`)
    - .planning/phases/01-n-n-t-ng-gateway/01-RESEARCH.md (Pattern 5 approach C; Pitfall 5)
  </read_first>
  <files>gateway/src/Services/HttpClient.php, gateway/src/Middleware/RequestIdMiddleware.php, gateway/src/Controllers/ProfilesController.php, gateway/src/Controllers/HealthController.php, gateway/src/Controllers/AuthController.php</files>
  <action>
A) X-Request-Id downstream (D-12, RESEARCH Pattern 5 approach C — request-scoped rid stashed on HttpClient, read at client construction):
   - In gateway/src/Services/HttpClient.php add `private static ?string $requestId = null;` + `public static function setRequestId(?string $rid): void { self::$requestId = $rid; }`. In create() add `'X-Request-Id' => self::$requestId ?? ''` to the headers array (alongside Accept + X-Forwarded-By).
   - In gateway/src/Middleware/RequestIdMiddleware.php after computing `$rid`, call `\App\Services\HttpClient::setRequestId($rid);`. Keep the existing request attribute set + `withHeader('X-Request-Id', $rid)` response behavior unchanged.
   - WHY THIS IS SAFE (correct the prior false premise): php-fpm workers are REUSED across requests, so a static is NOT reset between requests by process isolation. The mechanism is safe for a different reason: all service clients are constructed LAZILY per-request inside route handlers (PHP-DI factories run when first resolved), which is strictly AFTER the middleware stack — and therefore after RequestIdMiddleware has called setRequestId() for the CURRENT request. RequestIdMiddleware runs at the top of every request and overwrites the static, so the value can never be stale from a prior request. Add an explicit code comment in HttpClient stating this invariant: "X-Request-Id is set by RequestIdMiddleware at the start of every request; clients MUST NOT be constructed before that middleware runs (they are not — DI resolves them lazily inside route handlers)." If any future code constructs a client before the middleware, this guarantee breaks — the comment is the guard.

B) ProfilesController (D-07, PLAT-02): create gateway/src/Controllers/ProfilesController.php (namespace App\Controllers, final class ProfilesController) cloning UsersController: `__construct(private ProfileClient $profiles)`. D-07 (LOCKED) requires `/api/profiles/{id}` to return ONLY basic info: id, username, display_name. This is a PUBLIC, unauthenticated route — passing through the full profile-service body would leak `email` (and avatar_url) publicly. Therefore TRIM the response now via a whitelist; do NOT pass through verbatim.
   - `show(Request $req, Response $res, array $args): Response` body:
     1. `$upstream = $this->profiles->get((int)$args['id']);`
     2. `$status = $upstream->getStatusCode();`
     3. `$decoded = $this->decode($upstream);` (private decode() helper, same as UsersController)
     4. On success (2xx) trim to the D-07 whitelist BEFORE returning. The profile-service `{data: {...}}` envelope wraps the user object; whitelist its `data`:
        `if ($status >= 200 && $status < 300 && isset($decoded['data']) && is_array($decoded['data'])) { $decoded['data'] = array_intersect_key($decoded['data'], array_flip(['id','username','display_name'])); }`
        (array_intersect_key with a fixed whitelist key set — POSITIVE allowlist, so email/avatar_url/created_at/updated_at are dropped regardless of what the service adds later.)
     5. On error status, pass the error body through unchanged (do not whitelist `{error}` envelopes).
     6. `return Json::raw($res, $decoded, $status);`
   - Use App\Services\ProfileClient. This honors D-07 verbatim and prevents public email exposure on an unauthenticated route. (The fuller authenticated profile model arrives in Phase 2.)

C) 5-way HealthController (D-10): rewrite gateway/src/Controllers/HealthController.php constructor to `(ProfileClient $profile, ConnectionClient $connection, FeedClient $feed, SearchClient $search, NotificationClient $notification)`. Build `$promises` with keys profile/connection/feed/search/notification each `->healthAsync()`. Keep the existing `Utils::settle(...)->wait()` loop returning {status, services, ts}, 200 iff all ok else 503. The `services.<svc>` value MUST be the decoded downstream /health body (status, db, rid, ts) — NOT a reduced summary — so the stub-echoed `rid` (Plan 03) is surfaced for the smoke test's D-12 downstream-receipt assertion (ISSUE-5). Remove all UserClient/PostClient/CommentClient imports + usage.

D) AuthController retype (PROF-01): change constructor param `private UserClient $users` -> `private ProfileClient $users` (keep property name $users so call sites are unchanged). Change `use App\Services\UserClient;` -> `use App\Services\ProfileClient;`. JWT signing logic untouched.
  </action>
  <verify>
    <automated>php -l gateway/src/Services/HttpClient.php && php -l gateway/src/Middleware/RequestIdMiddleware.php && php -l gateway/src/Controllers/ProfilesController.php && php -l gateway/src/Controllers/HealthController.php && php -l gateway/src/Controllers/AuthController.php && grep -q 'X-Request-Id' gateway/src/Services/HttpClient.php && grep -q 'HttpClient::setRequestId' gateway/src/Middleware/RequestIdMiddleware.php && grep -q 'ProfileClient' gateway/src/Controllers/ProfilesController.php && grep -q 'notification' gateway/src/Controllers/HealthController.php && grep -q 'ProfileClient' gateway/src/Controllers/AuthController.php</automated>
  </verify>
  <acceptance_criteria>
    - HttpClient.php: `grep -q "'X-Request-Id'"` AND `grep -q 'public static function setRequestId'`
    - RequestIdMiddleware.php: `grep -q 'HttpClient::setRequestId'` AND still `grep -q "withHeader('X-Request-Id'"`
    - ProfilesController.php exists with `final class ProfilesController`, uses ProfileClient, calls `->get(`, and trims the success body to the D-07 whitelist: `grep -q 'array_intersect_key' gateway/src/Controllers/ProfilesController.php` AND `grep -q "display_name" gateway/src/Controllers/ProfilesController.php` AND `! grep -q "'email'" gateway/src/Controllers/ProfilesController.php` (email is NOT in the allowlist)
    - HttpClient.php carries the request-scoped X-Request-Id invariant comment (no false php-fpm claim): `grep -qi 'MUST NOT be constructed before' gateway/src/Services/HttpClient.php` AND `! grep -qi 'one request per process' gateway/src/Services/HttpClient.php`
    - HealthController.php contains all 5 keys: `for k in profile connection feed search notification; do grep -q "'$k'" gateway/src/Controllers/HealthController.php; done`; and `! grep -qE 'PostClient|CommentClient|UserClient' gateway/src/Controllers/HealthController.php`
    - HealthController surfaces each downstream body in `services` (so the stub `rid` is visible — ISSUE-5): the settle loop assigns the decoded body (not a boolean/summary) to each `services.<svc>` entry — confirm by reading the method (no acceptance grep can prove JSON shape, so verified at runtime by smoke check #6, which fails if the gateway body lacks the echoed rid).
    - AuthController.php: `grep -q 'ProfileClient'` AND `! grep -q 'use App\\Services\\UserClient'`
    - `php -l` clean on all 5 files
  </acceptance_criteria>
  <done>Downstream X-Request-Id forwarding in place; ProfilesController created; HealthController fans out to 5 services and surfaces each downstream body (carrying the stub-echoed rid); AuthController uses ProfileClient. All lint clean.</done>
</task>

<task type="auto">
  <name>Task 2: Rewire gateway DI + routes; remove retired post/comment + user code</name>
  <read_first>
    - gateway/public/index.php (DI container set() calls + middleware add block — DO NOT reorder middleware, Pitfall 4)
    - gateway/src/routes.php (the /api route group to rewrite)
    - gateway/src/Services/ProfileClient.php (Plan 02), ConnectionClient/FeedClient/SearchClient/NotificationClient.php (Plan 03)
  </read_first>
  <files>gateway/public/index.php, gateway/src/routes.php, gateway/src/Services/UserClient.php, gateway/src/Services/PostClient.php, gateway/src/Services/CommentClient.php, gateway/src/Controllers/UsersController.php, gateway/src/Controllers/PostsController.php, gateway/src/Controllers/AggregateController.php</files>
  <action>
1. gateway/public/index.php DI rewrite (RESEARCH Pitfall 5):
   - Replace `use` imports + `$container->set(...)` for UserClient/PostClient/CommentClient with ProfileClient/ConnectionClient/FeedClient/SearchClient/NotificationClient: `$container->set(ProfileClient::class, fn() => new ProfileClient());` ... one per service (5 total).
   - AuthController: `fn(Container $c) => new AuthController($c->get(ProfileClient::class), $jwtSecret)`.
   - HealthController: `fn(Container $c) => new HealthController($c->get(ProfileClient::class), $c->get(ConnectionClient::class), $c->get(FeedClient::class), $c->get(SearchClient::class), $c->get(NotificationClient::class))`.
   - ProfilesController: `fn(Container $c) => new ProfilesController($c->get(ProfileClient::class))`.
   - REMOVE the `set()` registrations and `use` imports for UsersController, PostsController, AggregateController.
   - Keep JwtAuthMiddleware registration and the entire middleware `$app->add(...)` block UNTOUCHED (Logging->CORS->RateLimit->RequestId, LIFO order — Pitfall 4).
2. gateway/src/routes.php rewrite (D-07): inside the `/api` group keep:
   - `$g->get('/health', [HealthController::class, 'check']);`
   - `$g->post('/auth/register', [AuthController::class, 'register']);`
   - `$g->post('/auth/login', [AuthController::class, 'login']);`
   - `$g->get('/me', [AuthController::class, 'me'])->add($jwtMw);`
   - CHANGE `/users/{id}` -> `$g->get('/profiles/{id:[0-9]+}', [ProfilesController::class, 'show']);`
   - REMOVE all /posts, /posts/{id}/full, /posts/{id}/comments, /comments routes and their `use` imports for PostsController/AggregateController/UsersController.
3. Delete the now-unused gateway files (post/comment/user retired — D-01; keep service-folder code on disk but gateway clients/controllers go): remove gateway/src/Services/UserClient.php, PostClient.php, CommentClient.php and gateway/src/Controllers/UsersController.php, PostsController.php, AggregateController.php via `git rm`. (ProfileClient replaces UserClient; the 4 stub clients replace post/comment.) Confirm nothing else imports them: `grep -rl 'UserClient\|PostClient\|CommentClient\|UsersController\|PostsController\|AggregateController' gateway/` returns no matches after edits.
  </action>
  <verify>
    <automated>php -l gateway/public/index.php && php -l gateway/src/routes.php && grep -q 'ProfileClient' gateway/public/index.php && grep -q "profiles/{id" gateway/src/routes.php && ! grep -qE 'PostsController|AggregateController|UsersController|UserClient|PostClient|CommentClient' gateway/public/index.php gateway/src/routes.php && ! grep -q "'/posts'" gateway/src/routes.php && test ! -f gateway/src/Services/UserClient.php && test ! -f gateway/src/Controllers/PostsController.php</automated>
  </verify>
  <acceptance_criteria>
    - gateway/public/index.php registers 5 clients: `for c in Profile Connection Feed Search Notification; do grep -q "${c}Client::class" gateway/public/index.php; done`
    - HealthController DI passes 5 clients; AuthController + ProfilesController DI use ProfileClient
    - `! grep -qE 'PostsController|AggregateController|UsersController|UserClient::class|PostClient::class|CommentClient::class' gateway/public/index.php`
    - Middleware block intact + ordered: `grep -A4 'addBodyParsingMiddleware' gateway/public/index.php` still shows LoggingMiddleware then CorsMiddleware then RateLimitMiddleware then RequestIdMiddleware add order unchanged
    - routes.php has `/profiles/{id:[0-9]+}` and `/auth/register`, `/auth/login`, `/me`, `/health`; `! grep -q "/posts" gateway/src/routes.php` AND `! grep -q "/comments" gateway/src/routes.php` AND `! grep -q "/users" gateway/src/routes.php`
    - Files removed: `test ! -f` for gateway/src/Services/{UserClient,PostClient,CommentClient}.php and gateway/src/Controllers/{UsersController,PostsController,AggregateController}.php
    - `grep -rl 'UsersController\|PostsController\|AggregateController\|UserClient\|PostClient\|CommentClient' gateway/src gateway/public` returns nothing
    - `php -l` clean on index.php and routes.php
  </acceptance_criteria>
  <done>Gateway DI registers exactly the 5 service clients + Auth/Health/Profiles controllers; routes expose /api/health, /api/auth/*, /api/me, /api/profiles/{id} only; all post/comment/user gateway code removed; middleware order untouched.</done>
</task>

<task type="auto">
  <name>Task 3: Rewrite docker-compose.yml to 8-container topology + .env.example</name>
  <read_first>
    - docker-compose.yml (anchor *svc-healthcheck, user/post/comment blocks, gateway env+depends_on, mariadb env)
    - .env.example (current 4 password vars + JWT + rate limit)
    - .planning/phases/01-n-n-t-ng-gateway/01-RESEARCH.md (Pattern 2 compose block clone; A3 keep service_healthy gates)
    - db/00-init.sh (Plan 05 — confirms the 5 *_SVC_DB_PASS names the mariadb block must pass)
  </read_first>
  <files>docker-compose.yml, .env.example</files>
  <action>
1. mariadb service env block: replace USER_SVC_DB_PASS/POST_SVC_DB_PASS/COMMENT_SVC_DB_PASS with the 5 new vars: `PROFILE_SVC_DB_PASS: ${PROFILE_SVC_DB_PASS}`, `CONNECTION_SVC_DB_PASS: ${CONNECTION_SVC_DB_PASS}`, `FEED_SVC_DB_PASS: ${FEED_SVC_DB_PASS}`, `SEARCH_SVC_DB_PASS: ${SEARCH_SVC_DB_PASS}`, `NOTIFICATION_SVC_DB_PASS: ${NOTIFICATION_SVC_DB_PASS}`. Keep MARIADB_ROOT_PASSWORD. Volume + healthcheck + network unchanged.
2. Rename the user-service block to profile-service: `profile-service:` with `build: ./services/profile-service`, env `DB_HOST: mariadb`, `DB_NAME: proconnect_profile`, `DB_USER: profile_svc`, `DB_PASS: ${PROFILE_SVC_DB_PASS}`, `networks: [blog-net]`, `depends_on: mariadb service_healthy`, `healthcheck: *svc-healthcheck`, `restart: unless-stopped`. NO `ports:` (PLAT-03 trust-by-network — Pitfall: never add a host port to a service).
3. REMOVE the post-service and comment-service blocks entirely (D-01; their code stays on disk but no container).
4. ADD 4 stub service blocks cloning the profile block (Pattern 2), for connection/feed/search/notification: `build: ./services/<svc>-service`, `DB_NAME: proconnect_<svc>`, `DB_USER: <svc>_svc`, `DB_PASS: ${<SVC>_SVC_DB_PASS}`, same network/depends_on/healthcheck/restart, NO ports.
5. gateway service: replace env `USER_SERVICE_URL/POST_SERVICE_URL/COMMENT_SERVICE_URL` with `PROFILE_SERVICE_URL: http://profile-service:80`, `CONNECTION_SERVICE_URL: http://connection-service:80`, `FEED_SERVICE_URL: http://feed-service:80`, `SEARCH_SERVICE_URL: http://search-service:80`, `NOTIFICATION_SERVICE_URL: http://notification-service:80`. Keep JWT_SECRET + RATE_LIMIT_PER_MIN + ports 127.0.0.1:8000:80 + gateway healthcheck. Replace `depends_on` (user/post/comment service_healthy) with the 5 services each `{ condition: service_healthy }` (RESEARCH A3 — enforces PLAT-05 "all healthy").
6. web service unchanged. Final services count = 8 (mariadb, gateway, web, profile, connection, feed, search, notification) — verify <=9.
7. .env.example: replace USER_SVC_DB_PASS/POST_SVC_DB_PASS/COMMENT_SVC_DB_PASS lines with the 5 new `*_SVC_DB_PASS=change-me-<svc>` lines. Keep DB_ROOT_PASSWORD, JWT_SECRET (>=32 char placeholder unchanged), RATE_LIMIT_PER_MIN. (The live VPS .env is edited by hand in Plan 06 — flagged there; Plan 06 constrains the values to a shell-safe [A-Za-z0-9]{32} charset.)
  </action>
  <verify>
    <automated>docker compose config -q && test $(docker compose config --services | wc -l | tr -d ' ') -le 9 && for s in mariadb gateway web profile-service connection-service feed-service search-service notification-service; do docker compose config --services | grep -qx "$s"; done && ! docker compose config --services | grep -qx 'post-service' && ! docker compose config --services | grep -qx 'comment-service' && grep -q 'PROFILE_SVC_DB_PASS' .env.example && grep -q 'NOTIFICATION_SERVICE_URL' docker-compose.yml && ! grep -qE 'POST_SVC_DB_PASS|COMMENT_SVC_DB_PASS' .env.example docker-compose.yml</automated>
  </verify>
  <acceptance_criteria>
    - `docker compose config -q` exits 0 (valid compose)
    - `docker compose config --services | wc -l` == 8 and `<= 9` (PLAT-05); services are exactly mariadb, gateway, web, profile-service, connection-service, feed-service, notification-service, search-service
    - `! docker compose config --services | grep -qx post-service` AND `! ... comment-service` (D-01 retired)
    - profile-service block: `DB_NAME: proconnect_profile`, `DB_USER: profile_svc`, `DB_PASS: ${PROFILE_SVC_DB_PASS}`, build ./services/profile-service, NO `ports:` line in any service block except gateway(8000) and web(8080): `docker compose config | grep -c 'published'` == 2
    - 4 stub blocks present with proconnect_<svc>/<svc>_svc env
    - gateway env has all 5 `*_SERVICE_URL` (profile/connection/feed/search/notification) and gateway depends_on lists all 5 with service_healthy: `grep -c 'service_healthy' docker-compose.yml` >= 6 (mariadb gate ×5 services + gateway gate ×5)
    - mariadb env block has all 5 `*_SVC_DB_PASS`, no USER/POST/COMMENT_SVC_DB_PASS
    - .env.example has 5 new `*_SVC_DB_PASS`, no old 3; JWT_SECRET placeholder unchanged
  </acceptance_criteria>
  <done>docker-compose.yml defines exactly 8 services (post/comment removed, profile + 4 stubs added), all internal-only except gateway/web, gateway depends on all 5 services healthy with 5 *_SERVICE_URL; .env.example carries 5 *_SVC_DB_PASS; compose config validates.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| internet -> gateway (8000) | only public entry; JWT verified here |
| gateway -> 5 services (blog-net) | trusted X-User-Id + X-Request-Id forwarded; services have no host port |
| service -> own DB | scoped *_svc user |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1-12 | Spoofing | a service accidentally bound to a host port -> direct unauthenticated access bypassing JWT | mitigate (HIGH) | Task 3: NO `ports:` on any of the 5 services; acceptance asserts exactly 2 published ports (gateway 8000, web 8080). |
| T-1-13 | Elevation | retired PostClient/CommentClient left in DI -> dangling routes / boot anomalies | mitigate | Task 2 removes all post/comment/user gateway code + DI + routes; acceptance greps for zero references. |
| T-1-14 | Tampering | middleware reorder breaks rate-limit/request-id for new traffic (PLAT-04) | mitigate | Task 2 forbids touching the `$app->add(...)` block; acceptance asserts Logging->CORS->RateLimit->RequestId order intact (Pitfall 4). |
| T-1-15 | Repudiation | missing request correlation across services | mitigate | X-Request-Id forwarded downstream (Task 1, D-12); each stub echoes it as `rid`, gateway /api/health surfaces it, and smoke-phase1.sh asserts the gateway-set id was received downstream (ISSUE-5 receipt proof). Response header preserved. |
| T-1-16 | Info disclosure | JWT_SECRET leaking into a service | mitigate | Services never receive JWT_SECRET; only gateway env has it; stubs are health-only. |
| T-1-17 | Info disclosure | email leaked on PUBLIC /api/profiles/{id} (unauthenticated) | mitigate (HIGH) | ProfilesController trims the success body to the D-07 whitelist {id,username,display_name} via array_intersect_key; acceptance + runtime smoke assert email is absent. Honors D-07 verbatim. |
</threat_model>

<verification>
`docker compose config -q` + service-count <=9 + `php -l` on all gateway PHP. Full functional gate: `docker compose up -d --wait && bash scripts/smoke-phase1.sh` (Plan 01 script) — asserts 5-way health ok, /api/profiles/2 returns duyet, /api/me 401, X-Request-Id downstream receipt, 5-account login. Two W2/W1-specific functional assertions (run against the booted stack):
- D-07 trim: `curl -sf http://127.0.0.1:8000/api/profiles/2 | grep -q duyet && curl -sf http://127.0.0.1:8000/api/profiles/2 | grep -qv '"email"'` — profile returns username but NOT email (positive whitelist verified at runtime).
- X-Request-Id DOWNSTREAM RECEIPT (D-12, ISSUE-5 — now concrete and consistent with Plan 01 + Plan 03): the gateway generates an X-Request-Id, forwards it to each service via HttpClient (Task 1A), each stub /health echoes it back as `rid` (Plan 03), and the gateway /api/health body surfaces those bodies under `services.<svc>` (Task 1C). The smoke script (Plan 01 check #6) reads the gateway response `X-Request-Id` header and asserts the SAME value appears as a `"rid"` inside the gateway /api/health body — proving the id was received downstream, not merely that the gateway response carried a header. Concretely: `rid=$(curl -sD- -o /tmp/b "$GW/api/health" | grep -i '^x-request-id:' | tr -d '\r' | awk '{print $2}'); grep -q "\"rid\":\"$rid\"" /tmp/b`.
This requires Plan 05's DB provisioning to have run on a fresh local volume.
</verification>

<success_criteria>
8-container stack boots clean; /api/profiles/{id} routes to profile-service; /api/health is 5-way; X-Request-Id flows downstream AND is provably received by a downstream service (smoke check #6); post/comment fully retired; JWT stays gateway-only; smoke-phase1.sh green locally.
</success_criteria>

<output>
After completion, create `.planning/phases/01-n-n-t-ng-gateway/01-04-SUMMARY.md`
</output>
</content>
