---
phase: 01-n-n-t-ng-gateway
plan: 06
type: execute
wave: 4
depends_on: [04, 05]
files_modified:
  - scripts/deploy.sh
  - .github/workflows/deploy.yml
autonomous: false
requirements: [PLAT-06]
must_haves:
  truths:
    - "Pushing main triggers CI -> the workflow `git pull --ff-only` in the VPS repo dir BEFORE invoking deploy.sh, so the NEW deploy.sh (preflight+backup+ordered-migrate) runs on the FIRST cutover, not the next one"
    - "Deploy order on cutover: bring up ONLY mariadb and wait healthy -> mandatory pre-wipe mysqldump backup (blocking) -> apply db/migrate-phase1.sql.tmpl -> THEN `docker compose up -d --remove-orphans` for the full 8-container topology -> gateway + public health verify"
    - "The deploy fails loudly if any required *_SVC_DB_PASS env var is missing from the live .env (preflight guard)"
    - "The pre-wipe backup is a BLOCKING precondition: dump failure aborts the deploy and the destructive migration never runs unless the backup file exists and is non-empty"
    - "The 5 new *_SVC_DB_PASS are constrained to a shell-safe + Compose-safe charset so `.env` sourcing and Compose interpolation cannot break"
  artifacts:
    - path: "scripts/deploy.sh"
      provides: "deploy with env preflight + mariadb-first boot + BLOCKING pre-wipe backup + ordered idempotent migration + full-topology up + health wait"
      contains: "migrate-phase1.sql.tmpl"
    - path: ".github/workflows/deploy.yml"
      provides: "git pull --ff-only before deploy.sh (cutover bootstrap) + lint + SSH deploy + public HTTPS health verify"
      contains: "soa.duyet.vn/api/health"
  key_links:
    - from: "scripts/deploy.sh"
      to: "db/migrate-phase1.sql.tmpl"
      via: "docker compose exec -T mariadb mysql"
      pattern: "migrate-phase1"
    - from: ".github/workflows/deploy.yml"
      to: "scripts/deploy.sh"
      via: "git pull --ff-only THEN ./scripts/deploy.sh"
      pattern: "pull --ff-only"
---

<objective>
Wire the Phase-1 DB cutover into the existing CI/CD and ship to production (PLAT-06). The deploy must, IN THIS ORDER: preflight-check the 5 new env vars exist in the live .env, bring up ONLY mariadb and wait until it is healthy, take a BLOCKING pre-wipe mysqldump backup (abort if it fails or is empty), apply the idempotent db/migrate-phase1.sql.tmpl against the running mariadb (because the live volume already exists and 00-init.sh will NOT re-run — RESEARCH Pitfall 1), THEN bring up the full 8-container stack with --remove-orphans (drops retired post/comment containers; only now do the DB-dependent services start, against DBs/users that already exist), and finally verify HTTPS health.

CRITICAL — FIRST-CUTOVER BOOTSTRAP (ISSUE-2): the GitHub Action SSHes to the VPS and runs `scripts/deploy.sh`. Today the `git pull` happens INSIDE deploy.sh, so on the FIRST cutover the OLD deploy.sh executes (no preflight/backup/migrate) and the new logic would only take effect on the NEXT deploy. Fix: the WORKFLOW does `git pull --ff-only` in the repo dir BEFORE invoking deploy.sh, guaranteeing the NEW deploy.sh runs on the cutover. deploy.sh no longer pulls (the workflow owns the pull); the web-change detection that depended on before/after SHA is reworked to diff against the reflog of the workflow's pull.

CRITICAL — MIGRATION ORDERING (ISSUE-3): the OLD flow did `docker compose up -d --remove-orphans` (all 8 containers) BEFORE the migration — so DB-dependent services boot against non-existent proconnect_* DBs/users, fail health, and the deploy smoke fails on first cutover. New order: mariadb-only up + healthy → backup → migrate → full up.

CRITICAL — BACKUP IS BLOCKING (ISSUE-4): the pre-wipe mysqldump must NOT be `|| true`. Dump failure aborts the deploy; the destructive migration runs only after the backup file is confirmed present and non-empty (`test -s`).

This plan is NOT autonomous: it contains a destructive live-VPS DB wipe and a git-ignored live .env edit that only a human can perform on the VPS.

Purpose: PLAT-06 (deploy via existing CI/CD, valid HTTPS, additive/site-not-broken per D-04). Depends on 04 (compose topology + 5 *_SERVICE_URL) and 05 (db/migrate-phase1.sql.tmpl + provisioning).
Output: updated scripts/deploy.sh + .github/workflows/deploy.yml; documented manual cutover steps.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-n-n-t-ng-gateway/01-CONTEXT.md
@.planning/phases/01-n-n-t-ng-gateway/01-RESEARCH.md
@.planning/phases/01-n-n-t-ng-gateway/01-VALIDATION.md

<interfaces>
<!-- scripts/deploy.sh CURRENT flow (read it): abort if no .env; BEFORE_SHA=git rev-parse HEAD; git pull --ff-only; AFTER_SHA; detect web/ change via git diff BEFORE..AFTER; docker compose build --pull; docker compose up -d --remove-orphans; web restart if WEB_TOUCHED; wait up to 90s on http://127.0.0.1:8000/api/health; sync host nginx; done. NOTE: the git pull is moving OUT to the workflow (ISSUE-2). -->
<!-- .github/workflows/deploy.yml CURRENT: lint job globs `gateway/src gateway/public services/*/src services/*/public -name '*.php' | php -l` (auto-covers new services). deploy job: ssh-keyscan; SSH to VPS runs `cd ${VPS_DIR} && PROJECT_DIR=${VPS_DIR} ./scripts/deploy.sh`; then verify https://soa.duyet.vn/api/health (5 retries x10s into /tmp/health.json). The SSH command must be changed to `cd ${VPS_DIR} && git pull --ff-only && PROJECT_DIR=${VPS_DIR} ./scripts/deploy.sh` (ISSUE-2). -->
<!-- db/migrate-phase1.sql.tmpl (Plan 05): idempotent, password-placeholdered (${*_SVC_DB_PASS}); apply via `set -a; source .env; envsubst < db/migrate-phase1.sql.tmpl | docker compose exec -T mariadb mysql -uroot -p"$DB_ROOT_PASSWORD"`. -->
<!-- docker-compose.yml (Plan 04): mariadb service name = `mariadb`, env DB_ROOT_PASSWORD; mariadb healthcheck = `healthcheck.sh --connect --innodb_initialized`. 8 services total; the 5 PHP services depend_on mariadb service_healthy. -->
<!-- Required live .env vars after Phase 1: DB_ROOT_PASSWORD, PROFILE_SVC_DB_PASS, CONNECTION_SVC_DB_PASS, FEED_SVC_DB_PASS, SEARCH_SVC_DB_PASS, NOTIFICATION_SVC_DB_PASS, JWT_SECRET (unchanged), RATE_LIMIT_PER_MIN. The old USER/POST/COMMENT_SVC_DB_PASS are obsolete. -->
<!-- ISSUE-6: the 5 new *_SVC_DB_PASS MUST be shell-safe + Compose-safe: charset [A-Za-z0-9] only, length >=32, no $ ` " ' space # = { }. Generator: `tr -dc 'A-Za-z0-9' </dev/urandom | head -c32; echo`. This keeps `set -a; . ./.env; set +a` sourcing and Docker Compose `.env` interpolation unambiguous. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reorder scripts/deploy.sh — drop internal git pull, add env preflight + mariadb-first boot + BLOCKING backup + ordered migration + full-topology up</name>
  <read_first>
    - scripts/deploy.sh (CURRENT flow — you are REORDERING it; the git pull moves to the workflow, and migration must run between mariadb-healthy and full up)
    - db/migrate-phase1.sql.tmpl (Plan 05 — the script to apply; note ${VAR} placeholders need envsubst)
    - docker-compose.yml (Plan 04 — confirm mariadb service name + DB_ROOT_PASSWORD env + mariadb healthcheck command)
  </read_first>
  <files>scripts/deploy.sh</files>
  <action>
Rewrite scripts/deploy.sh keeping `set -euo pipefail`, the `.env`-missing abort, the host nginx sync block, and the gateway health wait — but REORDER the middle and REMOVE the internal `git pull` (the workflow now pulls before invoking this script — ISSUE-2). New flow, in this exact order:

1. `.env` MISSING ABORT (keep as-is): exit 1 if no .env.

2. ENV PREFLIGHT (after the .env abort, before any docker action): source env shell-safely and assert the required vars are non-empty, failing loudly if any is missing:
   `set -a; . ./.env; set +a`
   `for v in DB_ROOT_PASSWORD PROFILE_SVC_DB_PASS CONNECTION_SVC_DB_PASS FEED_SVC_DB_PASS SEARCH_SVC_DB_PASS NOTIFICATION_SVC_DB_PASS JWT_SECRET; do if [ -z "${!v:-}" ]; then echo "[deploy] FATAL: $v missing in .env" >&2; exit 3; fi; done`
   (Guards RESEARCH Pitfall 2. The values are constrained to [A-Za-z0-9]{32} by the human checkpoint (Task 3, ISSUE-6) so `set -a; . ./.env` sourcing is safe — no special-char breakage.)

3. WEB-CHANGE DETECTION (reworked, ISSUE-2): the workflow already pulled, so there is no in-script before/after SHA. Detect whether web/ changed in the just-pulled commit range using the reflog: `if git rev-parse 'HEAD@{1}' >/dev/null 2>&1 && git diff --name-only 'HEAD@{1}' HEAD | grep -qE '^web/'; then WEB_TOUCHED=1; else WEB_TOUCHED=0; fi`. (HEAD@{1} is the pre-pull tip from the workflow's `git pull`. If reflog is unavailable, default WEB_TOUCHED=1 to be safe.)

4. BUILD: `docker compose build --pull` (unchanged).

5. MARIADB-FIRST BOOT (ISSUE-3 step 1): bring up ONLY mariadb and wait for healthy BEFORE migrating:
   `docker compose up -d mariadb`
   `echo "[deploy] waiting for mariadb healthy (up to 90s)"`
   `for i in $(seq 1 18); do if docker compose exec -T mariadb healthcheck.sh --connect --innodb_initialized >/dev/null 2>&1; then echo "[deploy] mariadb healthy after ${i}x5s"; break; fi; sleep 5; if [ "$i" -eq 18 ]; then echo "[deploy] FATAL: mariadb not healthy after 90s" >&2; docker compose ps; exit 4; fi; done`

6. PRE-WIPE BACKUP — BLOCKING (ISSUE-4, ISSUE-3 step 2): only when old blog DBs still exist; NO `|| true`; abort on dump failure; verify the file exists AND is non-empty before the migration:
   ```
   if docker compose exec -T mariadb mysql -uroot -p"$DB_ROOT_PASSWORD" -e 'USE blog_users' >/dev/null 2>&1; then
     mkdir -p backups
     BACKUP_FILE="backups/pre-phase1-$(date +%Y%m%d-%H%M%S).sql"
     echo "[deploy] backing up blog_* to $BACKUP_FILE (blocking)"
     if ! docker compose exec -T mariadb mysqldump -uroot -p"$DB_ROOT_PASSWORD" --databases blog_users blog_posts blog_comments > "$BACKUP_FILE"; then
       echo "[deploy] FATAL: pre-wipe backup failed — aborting before destructive migration" >&2
       rm -f "$BACKUP_FILE"
       exit 5
     fi
     if [ ! -s "$BACKUP_FILE" ]; then
       echo "[deploy] FATAL: backup file $BACKUP_FILE is empty — aborting before destructive migration" >&2
       exit 5
     fi
     echo "[deploy] backup OK ($(wc -c < "$BACKUP_FILE") bytes)"
   else
     echo "[deploy] no legacy blog_users DB present — skipping backup (already migrated)"
   fi
   ```
   Append `backups/` to .gitignore if not already present (so dumps are never committed).

7. APPLY MIGRATION (ISSUE-3 step 3; idempotent; RESEARCH Pitfall 1 option B): substitute placeholders and pipe into mysql, aborting on failure:
   `echo "[deploy] applying db/migrate-phase1.sql.tmpl"`
   `if command -v envsubst >/dev/null 2>&1; then envsubst < db/migrate-phase1.sql.tmpl; else echo "[deploy] FATAL: envsubst not found (install gettext-base)" >&2; exit 6; fi | docker compose exec -T mariadb mysql -uroot -p"$DB_ROOT_PASSWORD"`
   (envsubst is present on Ubuntu 24.04 via gettext-base. The migration drops blog_*, creates proconnect_* + scoped users (ALTER USER makes the password update idempotent), builds the profile table, reseeds 5 demo accounts.) Echo `[deploy] phase-1 DB cutover applied`.

8. FULL-TOPOLOGY UP (ISSUE-3 step 4): NOW bring up the rest of the 8-container stack — the proconnect_* DBs/users exist, so the 5 PHP services can boot healthy:
   `docker compose up -d --remove-orphans` (--remove-orphans drops retired post/comment containers).
   Then web restart if `WEB_TOUCHED -eq 1` (keep existing `docker compose restart web`).

9. GATEWAY HEALTH WAIT (ISSUE-3 step 5; keep existing 90s loop on http://127.0.0.1:8000/api/health, exit 2 on failure).

10. HOST NGINX SYNC (keep the existing sync_nginx_file block + reload; unchanged — host nginx still points at 127.0.0.1:8000).

Final order summary: .env abort -> ENV PREFLIGHT -> web-change detect (reflog) -> build -> up mariadb only + wait healthy -> BLOCKING backup -> migrate -> full up --remove-orphans -> gateway health wait -> nginx sync -> done. NO `git pull` inside this script (workflow owns it).
  </action>
  <verify>
    <automated>bash -n scripts/deploy.sh && grep -q 'migrate-phase1.sql.tmpl' scripts/deploy.sh && grep -q 'mysqldump' scripts/deploy.sh && grep -q 'PROFILE_SVC_DB_PASS' scripts/deploy.sh && grep -q 'remove-orphans' scripts/deploy.sh && grep -q 'up -d mariadb' scripts/deploy.sh && grep -q 'test ! -s\|! -s ' scripts/deploy.sh && ! grep -q 'mysqldump.* || true' scripts/deploy.sh && ! grep -q 'git pull' scripts/deploy.sh && grep -q 'backups' .gitignore</automated>
  </verify>
  <acceptance_criteria>
    - `bash -n scripts/deploy.sh` passes
    - Internal `git pull` REMOVED (ISSUE-2): `! grep -q 'git pull' scripts/deploy.sh`
    - Env preflight loops over the 6 secrets + JWT_SECRET and `exit 3` on missing: `grep -q 'FATAL: .* missing in .env' scripts/deploy.sh` AND `grep -q 'PROFILE_SVC_DB_PASS' scripts/deploy.sh`
    - ORDERING (ISSUE-3) is mariadb-first then migrate then full up: in file order, the line index of `up -d mariadb` < line index of `migrate-phase1.sql.tmpl` < line index of `up -d --remove-orphans`. Verify: `awk '/up -d mariadb/{a=NR} /migrate-phase1.sql.tmpl/{b=NR} /up -d --remove-orphans/{c=NR} END{exit !(a<b && b<c)}' scripts/deploy.sh`
    - mariadb-healthy gate present before migration: `grep -q 'healthcheck.sh --connect --innodb_initialized' scripts/deploy.sh`
    - Pre-wipe backup is BLOCKING (ISSUE-4): `! grep -qE 'mysqldump.*\|\| true' scripts/deploy.sh` AND `grep -q 'FATAL: pre-wipe backup failed' scripts/deploy.sh` AND `grep -qE '! -s "\$BACKUP_FILE"|! -s \$BACKUP_FILE' scripts/deploy.sh` (non-empty check before migration)
    - Backup guarded by blog_users existence + writes to backups/: `grep -q 'blog_users' scripts/deploy.sh` AND `grep -q 'mkdir -p backups' scripts/deploy.sh`
    - `backups/` is git-ignored: `grep -q 'backups' .gitignore`
    - Migration applied via mysql + envsubst substitution: `grep -q 'migrate-phase1.sql.tmpl' scripts/deploy.sh` AND `grep -q 'envsubst' scripts/deploy.sh`
    - `--remove-orphans` still present (retires post/comment containers) and runs AFTER migration
    - Existing gateway health wait on 127.0.0.1:8000/api/health retained
  </acceptance_criteria>
  <done>deploy.sh no longer pulls (workflow owns it); preflights env; brings up mariadb-only and waits healthy; takes a BLOCKING, non-empty-verified pre-wipe backup; applies the idempotent migration; THEN brings up the full 8-container topology with --remove-orphans; then waits on gateway health — all in the corrected ISSUE-3 order.</done>
</task>

<task type="auto">
  <name>Task 2: Workflow git pull --ff-only before deploy.sh (cutover bootstrap) + strengthen health verify</name>
  <read_first>
    - .github/workflows/deploy.yml (SSH deploy step + lint glob + public health verify)
  </read_first>
  <files>.github/workflows/deploy.yml</files>
  <action>
1. CUTOVER BOOTSTRAP (ISSUE-2): change the SSH deploy command so the repo is pulled BEFORE deploy.sh runs, so the NEW deploy.sh executes on the first cutover. Current: `cd ${VPS_DIR} && PROJECT_DIR=${VPS_DIR} ./scripts/deploy.sh`. Change to: `cd ${VPS_DIR} && git pull --ff-only && PROJECT_DIR=${VPS_DIR} ./scripts/deploy.sh`. (deploy.sh no longer pulls — Task 1.) Keep the `ssh -i ~/.ssh/id_deploy -o StrictHostKeyChecking=yes "${VPS_USER}@${VPS_HOST}"` wrapper and all secrets/env wiring UNCHANGED.
2. The lint glob `services/*/src services/*/public` already auto-covers the renamed profile-service and the 4 new stubs — confirm no edit needed there.
3. STRENGTHEN PUBLIC HEALTH VERIFY (aligns D-04/D-10): the current step does `curl -sf https://soa.duyet.vn/api/health -o /tmp/health.json` then `exit 0` on any 200. Strengthen it to also require all-5-services-ok: after the successful curl, `grep -q '"status":"ok"' /tmp/health.json` before `exit 0` (so a degraded gateway, any service down, fails CI). Keep the 5x retry-with-10s-sleep loop and the final `exit 1`.
  </action>
  <verify>
    <automated>grep -q 'soa.duyet.vn/api/health' .github/workflows/deploy.yml && grep -q 'pull --ff-only' .github/workflows/deploy.yml && grep -q 'scripts/deploy.sh' .github/workflows/deploy.yml && grep -q "services/\*/src" .github/workflows/deploy.yml && python3 -c "import yaml; yaml.safe_load(open('.github/workflows/deploy.yml')); print('YAML_OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `.github/workflows/deploy.yml` is valid YAML (parses without error)
    - Cutover bootstrap present (ISSUE-2): the SSH deploy command runs `git pull --ff-only` BEFORE `./scripts/deploy.sh` on the same line/command: `grep -q 'git pull --ff-only && PROJECT_DIR' .github/workflows/deploy.yml` (or equivalent ordering with pull preceding deploy.sh)
    - Lint glob still covers `services/*/src` (auto-covers profile + 4 stubs)
    - Public health verify hits `https://soa.duyet.vn/api/health` AND asserts all-ok: `grep -q '"status":"ok"' .github/workflows/deploy.yml`
    - SSH key/secrets wiring (id_deploy, StrictHostKeyChecking, VPS_HOST/USER/DIR) unchanged
  </acceptance_criteria>
  <done>The workflow `git pull --ff-only` runs in the VPS repo dir before invoking deploy.sh (so the new deploy.sh runs on first cutover), lint auto-covers new services, and the public health verify requires all 5 services ok.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: Live VPS .env edit (shell-safe secrets) + production cutover (HUMAN-ONLY)</name>
  <files>(live VPS) .env — git-ignored, hand-edited on the VPS only</files>
  <action>
HUMAN-ONLY (no CLI/API substitute): SSH to the VPS, add the 5 new *_SVC_DB_PASS secrets to the git-ignored .env using a SHELL-SAFE + COMPOSE-SAFE charset (ISSUE-6), remove the obsolete USER/POST/COMMENT_SVC_DB_PASS, keep DB_ROOT_PASSWORD + JWT_SECRET unchanged, then run the local smoke + Codex review, push main, and verify the production cutover. Exact steps and verification in <how-to-verify>. This task gates the production deploy; the destructive DB wipe is backed up by deploy.sh first (blocking backup).
  </action>
  <what-built>
deploy.sh now preflights env vars, brings up mariadb first, takes a BLOCKING pre-wipe backup, applies db/migrate-phase1.sql.tmpl, then brings up the full 8-container stack; the workflow `git pull --ff-only` before deploy.sh so the new logic runs on first cutover; CI verifies all-5-ok public health. But the live VPS .env (git-ignored) does NOT yet contain the 5 new *_SVC_DB_PASS secrets — only a human can add them on the VPS before deploy, and the production DB wipe is destructive.
  </what-built>
  <how-to-verify>
PRE-DEPLOY (do BEFORE pushing main, or the deploy preflight will fail loudly with exit 3):
1. SSH to the VPS (alias `vps-root`, host 14.225.29.159) and edit the project .env. Generate 5 SHELL-SAFE + COMPOSE-SAFE passwords — charset [A-Za-z0-9] ONLY, length 32, NO `$` backtick `"` `'` space `#` `=` `{` `}` (ISSUE-6: these break `set -a; . ./.env` sourcing and Docker Compose `.env` interpolation). Generate each with:
   `tr -dc 'A-Za-z0-9' </dev/urandom | head -c32; echo`
   Set (each a distinct generated value):
   PROFILE_SVC_DB_PASS=<32 alnum>   CONNECTION_SVC_DB_PASS=<32 alnum>   FEED_SVC_DB_PASS=<32 alnum>   SEARCH_SVC_DB_PASS=<32 alnum>   NOTIFICATION_SVC_DB_PASS=<32 alnum>
   Write them WITHOUT quotes (alnum-only values need none, and Compose `.env` does not strip quotes the way a shell does — keeping them unquoted avoids ambiguity).
   Keep DB_ROOT_PASSWORD and JWT_SECRET (>=16 chars) UNCHANGED — changing JWT_SECRET would invalidate existing tokens (PROF-01). If the existing DB_ROOT_PASSWORD contains shell-special chars, leave it as-is (it was already working) but be aware deploy.sh sources it; if sourcing ever errors, regenerate it to the same alnum charset and update the running mariadb root password accordingly. Remove the obsolete USER_SVC_DB_PASS/POST_SVC_DB_PASS/COMMENT_SVC_DB_PASS lines.
2. Confirm on the VPS: `grep -c '_SVC_DB_PASS' .env` shows the 5 new vars, and each value matches `^[A-Za-z0-9]{32}$`: `grep -E '_SVC_DB_PASS=' .env | sed 's/.*=//' | grep -vqE '^[A-Za-z0-9]{32}$' && echo BAD_CHARSET || echo CHARSET_OK` should print CHARSET_OK.
3. Sanity: `set -a; . ./.env; set +a; echo OK` sources without error (proves no special-char breakage).

DEPLOY:
4. Locally (mandatory per CLAUDE.md before any commit): `docker compose up -d --wait && bash scripts/smoke-phase1.sh` must be ALL PASS. Then run /codex-impl-review and only commit/push after Codex APPROVES.
5. Push main -> GitHub Actions lint -> the workflow `git pull --ff-only` on the VPS -> the NEW deploy.sh runs: preflight env (passes now) -> mariadb-only up + healthy -> blocking backup to backups/pre-phase1-*.sql -> apply migrate-phase1.sql.tmpl (drops blog_*, creates proconnect_*, reseeds 5 accounts) -> full up --remove-orphans (8 containers) -> health verify.

POST-DEPLOY VERIFY:
6. `curl -sf https://soa.duyet.vn/api/health` returns 200 with `"status":"ok"` and all 5 service keys (profile/connection/feed/search/notification).
7. Login works: `curl -sf -XPOST https://soa.duyet.vn/api/auth/login -H 'Content-Type: application/json' -d '{"login":"duyet","password":"demo@123**"}'` returns a token.
8. `curl -sf https://soa.duyet.vn/api/profiles/2` returns the duyet profile (username, no email).

ROLLBACK (if needed): restore the dump — `docker compose exec -T mariadb mysql -uroot -p"$DB_ROOT_PASSWORD" < backups/pre-phase1-<ts>.sql` and redeploy the previous commit.
  </how-to-verify>
  <verify>
    <automated>curl -sf https://soa.duyet.vn/api/health | grep -q '"status":"ok"'</automated>
  </verify>
  <done>Live .env carries the 5 shell-safe *_SVC_DB_PASS (32 alnum each); production deploy ran the NEW deploy.sh on cutover in the correct mariadb-first/backup/migrate/full-up order; https://soa.duyet.vn/api/health is 200 with all 5 services ok; login + /api/profiles/2 work.</done>
  <resume-signal>Type "deployed" once https://soa.duyet.vn/api/health is 200 with all 5 services ok and login works; or describe the failure (e.g. which service is down, env var missing, charset rejected).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub Actions -> VPS (SSH) | deploy automation; existing trusted key |
| operator -> live mariadb | destructive production DB wipe |
| git repo -> live secrets | .env is git-ignored; secrets never committed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1-17 | Denial / data loss | production DB wipe with no rollback | mitigate (HIGH) | deploy.sh takes a BLOCKING pre-wipe mysqldump to backups/ (abort on failure or empty file) before applying the migration; rollback documented in Task 3. Data is throwaway demo data (D-06). Wipe is contained to ProConnect's own mariadb_data volume (RESEARCH decisive finding) — cannot affect other VPS sites. |
| T-1-18 | Info disclosure | DB passwords or dumps committed to git | mitigate (HIGH) | migrate-phase1.sql.tmpl uses placeholders (Plan 05); deploy.sh substitutes from git-ignored .env at runtime; backups/ added to .gitignore. Live .env edited by hand, never committed. |
| T-1-19 | Denial | deploy proceeds with missing env -> services boot with empty DB passwords | mitigate | Env preflight (Task 1) hard-fails (exit 3) if any required *_SVC_DB_PASS is missing before build/migrate. |
| T-1-20 | Spoofing | direct-to-origin bypass of Cloudflare during deploy | accept | Host nginx $cf_allowed gate unchanged (RESEARCH Runtime State — no host nginx change); gateway stays on 127.0.0.1:8000 loopback only. |
| T-1-21 | Tampering | JWT_SECRET accidentally changed -> all sessions invalidated | mitigate | Task 3 explicitly instructs keeping JWT_SECRET unchanged (PROF-01). |
| T-1-22 | Denial / correctness | first cutover runs OLD deploy.sh (no preflight/backup/migrate) | mitigate (ISSUE-2) | Workflow `git pull --ff-only` before deploy.sh; deploy.sh no longer self-pulls; acceptance asserts pull precedes deploy.sh invocation. |
| T-1-23 | Denial | DB-dependent services boot before proconnect_* DBs/users exist -> health fail on cutover | mitigate (ISSUE-3) | deploy.sh order: mariadb-only up + healthy -> backup -> migrate -> full up --remove-orphans; acceptance asserts line-order a<b<c. |
| T-1-24 | Denial / correctness | silent backup failure lets destructive migration run with no recoverable dump | mitigate (ISSUE-4) | mysqldump is blocking (no `|| true`); abort on non-zero exit AND on empty file (`! -s`); acceptance asserts both guards. |
| T-1-25 | Tampering | special chars in DB passwords break `.env` sourcing / Compose interpolation -> wrong/empty credentials | mitigate (ISSUE-6) | 5 new *_SVC_DB_PASS constrained to [A-Za-z0-9]{32}; generator documented; Task 3 verifies charset + sourcing; preflight uses `set -a; . ./.env; set +a`. |
</threat_model>

<verification>
`bash -n scripts/deploy.sh` + `awk` line-order check (mariadb-up < migrate < full-up) + YAML parse of deploy.yml + `grep 'pull --ff-only'` locally. Production gate: after the human cutover, `https://soa.duyet.vn/api/health` is 200 with all 5 services ok, login + /api/profiles/2 succeed. The pre-push gate is `bash scripts/smoke-phase1.sh` green locally + Codex impl-review approval (CLAUDE.md mandatory workflow).
</verification>

<success_criteria>
Push main pulls the repo on the VPS, runs the NEW deploy.sh which (in order) boots mariadb, takes a blocking backup, applies the idempotent migration, then brings up the 8-container stack to soa.duyet.vn; public HTTPS /api/health is green with all 5 services; register/login still work; secrets are shell-safe and never committed; rollback path documented.
</success_criteria>

<output>
After completion, create `.planning/phases/01-n-n-t-ng-gateway/01-06-SUMMARY.md`
</output>
</content>
