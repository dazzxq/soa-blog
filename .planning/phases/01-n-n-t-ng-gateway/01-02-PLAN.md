---
phase: 01-n-n-t-ng-gateway
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - services/profile-service/**
  - gateway/src/Services/ProfileClient.php
autonomous: true
requirements: [PLAT-01, PROF-01]
must_haves:
  truths:
    - "services/profile-service exists (git-mv'd from user-service) with full auth/user logic intact"
    - "services/user-service no longer exists"
    - "A ProfileClient typed client exists in the gateway reading PROFILE_SERVICE_URL, with health/healthAsync/get/batch/create/verifyCredentials/update mirroring the old UserClient"
  artifacts:
    - path: "services/profile-service/src/Db.php"
      provides: "profile-service PDO singleton (defaults updated to proconnect_profile/profile_svc)"
      contains: "proconnect_profile"
    - path: "gateway/src/Services/ProfileClient.php"
      provides: "typed gateway client for profile-service"
      contains: "PROFILE_SERVICE_URL"
  key_links:
    - from: "gateway/src/Services/ProfileClient.php"
      to: "profile-service"
      via: "getenv PROFILE_SERVICE_URL"
      pattern: "PROFILE_SERVICE_URL"
---

<objective>
Rename `user-service` → `profile-service` in place via `git mv` (D-05), preserving all auth/user logic and git history. Create the gateway-side `ProfileClient` (a verbatim clone of `UserClient`) reading `PROFILE_SERVICE_URL`. This plan owns ONLY the profile-service directory and the new ProfileClient file — it does NOT touch docker-compose.yml, routes.php, or public/index.php (those converge in Plan 04, Wave 2).

Purpose: PLAT-01 (profile microservice behind the gateway) + PROF-01 (auth/register/login must keep working through the renamed service). D-05 locks `git mv` in place.
Output: `services/profile-service/` (full tree), `gateway/src/Services/ProfileClient.php`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-n-n-t-ng-gateway/01-CONTEXT.md
@.planning/phases/01-n-n-t-ng-gateway/01-RESEARCH.md

<interfaces>
<!-- Old client being cloned (gateway/src/Services/UserClient.php) — ProfileClient is a verbatim copy with only the env var and class name changed -->
class UserClient {
  __construct() { $base = getenv('USER_SERVICE_URL') ?: 'http://user-service:80'; $this->http = HttpClient::create($base); }
  health(): ResponseInterface; healthAsync(): PromiseInterface;
  get(int $id): ResponseInterface; getAsync(int $id): PromiseInterface;
  batch(array $ids): ResponseInterface; batchAsync(array $ids): PromiseInterface;
  create(array $body): ResponseInterface;
  verifyCredentials(string $login, string $password): ResponseInterface;
  update(int $id, array $body, int $callerId): ResponseInterface;
}
<!-- Db.php defaults (services/user-service/src/Db.php:21-24) — only the fallback defaults change; real values come from env in compose -->
$name = getenv('DB_NAME') ?: 'blog_users';   // -> default 'proconnect_profile'
$user = getenv('DB_USER') ?: 'user_svc';     // -> default 'profile_svc'
<!-- AuthController (gateway) calls $this->users->create / verifyCredentials / get — the ProfileClient MUST keep these exact method signatures so AuthController rewiring in Plan 04 is a one-line type swap -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: git mv user-service → profile-service and update internal identifiers</name>
  <read_first>
    - services/user-service/src/Db.php (default DB_NAME/DB_USER fallbacks to update)
    - services/user-service/composer.json (the "name" field to update)
    - services/user-service/src/Controllers/UserController.php (confirm no hardcoded "user-service"/"blog_users" strings beyond Db defaults; logic stays unchanged)
  </read_first>
  <files>services/profile-service/**</files>
  <action>
1. Rename the directory preserving history: `git mv services/user-service services/profile-service`.
2. In `services/profile-service/composer.json` change the `"name"` field from `"soa-blog/user-service"` to `"soa-blog/profile-service"` and the `"description"` to `"Profile microservice for ProConnect"`. Leave `require`/`autoload`/`config` UNTOUCHED (deps identical — do NOT run composer update; the committed `composer.lock` stays verbatim). vendor/ is git-ignored and rebuilt inside the image at `docker compose build` time — there is no vendor/ to preserve in git.
3. In `services/profile-service/src/Db.php` update ONLY the two fallback defaults: `getenv('DB_NAME') ?: 'blog_users'` → `?: 'proconnect_profile'`, and `getenv('DB_USER') ?: 'user_svc'` → `?: 'profile_svc'`. These are fallbacks only; the real values are injected by compose env in Plan 04. Do not change the DSN structure or PDO options.
4. Do NOT rename the internal `/users` routes inside profile-service (RESEARCH anti-pattern: unnecessary churn; D-05 keeps logic). `src/routes.php`, `UserController` class name, and all auth/user logic stay exactly as-is. The gateway-facing surface change to `/api/profiles/{id}` happens in Plan 04.
5. Do NOT touch docker-compose.yml or any gateway file in this task (Plan 04 owns the compose rename + DI rewire).
6. Confirm `services/user-service` no longer exists after the move.
  </action>
  <verify>
    <automated>test -d services/profile-service && test ! -d services/user-service && grep -q 'soa-blog/profile-service' services/profile-service/composer.json && grep -q 'proconnect_profile' services/profile-service/src/Db.php && php -l services/profile-service/public/index.php && php -l services/profile-service/src/Db.php && php -l services/profile-service/src/Controllers/UserController.php</automated>
  </verify>
  <acceptance_criteria>
    - `test -d services/profile-service` succeeds AND `test ! -d services/user-service` succeeds
    - `git log --follow services/profile-service/src/Db.php | head -1` shows commit history preserved (git mv, not delete+add)
    - `grep -q 'soa-blog/profile-service' services/profile-service/composer.json`
    - `grep -q "?: 'proconnect_profile'" services/profile-service/src/Db.php` AND `grep -q "?: 'profile_svc'" services/profile-service/src/Db.php`
    - `grep -q 'blog_users' services/profile-service/src/Db.php` returns NOTHING (old default removed)
    - profile-service/src/routes.php STILL contains `/users` routes (internal surface unchanged): `grep -q "'/users'" services/profile-service/src/routes.php`
    - `php -l` passes on public/index.php, src/Db.php, src/Controllers/UserController.php
  </acceptance_criteria>
  <done>services/profile-service exists with history, user-service is gone, composer name + Db.php defaults updated to proconnect_profile/profile_svc, internal /users routes and auth logic untouched, all PHP lints clean.</done>
</task>

<task type="auto">
  <name>Task 2: Create gateway ProfileClient (clone of UserClient)</name>
  <read_first>
    - gateway/src/Services/UserClient.php (the verbatim source to clone — class name + env var are the ONLY changes)
    - gateway/src/Services/HttpClient.php (the factory ProfileClient calls — confirm HttpClient::create signature unchanged)
  </read_first>
  <files>gateway/src/Services/ProfileClient.php</files>
  <action>
Create `gateway/src/Services/ProfileClient.php` as a byte-for-byte clone of `gateway/src/Services/UserClient.php` with exactly two changes:
1. `final class UserClient` → `final class ProfileClient`.
2. Constructor base URL: `$base = getenv('USER_SERVICE_URL') ?: 'http://user-service:80';` → `$base = getenv('PROFILE_SERVICE_URL') ?: 'http://profile-service:80';`.
Keep ALL methods with identical signatures and bodies: `health()`, `healthAsync()`, `get(int $id)`, `getAsync(int $id)`, `batch(array $ids)`, `batchAsync(array $ids)`, `create(array $body)`, `verifyCredentials(string $login, string $password)`, `update(int $id, array $body, int $callerId)`. The internal URLs (`/users`, `/users/{id}`, `/users/verify-credentials`) stay as `/users` — the internal profile-service route surface is unchanged (D-05). Namespace stays `App\Services`.
Do NOT delete gateway/src/Services/UserClient.php in this plan — Plan 04 (gateway re-wire) removes it together with PostClient/CommentClient to keep DI removal atomic.
  </action>
  <verify>
    <automated>php -l gateway/src/Services/ProfileClient.php && grep -q 'final class ProfileClient' gateway/src/Services/ProfileClient.php && grep -q 'PROFILE_SERVICE_URL' gateway/src/Services/ProfileClient.php && grep -q "http://profile-service:80" gateway/src/Services/ProfileClient.php && grep -q 'verifyCredentials' gateway/src/Services/ProfileClient.php</automated>
  </verify>
  <acceptance_criteria>
    - `php -l gateway/src/Services/ProfileClient.php` returns "No syntax errors"
    - `grep -q 'final class ProfileClient' gateway/src/Services/ProfileClient.php`
    - `grep -q "getenv('PROFILE_SERVICE_URL')" gateway/src/Services/ProfileClient.php` AND `grep -q "http://profile-service:80" gateway/src/Services/ProfileClient.php`
    - All 6 method groups present: `grep -c 'public function' gateway/src/Services/ProfileClient.php` >= 8 (health, healthAsync, get, getAsync, batch, batchAsync, create, verifyCredentials, update)
    - `grep -q "'/users'" gateway/src/Services/ProfileClient.php` (internal path unchanged) AND `grep -q "verify-credentials" gateway/src/Services/ProfileClient.php`
    - `grep -q 'namespace App\\\\Services' gateway/src/Services/ProfileClient.php`
  </acceptance_criteria>
  <done>gateway/src/Services/ProfileClient.php is a working clone of UserClient with class name ProfileClient and env PROFILE_SERVICE_URL, all method signatures preserved, lint clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| gateway → profile-service | gateway forwards trusted X-User-Id on mutating calls; profile-service trusts it (unchanged from user-service) |
| profile-service → its DB | scoped profile_svc user, prepared statements |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1-01 | Elevation of privilege | profile-service still must NOT verify JWT or know JWT_SECRET | mitigate | Rename keeps profile-service dependency-minimal (no firebase/php-jwt in composer.json — verified). Task 1 forbids adding JWT logic; auth stays at gateway AuthController. |
| T-1-02 | Spoofing | profile-service must remain internal-only (no host port) | mitigate | This plan does NOT touch docker-compose.yml; the profile compose block in Plan 04 inherits user-service's no-`ports:` config. Flagged for Plan 04. |
| T-1-03 | Tampering | SQL injection via preserved UserController queries | accept | Unchanged code uses real prepared statements (EMULATE_PREPARES=false, Db.php:31); no new query code added. |
</threat_model>

<verification>
`php -l` on all touched PHP; `test -d services/profile-service && test ! -d services/user-service`. Functional verification (login still works) happens after Plan 04 wires the gateway DI and Plan 05 provisions the DB, then `bash scripts/smoke-phase1.sh`.
</verification>

<success_criteria>
profile-service exists with full logic and history; ProfileClient exists and points at PROFILE_SERVICE_URL; no compose/routes/DI files touched (clean handoff to Plan 04).
</success_criteria>

<output>
After completion, create `.planning/phases/01-n-n-t-ng-gateway/01-02-SUMMARY.md`
</output>
