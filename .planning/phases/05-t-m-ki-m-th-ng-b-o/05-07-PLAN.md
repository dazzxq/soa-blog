---
phase: 05-t-m-ki-m-th-ng-b-o
plan: 07
type: execute
wave: 5
depends_on: [05-01, 05-02, 05-03, 05-04, 05-05, 05-06]
files_modified: []
autonomous: false
requirements: [SEARCH-01, SEARCH-02, NOTIF-01, NOTIF-02, NOTIF-03]
must_haves:
  truths:
    - "Codex has reviewed and approved the Phase-5 implementation before any commit"
    - "main is pushed, CI/CD deploys to soa.duyet.vn, the phase-5 migration runs on the live volume, and the site stays up"
    - "smoke-phase5.sh and the Phase 1-4 regression smokes all pass against the live stack"
  artifacts:
    - path: ".planning/phases/05-t-m-ki-m-th-ng-b-o/05-07-SUMMARY.md"
      provides: "Cutover record: deploy result + full smoke output"
  key_links:
    - from: "GitHub Actions deploy"
      to: "scripts/deploy.sh on VPS"
      via: "git pull --ff-only → deploy.sh → 05-migrate-phase5.sql → docker compose up"
      pattern: "deploy"
---

<objective>
Live cutover for Phase 5. Run the MANDATORY `/codex-impl-review` gate, commit only after Codex APPROVES, push `main`, let GitHub Actions deploy to `soa.duyet.vn` (running `scripts/deploy.sh` → the blocking `05-migrate-phase5.sql` step → full-topology up), then verify the live stack with `smoke-phase5.sh` plus the Phase 1-4 regression smokes. The site must stay up (additive deploy). This plan is orchestrator-run and pauses at the review + verify gates.

Purpose: Phase 5 code is only "done" when it is live on the VPS, the live-volume migration has run, and every requirement smoke passes — Docker is not available locally (runtime verification is on the VPS per CLAUDE.md).
Output: a deployed, smoke-green Phase 5 + 05-07-SUMMARY.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/05-t-m-ki-m-th-ng-b-o/05-VALIDATION.md
@scripts/deploy.sh
@scripts/smoke-phase5.sh

<interfaces>
<!-- Deploy flow (CLAUDE.md + MEMORY): local git → /codex-impl-review APPROVE → push public GitHub main → GitHub Actions (.github/workflows/deploy.yml: php -l lint job, then SSH deploy job runs scripts/deploy.sh) → VPS git pull --ff-only → deploy.sh → /api/health verify. -->
<!-- deploy.sh runs the blocking 05-migrate-phase5.sql step (Plan 01) BEFORE full-topology up. -->
<!-- Smoke: bash scripts/smoke-phase5.sh + regression bash scripts/smoke-phase{1,2,3,4}.sh against GW=https://soa.duyet.vn (or http://127.0.0.1:8000 on the VPS host). -->
<!-- MANDATORY (CLAUDE.md): /codex-impl-review must APPROVE before commit; fix valid issues and re-review. -->
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <what-built>All Phase-5 code (migration, both services, gateway compose + best-effort hooks, UI) is complete and php -l clean across Plans 01-06.</what-built>
  <how-to-verify>
Run the MANDATORY Codex implementation review before any commit:
1. Invoke `/codex-impl-review` over the Phase-5 diff (db/05-migrate-phase5.sql, deploy.sh, smoke-phase5.sh, both services' new controllers + routes, gateway SearchController/NotificationsController + extended clients + modified ConnectionsController/FeedController + routes/DI, web/search.html + bell).
2. If Codex finds valid issues, fix them and re-run `/codex-impl-review` until it APPROVES.
3. Confirm APPROVED before proceeding to commit.
  </how-to-verify>
  <resume-signal>Type "codex approved" once /codex-impl-review returns APPROVE (or paste the approval).</resume-signal>
</task>

<task type="auto">
  <name>Task 1: Commit + push main (triggers CI/CD deploy)</name>
  <read_first>git status / the Phase-5 changed files. .github/workflows/deploy.yml for the deploy trigger (push to main).</read_first>
  <files></files>
  <action>
After Codex APPROVE: stage the Phase-5 changes and commit with a Vietnamese-friendly conventional message, e.g. `feat(phase5): tìm kiếm người dùng (compose) + thông báo polling (gateway-coordinated)`. Include the Co-Authored-By trailer. Push to `main` (the public GitHub remote) — this triggers GitHub Actions: the `php -l` lint job then the SSH deploy job (`scripts/deploy.sh` on the VPS: `git pull --ff-only` → migrations incl. the blocking `05-migrate-phase5.sql` → `docker compose build` → full-topology up → `/api/health`).
Do NOT push if on a non-main branch without the user's go-ahead; this is the live production deploy.
  </action>
  <verify>
    <automated>git log --oneline -1 | grep -qi "phase5\|phase 5\|tìm kiếm\|thông báo" && echo OK</automated>
  </verify>
  <done>Phase-5 commit on main, pushed; GitHub Actions deploy triggered.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>CI/CD deploy to soa.duyet.vn: lint job, then deploy.sh ran the blocking phase-5 migration (creating search_index + notifications on the live volume) and brought up the full topology.</what-built>
  <how-to-verify>
1. Watch the GitHub Actions run for the push → both the lint job and the deploy job succeed (green).
2. Confirm `https://soa.duyet.vn/api/health` is 200 and reports all services (incl. search + notification) healthy.
3. Confirm the deploy log shows `[deploy] phase-5 additive migration applied` (the migration ran, no error).
4. Confirm the site (feed/connections/profile) is still up — additive deploy did not break Phases 1-4.
  </how-to-verify>
  <resume-signal>Type "deployed" once Actions is green, /api/health is healthy, and the migration log line is present.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Phase-5 endpoints live behind the gateway: /api/search, /api/search/reindex, /api/notifications + mark-read; best-effort notify on invite/react/comment.</what-built>
  <how-to-verify>
Run the smoke suite against the live stack (on the VPS host where Docker is available, or with `GW=https://soa.duyet.vn`):
1. `bash scripts/smoke-phase5.sh` → all PASS (reindex; search by name + skill with connection_status + no PII; invite→notify; react/comment→post-author notify; best-effort 2xx; unread_count; mark-one decrements; mark-all → 0; no email leak).
2. Regression: `bash scripts/smoke-phase1.sh && bash scripts/smoke-phase2.sh && bash scripts/smoke-phase3.sh && bash scripts/smoke-phase4.sh` → all PASS (Phases 1-4 not broken).
3. Confirm total smoke latency is reasonable (<60s for phase5) and the smoke left the demo seed intact (search_index rows + demo notifications still present; read state changes are acceptable).
  </how-to-verify>
  <resume-signal>Type "smoke green" (or paste any FAIL lines to fix via a gap-closure plan).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| local repo → public GitHub → VPS | The deploy path crosses to production; the migration runs as root on the live volume. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-24 | Tampering | live-volume migration during deploy | mitigate | The migration is non-destructive + idempotent (Plan 01, verified); deploy.sh runs it blocking (no `|| true`) so a failure halts the deploy before services boot — verified pre-deploy by Codex review. |
| T-05-25 | Denial of Service | additive deploy breaking the live site | mitigate | Additive only (new tables, new code, no rename/drop); Phase 1-4 regression smokes are a release gate; /api/health checked before sign-off. |
| T-05-26 | Repudiation | shipping unreviewed code | mitigate | MANDATORY /codex-impl-review APPROVE gate before commit (CLAUDE.md); no commit without it. |
</threat_model>

<verification>
- /codex-impl-review APPROVE before commit (mandatory gate).
- GitHub Actions green + /api/health healthy + migration log line present.
- smoke-phase5.sh + smoke-phase1..4.sh all PASS against the live stack.
</verification>

<success_criteria>
- Codex approved; main pushed; CI/CD deployed to soa.duyet.vn with the phase-5 migration applied; site up.
- smoke-phase5 + Phase 1-4 regression all green; demo seed intact.
- 05-07-SUMMARY.md records the deploy + smoke outcome.
</success_criteria>

<output>
After completion, create `.planning/phases/05-t-m-ki-m-th-ng-b-o/05-07-SUMMARY.md`.
</output>
