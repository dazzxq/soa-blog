---
phase: 05-t-m-ki-m-th-ng-b-o
plan: 06
type: execute
wave: 4
depends_on: [05-04, 05-05]
files_modified:
  - web/search.html
  - web/feed.html
  - web/connections.html
  - web/index.html
autonomous: false
requirements: [SEARCH-01, SEARCH-02, NOTIF-02, NOTIF-03]
must_haves:
  truths:
    - "A logged-in user types a query, lands on a results page, and sees user cards with a quick-connect button + relationship status"
    - "A notification bell in the navbar shows an unread badge that updates by polling ~15s and lets the user mark read / read-all"
  artifacts:
    - path: "web/search.html"
      provides: "Search box + results list + quick-connect buttons (Vietnamese, x-text)"
      contains: "connection_status"
    - path: "web/feed.html"
      provides: "Navbar with search box + notification bell partial"
      contains: "notificationBell"
  key_links:
    - from: "web/search.html"
      to: "GET /api/search?q="
      via: "api.get in an Alpine component"
      pattern: "api.get\\('/search"
    - from: "notification bell"
      to: "GET /api/notifications + POST .../read"
      via: "setInterval ~15s poll + api.post mark-read"
      pattern: "setInterval"
---

<objective>
Ship the D-10 UI: a `search.html` page (query input → results cards with quick-connect button + relationship status) and a notification bell partial (unread badge polling ~15s, dropdown with mark-read + mark-all-read) added to the navbar of the existing pages. Minimal, Vietnamese, `x-text` only (XSS-safe). Reuse `window.api`/`auth`/`formatDate` and the connection quick-connect action. This plan ends with a human-verify checkpoint (browser UX cannot be smoke-asserted).

Purpose: SEARCH-01/02 + NOTIF-02/03 need a user-facing surface; the bell makes notifications visible near-real-time via polling.
Output: web/search.html + bell partial wired into feed.html/connections.html/index.html navbars.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/05-t-m-ki-m-th-ng-b-o/05-CONTEXT.md
@.planning/phases/05-t-m-ki-m-th-ng-b-o/05-RESEARCH.md
@web/feed.html
@web/connections.html
@web/assets/app.js

<interfaces>
<!-- app.js: window.api.get(p)/post(p,body) → {ok,status,data,meta}; api.post(p) with no body sends empty body (fine for mark-read). auth.isLoggedIn()/token()/user(). formatDate(ts). navbar() Alpine factory (declared as <nav x-data="navbar()">). -->
<!-- GET /api/search?q=&limit= → {data:[{id,username,display_name,headline,avatar_url,connection_status}], meta:{q,degraded?}}. connection_status ∈ none|pending_outgoing|pending_incoming|connected|unknown. -->
<!-- Quick-connect action (reuse connections.html): POST /api/connections/requests {target_id}. -->
<!-- GET /api/notifications → {data:[{id,type,actor_id,ref_id,created_at,read_at,actor:{id,username,display_name,avatar_url}|null}], meta:{unread_count}}. POST /api/notifications/{id}/read; POST /api/notifications/read-all. -->
<!-- Existing navbar block (feed.html lines 15-35): <nav x-data="navbar()"> with a logged-in <template x-if="isLoggedIn"> span containing the nav links. The bell + search box go inside that logged-in span. -->
<!-- Pages load app.js then Alpine CDN; Tailwind Play CDN. All pages Vietnamese. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: web/search.html — search box + results cards + quick-connect</name>
  <read_first>web/connections.html (Alpine x-data page structure, the connect action POST /api/connections/requests, x-text discipline, card markup, Tailwind classes). web/feed.html nav block to copy the navbar. web/assets/app.js api/auth.</read_first>
  <files>web/search.html</files>
  <action>
Create `web/search.html` cloning the connections.html page skeleton (same `<head>` CDN includes, `<nav x-data="navbar()">` block copied from feed.html, Vietnamese). Main is an Alpine component `searchPage()`:
- A search input bound to `q` (Vietnamese placeholder e.g. "Tìm người theo tên, chức danh, kỹ năng…") with a submit/Enter handler calling `run()`. Read an initial `?q=` from the URL (`new URLSearchParams(location.search).get('q')`) and run on load via `x-init`.
- `run()`: if `q.trim()===''` clear results; else `const r = await api.get('/search?q=' + encodeURIComponent(this.q))`; `this.results = r.data || []`; handle `r.meta?.degraded` with a subtle Vietnamese notice ("Một số trạng thái quan hệ tạm thời không tải được").
- Render results with `<template x-for="u in results" :key="u.id">`: avatar (`u.avatar_url` with a fallback), `x-text="u.display_name"`, `x-text="u.headline"`, and a status-aware quick-connect button:
  - status `none` → "Kết nối" button calling `connect(u)`.
  - status `pending_outgoing` → disabled "Đã gửi lời mời".
  - status `pending_incoming` → "Chờ bạn phản hồi" (link to connections.html).
  - status `connected` → "Đã kết nối" (disabled/checkmark).
  - status `unknown` → show "Kết nối" but with a subtle note.
- `connect(u)`: `await api.post('/connections/requests', { target_id: u.id })`; on success set `u.connection_status='pending_outgoing'` (optimistic); catch → Vietnamese error toast/inline.
- Use `x-text` ONLY (never `x-html`). Empty-state Vietnamese message when results empty after a query ("Không tìm thấy người dùng nào").
Guard the whole page behind `auth.isLoggedIn()` (redirect to /login.html if not, mirroring feed.html behavior).
  </action>
  <verify>
    <automated>grep -q "api.get('/search" web/search.html && grep -q "connection_status" web/search.html && grep -q "/connections/requests" web/search.html && grep -q "x-text" web/search.html && ! grep -q "x-html" web/search.html && grep -qi "Tìm" web/search.html && echo OK</automated>
  </verify>
  <done>search.html: query input (reads ?q=), results cards with avatar/name/headline + status-aware quick-connect button (reuses POST /api/connections/requests), degrade notice, empty state, Vietnamese, x-text only, auth-guarded.</done>
</task>

<task type="auto">
  <name>Task 2: Notification bell partial + navbar search box on feed/connections/index</name>
  <read_first>web/feed.html nav block (lines 15-35, the isLoggedIn span). web/connections.html + web/index.html navbars (same pattern). web/assets/app.js (api/auth/formatDate). RESEARCH Pattern 8 notificationBell() snippet.</read_first>
  <files>web/feed.html, web/connections.html, web/index.html</files>
  <action>
Add a notification-bell Alpine component + a compact navbar search box into the logged-in `<template x-if="isLoggedIn">` span of each of feed.html, connections.html, index.html.

1. Navbar search box: a small `<form>` (or input + Enter handler) that navigates to `/search.html?q=` + encoded value (`@submit.prevent="location.href='/search.html?q='+encodeURIComponent($refs.nq.value)"`). Vietnamese placeholder "Tìm kiếm". Keep it minimal — it just routes to search.html.

2. Notification bell: add `<div x-data="notificationBell()" x-init="start()">` containing a bell button with `x-show="unread > 0"` badge `x-text="unread"`, and a dropdown (`x-show="open"`) listing items with `x-text="message(n)"` + `x-text="formatDate(n.created_at)"`, a per-item click `@click="markOne(n.id)"`, and a "Đánh dấu tất cả đã đọc" button `@click="markAll()"`. Unread items styled bold (`:class="n.read_at ? '' : 'font-semibold'"`). x-text ONLY.

3. Define `notificationBell()` ONCE in `web/assets/app.js` as a `window.notificationBell` factory (so all three pages reuse it — do NOT duplicate inline) per RESEARCH Pattern 8:
```js
window.notificationBell = function () {
  return {
    open: false, items: [], unread: 0, timer: null,
    async load() {
      if (!auth.isLoggedIn()) return;
      try {
        const r = await api.get('/notifications');
        this.items = r.data || [];
        this.unread = (r.meta && r.meta.unread_count) || 0;
      } catch (e) { /* degrade silently — keep last badge */ }
    },
    start() { this.load(); this.timer = setInterval(() => this.load(), 15000); },  // ~15s (D-06)
    async markOne(id) { try { await api.post('/notifications/' + id + '/read'); } catch(e){} this.load(); },
    async markAll() { try { await api.post('/notifications/read-all'); } catch(e){} this.load(); },
    message(n) {
      const who = (n.actor && n.actor.display_name) || 'Ai đó';
      if (n.type === 'invite')   return who + ' đã gửi cho bạn lời mời kết nối';
      if (n.type === 'reaction') return who + ' đã bày tỏ cảm xúc về bài viết của bạn';
      if (n.type === 'comment')  return who + ' đã bình luận về bài viết của bạn';
      return who + ' có hoạt động mới';
    },
  };
};
```
   NOTE: web/assets/app.js is a shared file — appending `window.notificationBell` is additive and does not conflict with Plan 04/05 (those touch gateway PHP only). Add `window.notificationBell = notificationBell;` style consistent with the existing `window.navbar` export.
  </action>
  <verify>
    <automated>grep -q "notificationBell" web/assets/app.js && grep -q "setInterval" web/assets/app.js && grep -q "/notifications/read-all" web/assets/app.js && grep -q "notificationBell()" web/feed.html && grep -q "search.html?q=" web/feed.html && grep -q "notificationBell()" web/connections.html && grep -q "notificationBell()" web/index.html && ! grep -q "x-html" web/feed.html && echo OK</automated>
  </verify>
  <done>app.js defines window.notificationBell (15s poll, mark-one/all, Vietnamese messages); feed.html/connections.html/index.html navbars carry the bell + a search box routing to search.html?q=; x-text only.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Search page (query → quick-connect cards with relationship status) and a navbar notification bell (unread badge polling ~15s, mark-read/read-all) on feed/connections/index. Deployed on the VPS after Plan 07 cutover, or testable locally via docker compose.</what-built>
  <how-to-verify>
1. Log in as duyet on https://soa.duyet.vn (or local stack). 
2. Type "duyet" in the navbar search box → land on /search.html?q=duyet → see duyet's card with name/headline and a relationship-status-aware button. Search "PHP" → duyet appears (skill match).
3. Click "Kết nối" on a not-connected user → button flips to "Đã gửi lời mời".
4. As another user (e.g. diep) react/comment on duyet's post 1; as demo send duyet an invite. Within ~15s the duyet bell badge increments; open the dropdown → Vietnamese messages with actor names, newest first.
5. Click a notification → it marks read (badge decrements). Click "Đánh dấu tất cả đã đọc" → badge → 0.
6. Confirm no email addresses appear anywhere in search cards or notifications; all text is Vietnamese.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues (e.g. badge not updating, status wrong, PII visible).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| user-typed search query / notification content → DOM | Rendered content could carry markup. |
| browser → gateway | All calls authenticated via the Bearer token app.js attaches. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-21 | Tampering (XSS) | search.html + bell rendering | mitigate | `x-text` ONLY (no `x-html`) for all user/notification content (D-10); avatar via `:src` bound attribute, names/messages via x-text. |
| T-05-22 | Information disclosure | search cards + bell actor | mitigate | UI renders only the gateway-allowlisted fields (no email key exists in the payload); human-verify step explicitly checks no email is shown. |
| T-05-23 | Denial of Service | 15s poll loop | accept | Poll only when logged in; ~15s interval is light (D-06); single GET per tick — acceptable for a demo, no backoff needed. |
</threat_model>

<verification>
- `grep` asserts: api.get('/search'), connection_status, quick-connect, notificationBell + setInterval + read-all, x-text only (no x-html).
- Human-verify checkpoint covers the browser UX (search → connect, bell badge poll/mark-read, no PII, Vietnamese) — runs after the Plan 07 cutover.
</verification>

<success_criteria>
- search.html: query → relationship-aware quick-connect cards (Vietnamese, x-text, auth-guarded).
- Notification bell on 3 pages: unread badge polling ~15s, mark-one + mark-all, Vietnamese actor messages.
- Human verifies the live UX and confirms no PII leak.
</success_criteria>

<output>
After completion, create `.planning/phases/05-t-m-ki-m-th-ng-b-o/05-06-SUMMARY.md`.
</output>
