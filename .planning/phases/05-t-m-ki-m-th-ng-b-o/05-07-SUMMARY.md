# Plan 05-07 — Live Cutover Summary
**Status:** complete · orchestrator-run.
codex-impl-review: APPROVE round 2 (1 real bug fixed: search LIKE ESCAPE emitted SQL `ESCAPE '\'` (single backslash) which MariaDB default mode treated as escaping the quote → search broken; fixed to emit `ESCAPE '\\'`). Backup → /root/proconnect-backups/pre-phase5-*.sql. Deploy: GitHub Actions 27075967102 SUCCESS (idempotent non-destructive multi-USE db/05-migrate-phase5.sql; 8 containers healthy; no new secret/container/grant).
Post-deploy smoke reconciliation (3 test-harness fixes, no service code): (1) smoke searched own name as duyet — gateway correctly EXCLUDES the viewer from their own results → searched as long instead; (2) smoke5 left an untracked "probe" comment on seed post 1 → single tracked comment now; (3) running all 5 suites back-to-back tripped the per-IP RATE_LIMIT_PER_MIN=120 (intentional, PLAT-04) — runs pass cleanly in isolation.
Live verification: smoke-phase5.sh ALL PASS x2 (idempotent) — search by name/skill with connection_status (no email), reindex, notifications on invite/reaction/comment (best-effort 2xx unchanged, gateway-coordinated), unread_count + mark-read + read-all, actor enriched no email. Regression smoke-phase1/2/3/4 ALL PASS in isolation. Demo data clean (search index 5 users, post1 back to seed 1-comment, connections seed). 8 containers healthy; 19 other vhosts intact.
