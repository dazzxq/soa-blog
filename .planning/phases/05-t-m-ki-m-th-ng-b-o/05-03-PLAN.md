---
phase: 05-t-m-ki-m-th-ng-b-o
plan: 03
type: execute
wave: 1
depends_on: [05-01]
files_modified:
  - services/notification-service/src/Controllers/NotificationController.php
  - services/notification-service/src/routes.php
autonomous: true
requirements: [NOTIF-01, NOTIF-02, NOTIF-03]
must_haves:
  truths:
    - "notification-service creates a notification from the gateway-supplied recipient/actor/type/ref"
    - "A recipient lists only their own notifications newest-first with an exact unread_count"
    - "A user can mark one of their notifications read, or all of them, and cannot touch another user's"
  artifacts:
    - path: "services/notification-service/src/Controllers/NotificationController.php"
      provides: "Recipient-scoped CRUD (create/index/markRead/markAllRead), X-User-Id scoped reads/marks"
      contains: "read_at = NOW()"
    - path: "services/notification-service/src/routes.php"
      provides: "POST /notifications, GET /notifications, POST /notifications/read-all, POST /notifications/{id}/read"
      contains: "/notifications/read-all"
  key_links:
    - from: "NotificationController::markRead"
      to: "notifications row scoped by X-User-Id"
      via: "scoped UPDATE then scoped SELECT existence (not rowCount)"
      pattern: "WHERE id = :id AND user_id = :u"
---

<objective>
Build notification-service for real (currently a `/health` stub): a `NotificationController` with create (gateway-trusted), index (recipient-scoped list newest-first + exact unread_count), markRead (scoped, existence by scoped SELECT not rowCount), and markAllRead (scoped). Clones the connection-service raw-PDO X-User-Id doctrine verbatim (D-07/D-09). This is the NOTIF-02/03 data engine and the create sink for the gateway's best-effort hooks (Plan 05).

Purpose: NOTIF-02 needs a scoped list + badge count; NOTIF-03 needs scoped mark-read; the gateway needs a create endpoint to coordinate notifications (Plan 05).
Output: `services/notification-service/src/Controllers/NotificationController.php`, extended `services/notification-service/src/routes.php`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/05-t-m-ki-m-th-ng-b-o/05-CONTEXT.md
@.planning/phases/05-t-m-ki-m-th-ng-b-o/05-RESEARCH.md
@services/notification-service/src/routes.php
@services/notification-service/src/Db.php
@services/connection-service/src/Controllers/ConnectionController.php
@services/feed-service/src/Controllers/CommentController.php

<interfaces>
<!-- notification-service scaffold (reuse): App\Db::pdo() (PDO singleton, EMULATE_PREPARES=false, FETCH_ASSOC),
     App\Json (Json::ok/$list/$raw), App\DomainError(int,string,string), App\JsonErrorHandler. -->
<!-- notifications columns (db/05-migrate-phase5.sql): id PK AUTO, user_id (recipient), type ENUM('invite','reaction','comment'),
     actor_id, ref_id NULL, created_at, read_at NULL. -->
<!-- D-07: reads/marks scoped to X-User-Id header (recipient). create trusts the gateway body (recipient/actor in body) —
     the gateway is the only blog-net caller. -->
<!-- DOCTRINE (connection-service ConnectionController::accept): a scoped UPDATE then a scoped SELECT proves existence;
     rowCount() is UNRELIABLE for mark-read (no-op UPDATE on already-read row → 0 → false 404). LIMIT binds PDO::PARAM_INT. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: NotificationController — create + index (unread_count) + markRead + markAllRead</name>
  <read_first>services/connection-service/src/Controllers/ConnectionController.php — X-User-Id scoping (getHeaderLine('X-User-Id')), scoped UPDATE + scoped SELECT existence (accept()), uniform 404, Vietnamese DomainError. feed-service CommentController::index for LIMIT PARAM_INT. notification-service Db.php/Json.php helpers.</read_first>
  <files>services/notification-service/src/Controllers/NotificationController.php</files>
  <behavior>
    - create({user_id:5, actor_id:1, type:'invite', ref_id:null}) → 201 Json::ok({id:N}); row inserted.
    - create with user_id<=0 or actor_id<=0 → DomainError(400, VALIDATION_FAILED, Vietnamese).
    - create with type not in {invite,reaction,comment} → DomainError(400, VALIDATION_FAILED, Vietnamese).
    - index (X-User-Id:2) → 200 Json::list of user 2's rows ONLY, ORDER BY created_at DESC, id DESC; meta.unread_count = exact COUNT of read_at IS NULL for user 2; meta.total = count of returned rows.
    - index without X-User-Id → DomainError(401, UNAUTHORIZED, Vietnamese).
    - index never returns another user's rows (IDOR-safe — WHERE user_id = :me).
    - markRead without X-User-Id (missing/<=0) → DomainError(401, UNAUTHORIZED, Vietnamese) — the auth error is NOT masked by a 404 (must short-circuit BEFORE any scoped query, so a missing header never silently becomes user 0).
    - markRead(id of user 2's UNREAD notif, X-User-Id:2) → 200 {id, read_at set}; unread_count drops by 1.
    - markRead(id of an ALREADY-READ row, X-User-Id:2) → 200 (NOT 404 — existence proven by scoped SELECT, not rowCount).
    - markRead(id belonging to user 3, X-User-Id:2) → DomainError(404, NOTIFICATION_NOT_FOUND, Vietnamese) — cannot read/mark another's.
    - markAllRead(X-User-Id:2) → 200 {marked:N}; subsequent index unread_count == 0.
    - markAllRead without X-User-Id (missing/<=0) → DomainError(401, UNAUTHORIZED, Vietnamese).
  </behavior>
  <action>
Create `services/notification-service/src/Controllers/NotificationController.php` (namespace `App\Controllers`; use `App\Db`, `App\Json`, `App\DomainError`, `PDO`, PSR-7). Implement exactly per RESEARCH Pattern 6:

`create(Request,Response)`: read body `user_id`(recipient), `actor_id`, `type`, `ref_id` (null if absent). Validate `user_id>0 && actor_id>0` else DomainError(400,'VALIDATION_FAILED','Thiếu user_id hoặc actor_id.'); validate `in_array($type, ['invite','reaction','comment'], true)` else DomainError(400,'VALIDATION_FAILED','Loại thông báo không hợp lệ.'). `INSERT INTO notifications (user_id, type, actor_id, ref_id) VALUES (:u,:t,:a,:r)`. Return `Json::ok($res, ['id'=>(int)Db::pdo()->lastInsertId()], 201)`.

`index(Request,Response)`: `$me = (int)($req->getHeaderLine('X-User-Id') ?: 0)`; if `<=0` DomainError(401,'UNAUTHORIZED','Thiếu thông tin người dùng (X-User-Id).'). `$limit = min(50, max(1, (int)($req->getQueryParams()['limit'] ?? 30)))`. SELECT `id, user_id, type, actor_id, ref_id, created_at, read_at FROM notifications WHERE user_id = :u ORDER BY created_at DESC, id DESC LIMIT :lim` (bind :u and :lim PARAM_INT). Shape rows (cast id/user_id/actor_id/ref_id to int; ref_id may stay null). Then a SEPARATE scoped query `SELECT COUNT(*) FROM notifications WHERE user_id = :u AND read_at IS NULL` → `$unread`. Return `Json::list($res, $rows, ['unread_count'=>$unread, 'total'=>count($rows)])`.

`markRead(Request,Response,$args)`: FIRST `$me = (int)($req->getHeaderLine('X-User-Id') ?: 0)`; if `<=0` DomainError(401,'UNAUTHORIZED','Thiếu thông tin người dùng (X-User-Id).') — this guard MUST run before any DB query so a missing header is a 401, not a masked 404 against user 0 (same X-User-Id discipline as index/markAllRead). Then `$id = (int)$args['id']`. Scoped `UPDATE notifications SET read_at = NOW() WHERE id = :id AND user_id = :u AND read_at IS NULL` (execute). Then prove existence with a SCOPED SELECT `SELECT id, read_at FROM notifications WHERE id = :id AND user_id = :u LIMIT 1`; if `false` → DomainError(404,'NOTIFICATION_NOT_FOUND','Không tìm thấy thông báo.'). Return `Json::ok($res, ['id'=>$id, 'read_at'=>$row['read_at']])`. (Do NOT use rowCount for existence — a no-op UPDATE on an already-read row yields 0.)

`markAllRead(Request,Response)`: `$me = (int)($req->getHeaderLine('X-User-Id') ?: 0)` (if `<=0` DomainError 401, same Vietnamese message). `UPDATE notifications SET read_at = NOW() WHERE user_id = :u AND read_at IS NULL`; return `Json::ok($res, ['marked'=>$stmt->rowCount()])` (rowCount IS meaningful here = newly-read count).
  </action>
  <verify>
    <automated>php -l services/notification-service/src/Controllers/NotificationController.php && grep -q "read_at = NOW()" services/notification-service/src/Controllers/NotificationController.php && grep -q "WHERE id = :id AND user_id = :u" services/notification-service/src/Controllers/NotificationController.php && grep -q "unread_count" services/notification-service/src/Controllers/NotificationController.php && grep -q "invite" services/notification-service/src/Controllers/NotificationController.php && grep -q "NOTIFICATION_NOT_FOUND" services/notification-service/src/Controllers/NotificationController.php && [ "$(grep -o "UNAUTHORIZED" services/notification-service/src/Controllers/NotificationController.php | wc -l | tr -d ' ')" -ge 3 ] && echo OK</automated>
  </verify>
  <done>NotificationController: create validates recipient/actor/type-enum; index is recipient-scoped newest-first + exact unread_count; markRead guards X-User-Id<=0→401 BEFORE any query, then scoped UPDATE + scoped SELECT existence (404 only for cross-user/missing, 200 for already-read); markAllRead scoped with the same 401 guard. Vietnamese errors. `php -l` clean.</done>
</task>

<task type="auto">
  <name>Task 2: Wire notification-service routes (literal /read-all before {id}/read)</name>
  <read_first>services/notification-service/src/routes.php (only /health). RESEARCH Code Examples ROUTE-ORDERING note: register literal /notifications/read-all BEFORE /notifications/{id}/read.</read_first>
  <files>services/notification-service/src/routes.php</files>
  <action>
Extend `services/notification-service/src/routes.php` (keep `/health` first). Add `use App\Controllers\NotificationController;` and register, in this order:
```php
$app->post('/notifications',                    [NotificationController::class, 'create']);
$app->get ('/notifications',                    [NotificationController::class, 'index']);
$app->post('/notifications/read-all',           [NotificationController::class, 'markAllRead']);  // literal BEFORE {id}
$app->post('/notifications/{id:[0-9]+}/read',   [NotificationController::class, 'markRead']);
```
No JWT/X-User-Id middleware in the service — the controller reads X-User-Id directly (D-07).
  </action>
  <verify>
    <automated>php -l services/notification-service/src/routes.php && grep -q "NotificationController" services/notification-service/src/routes.php && grep -q "read-all" services/notification-service/src/routes.php && grep -q "{id:\[0-9\]+}/read" services/notification-service/src/routes.php && awk '/read-all/{a=NR} /\{id:\[0-9\]\+\}\/read/{b=NR} END{exit !(a && b && a<b)}' services/notification-service/src/routes.php && echo OK</automated>
  </verify>
  <done>routes.php exposes create/index/read-all/{id}/read with /read-all registered before {id}/read; /health retained; `php -l` clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| gateway → notification-service | Only caller on blog-net; no host port (D-07). |
| recipient X-User-Id → reads/marks | A user may only read/mark their OWN notifications. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-08 | Elevation / Information disclosure | index/markRead/markAllRead (IDOR) | mitigate | Every read/UPDATE scoped by `X-User-Id` in the WHERE clause; X-User-Id<=0 → 401 guard on ALL THREE (index/markRead/markAllRead) runs BEFORE any query so a missing header is never coerced to user 0; uniform 404 (NOTIFICATION_NOT_FOUND) for cross-user/missing; never trusts a body user_id on reads/marks. |
| T-05-09 | Spoofing | POST /notifications create | accept | create trusts the gateway-supplied body (recipient/actor); notification-service has no host port and is only reachable via the gateway on blog-net (trust-by-network, D-07). |
| T-05-10 | Tampering | type field | mitigate | type validated against the ENUM allowlist {invite,reaction,comment}; user_id/actor_id cast to int; ref_id nullable int. |
| T-05-11 | Repudiation/Correctness | markRead existence | mitigate | Existence proven by scoped SELECT, not rowCount — an already-read row returns 200 not a false 404 (connection-service accept doctrine). |
</threat_model>

<verification>
- `php -l` on both files.
- `grep`/`awk` asserts: scoped WHERE, unread_count, route ordering, UNAUTHORIZED appears ≥3× (index + markRead + markAllRead all guard X-User-Id).
- Runtime (Plan 07): smoke-phase5 list/mark-read/mark-all assertions via the gateway.
</verification>

<success_criteria>
- create validates recipient/actor/type-enum and inserts; index recipient-scoped newest-first + exact unread_count; markRead guards missing X-User-Id (401) then scoped with SELECT-existence (200 on already-read, 404 cross-user); markAllRead scoped with the same 401 guard.
- Routes correctly ordered; both files `php -l` clean.
</success_criteria>

<output>
After completion, create `.planning/phases/05-t-m-ki-m-th-ng-b-o/05-03-SUMMARY.md`.
</output>
</content>
</invoke>
