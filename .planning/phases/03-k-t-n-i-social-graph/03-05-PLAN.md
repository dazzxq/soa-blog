---
phase: 03-k-t-n-i-social-graph
plan: 05
type: execute
wave: 4
depends_on: ["03-01", "03-02", "03-03", "03-04"]
files_modified: []
autonomous: false
requirements: [CONN-01, CONN-02, CONN-03, CONN-04, CONN-05, CONN-06, CONN-07]
tags: [deploy, cutover, smoke, verification]

must_haves:
  truths:
    - "Codex impl-review approves the Phase 3 code before any commit (CLAUDE.md gate)"
    - "Push to main triggers CI/CD; deploy.sh applies db/03-migrate-phase3.sql to the live proconnect_connection volume"
    - "smoke-phase3.sh passes on the deployed soa.duyet.vn stack (CONN-01..07 + the /full payoff + email-leak + ownership)"
    - "smoke-phase1.sh + smoke-phase2.sh still pass (no regression); the production site stays live"
  artifacts: []
  key_links:
    - from: "GitHub Actions deploy"
      to: "live proconnect_connection volume"
      via: "scripts/deploy.sh step 7c migration"
      pattern: "03-migrate-phase3"
---

<objective>
Live cutover for Phase 3: run the mandatory Codex impl-review, commit + push to main,
let CI/CD deploy to soa.duyet.vn (applying the idempotent connections migration to the
live volume), then verify the full smoke suite green on the deployed stack and confirm
the D-05 /full payoff is live. This mirrors the Phase 1/2 cutover.

Purpose: Phase 3 code is only "done" when it runs on the live VPS with no regression and
the migration has materialized the connections table on the existing volume (Pitfall 1).
Docker is not available locally, so runtime verification happens here, on the VPS.

Output: deployed Phase 3 on soa.duyet.vn; smoke-phase1/2/3 all green; no code files
changed in this plan (orchestration + verification only).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-k-t-n-i-social-graph/03-VALIDATION.md

<interfaces>
Deploy flow (Phase 1/2 precedent): push main -> GitHub Actions (.github/workflows/deploy.yml):
  (1) lint job = php -l over all *.php; (2) deploy job = SSH to VPS -> scripts/deploy.sh
  (git pull --ff-only -> docker compose build --pull -> step 7c migration -> up -d ->
  gateway health wait) -> verify https://soa.duyet.vn/api/health.

CLAUDE.md mandatory gates: /codex-impl-review BEFORE commit; only commit after Codex APPROVE.
(The /codex-plan-review gate runs on THIS plan set before any Phase 3 coding begins.)

Smoke entry points: GW=https://soa.duyet.vn bash scripts/smoke-phaseN.sh (the scripts
accept a GW override). Demo creds: any of demo/duyet/long/diep/tai with password demo@123**.
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Codex impl-review gate (mandatory, before commit)</name>
  <what-built>The full Phase 3 changeset across Plans 01-04: db/03-migrate-phase3.sql + db/01-schema-connection.sql, deploy.sh step 7c, scripts/smoke-phase3.sh, connection-service ConnectionController + routes, gateway ConnectionsController + ConnectionClient + routes + DI, web/connections.html + profile.html.</what-built>
  <action>Present the full Phase 3 changeset to Codex via /codex-impl-review and iterate until APPROVE. This is a human-gated checkpoint: no automated work, the executor runs the review command, applies any valid fixes, and re-runs until Codex approves. No commit happens before approval.</action>
  <how-to-verify>
    1. Run `/codex-impl-review` on the staged Phase 3 changes (CLAUDE.md mandatory gate).
    2. If Codex raises valid issues, fix them and re-run `/codex-impl-review` until APPROVE.
    3. Confirm `php -l` is clean across all changed *.php (CI lint mirrors this).
    Do NOT commit until Codex APPROVES.
  </how-to-verify>
  <resume-signal>Type "approved" once Codex has approved the implementation, or describe the issues to fix.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Commit + push to main (triggers CI/CD deploy)</name>
  <read_first>scripts/deploy.sh (the full deploy flow incl. step 7c added in Plan 01); .github/workflows/deploy.yml (lint + deploy jobs).</read_first>
  <files></files>
  <action>
After Codex APPROVE (Task 1): from a feature branch (per the SOA workflow rule: local git
-> review -> push public GitHub -> VPS pull -> CI/CD auto-deploy), stage the Phase 3 files,
commit with a descriptive message, and merge/push to main so GitHub Actions deploys.
Use the gsd commit helper / git as appropriate. The commit message ends with the
Co-Authored-By trailer. Push to the default branch only after review per CLAUDE.md.
Then monitor the GitHub Actions run to green (lint + deploy jobs).
  </action>
  <verify>
    <automated>git log --oneline -1 | grep -iE "conn|phase 3|social|kết nối" && gh run list --limit 1 --json conclusion --jq '.[0].conclusion' | grep -q success</automated>
  </verify>
  <done>Phase 3 committed and pushed; GitHub Actions deploy run concludes success.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify live deploy + full smoke suite (no regression) + D-05 payoff</name>
  <what-built>Phase 3 deployed to soa.duyet.vn: connection-service real routes, gateway connection invariant + composition, connections.html + profile badge, and the live-volume connections table + demo seed via deploy.sh step 7c.</what-built>
  <action>Human-gated verification checkpoint: run the full smoke suite (smoke-phase1/2/3) against https://soa.duyet.vn, confirm SHOW TABLES lists connections with the demo seed, confirm the /full payoff shows connection_status:connected, and eyeball the browser UX. The executor runs the commands listed in how-to-verify; the human confirms the results.</action>
  <how-to-verify>
    1. Confirm migration applied: on the VPS, `docker compose exec -T mariadb mysql -uroot -p... proconnect_connection -e "SHOW TABLES;"` lists `connections`, and `SELECT id,requester_id,addressee_id,status FROM connections;` shows the demo seed (2->3 accepted, 1->2 pending).
    2. Full smoke, no regression:
       `GW=https://soa.duyet.vn bash scripts/smoke-phase1.sh && GW=https://soa.duyet.vn bash scripts/smoke-phase2.sh && GW=https://soa.duyet.vn bash scripts/smoke-phase3.sh`
       -> all three print ALL PASS.
    3. D-05 payoff (the headline): as duyet, `curl -H "Authorization: Bearer <duyet-token>" https://soa.duyet.vn/api/profiles/3/full` shows `"connection_status":"connected"` and NO `"degraded":true` for the connection part.
    4. Browser UX (manual-only per 03-VALIDATION.md): open https://soa.duyet.vn/connections.html - send/accept/reject/cancel an invite + see suggestions render; open a profile and confirm the badge + context action behave; confirm no email appears on any card.
    5. Production site still serves normally (no 5xx on /api/health).
  </how-to-verify>
  <resume-signal>Type "approved" once all three smoke suites pass, the /full payoff shows connected, and the browser UX checks out; otherwise describe the failure for gap closure.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub Actions → VPS | SSH-keyed deploy; runs scripts/deploy.sh as configured |
| deploy.sh → live mariadb volume | Step 7c migration applied as root; non-destructive + idempotent (Plan 01) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-19 | Denial (broken prod) | live deploy | mitigate | Migration is additive/idempotent; deploy.sh waits for gateway health and aborts on failure; smoke-phase1/2 confirm no regression before sign-off. |
| T-03-20 | Integrity (review gate) | commit | mitigate | Mandatory /codex-impl-review APPROVE before commit (CLAUDE.md); CI php -l lint gate. |
| T-03-21 | Information disclosure | live cards | mitigate | smoke-phase3 asserts no @/email in connection/suggestion bodies on the deployed stack. |
</threat_model>

<verification>
- GitHub Actions deploy run = success (lint + deploy).
- smoke-phase1.sh + smoke-phase2.sh + smoke-phase3.sh all ALL PASS against https://soa.duyet.vn.
- SHOW TABLES confirms the connections table + demo seed on the live volume.
- /api/profiles/3/full as duyet shows connection_status:connected (D-05 payoff live).
</verification>

<success_criteria>
- Phase 3 is deployed on soa.duyet.vn with the connections table materialized on the live volume.
- The full smoke suite is green (no regression) and the D-05 /full payoff is live.
- The mandatory Codex review gate was satisfied before commit.
</success_criteria>

<output>
After completion, create `.planning/phases/03-k-t-n-i-social-graph/03-05-SUMMARY.md`
</output>
