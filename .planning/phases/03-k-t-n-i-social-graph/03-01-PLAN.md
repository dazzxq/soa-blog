---
phase: 03-k-t-n-i-social-graph
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - db/03-migrate-phase3.sql
  - db/01-schema-connection.sql
  - scripts/deploy.sh
  - scripts/smoke-phase3.sh
autonomous: true
requirements: [CONN-01, CONN-02, CONN-03, CONN-04, CONN-05, CONN-06, CONN-07]
tags: [database, migration, smoke, social-graph]

must_haves:
  truths:
    - "The connections table exists in proconnect_connection on the live VPS volume after deploy"
    - "Re-running the phase-3 migration is a no-op (idempotent CREATE IF NOT EXISTS + guarded seed)"
    - "Demo graph fixtures exist: duyet(2)<->long(3) accepted; demo(1)->duyet(2) pending"
    - "smoke-phase3.sh runs against a running stack and asserts every CONN req + the /full payoff + email-leak + ownership"
  artifacts:
    - path: "db/03-migrate-phase3.sql"
      provides: "Idempotent live-volume connections table + demo seed"
      contains: "CREATE TABLE IF NOT EXISTS connections"
    - path: "db/01-schema-connection.sql"
      provides: "Fresh-volume schema convergence for connection-service"
      contains: "CREATE TABLE IF NOT EXISTS connections"
    - path: "scripts/deploy.sh"
      provides: "Blocking phase-3 migration step after step 7b"
      contains: "03-migrate-phase3.sql"
    - path: "scripts/smoke-phase3.sh"
      provides: "Full CONN-01..07 + payoff smoke harness"
      contains: "smoke3"
  key_links:
    - from: "scripts/deploy.sh"
      to: "db/03-migrate-phase3.sql"
      via: "docker compose exec mariadb mysql proconnect_connection < db/03-migrate-phase3.sql"
      pattern: "03-migrate-phase3\\.sql"
---

<objective>
Wave 0 infrastructure for Phase 3: ship the idempotent live-volume migration that
creates the `connections` table + demo graph seed in `proconnect_connection`, wire it
BLOCKING into deploy.sh, add the fresh-volume schema file for from-scratch convergence,
and create the `scripts/smoke-phase3.sh` harness that every later wave's acceptance
criteria reference.

Purpose: Without the live migration, every connection endpoint 500s on `SELECT ... FROM
connections` (Pitfall 1 — `db/*.sql` only run on a fresh volume; the VPS volume already
exists). Without the smoke harness, no later task is verifiable. This plan is a clone of
the proven Phase-2 migration + smoke pattern.

Output: db/03-migrate-phase3.sql, db/01-schema-connection.sql, deploy.sh step 7c,
scripts/smoke-phase3.sh.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-k-t-n-i-social-graph/03-CONTEXT.md
@.planning/phases/03-k-t-n-i-social-graph/03-RESEARCH.md
@.planning/phases/03-k-t-n-i-social-graph/03-VALIDATION.md

<interfaces>
<!-- Templates to clone. Do NOT invent new shapes — these are proven and shipped. -->

Demo user ids (db/migrate-phase1.sql.tmpl seed): 1=demo, 2=duyet, 3=long, 4=diep, 5=tai.
Shared demo password: `demo@123**`.

deploy.sh step 7b (the slot AFTER which step 7c goes) — scripts/deploy.sh lines 126-134:
```bash
# 7b) PHASE-2 ADDITIVE MIGRATION (idempotent; RESEARCH Pitfall 1) -------------
echo "[deploy] applying db/02-migrate-phase2.sql (additive)"
docker compose exec -T mariadb mysql -uroot -p"$DB_ROOT_PASSWORD" proconnect_profile < db/02-migrate-phase2.sql
echo "[deploy] phase-2 additive migration applied"
```

D-02 table shape (LOCKED): connections(id, requester_id BIGINT, addressee_id BIGINT,
status ENUM('pending','accepted') DEFAULT 'pending', created_at, updated_at,
UNIQUE(requester_id, addressee_id), INDEX(addressee_id), INDEX(status)).

smoke-phase2.sh scaffolding to clone (scripts/smoke-phase2.sh): `set -euo pipefail`,
`GW="${GW:-http://127.0.0.1:8000}"`, `PW='demo@123**'`, `FAILURES=0`,
`pass()`/`fail()` helpers, `trap restore EXIT` non-destructive cleanup, login via
`grep -o '"token":"[^"]*"' | cut -d'"' -f4`, final verdict `ALL PASS`/`N FAILURES`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: db/03-migrate-phase3.sql (idempotent live migration + demo seed)</name>
  <read_first>db/02-migrate-phase2.sql (the EXACT idempotent-migration template + header rationale + guarded-seed pattern); .planning/phases/03-k-t-n-i-social-graph/03-RESEARCH.md §"Idempotent live migration (db/03-migrate-phase3.sql)" (lines 426-457, the verbatim DDL + seed).</read_first>
  <files>db/03-migrate-phase3.sql</files>
  <action>
Create db/03-migrate-phase3.sql cloning the header + structure of db/02-migrate-phase2.sql.
Top line MUST be `USE proconnect_connection;` (the deploy step also passes the DB name).
NON-DESTRUCTIVE: only CREATE TABLE IF NOT EXISTS + guarded INSERT — NO DROP/ALTER DROP.

DDL (D-02, exact):
```sql
CREATE TABLE IF NOT EXISTS connections (
  id BIGINT UNSIGNED PRIMARY KEY AUTO_INCREMENT,
  requester_id BIGINT UNSIGNED NOT NULL,
  addressee_id BIGINT UNSIGNED NOT NULL,
  status ENUM('pending','accepted') NOT NULL DEFAULT 'pending',
  created_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
  updated_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  UNIQUE KEY uq_conn_pair (requester_id, addressee_id),
  INDEX idx_conn_addressee (addressee_id),
  INDEX idx_conn_status (status)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;
```

Idempotent demo seed (D-08) — explicit ids + `WHERE NOT EXISTS` so re-runs are no-ops:
- id 1: requester 2 (duyet), addressee 3 (long), status 'accepted'.
- id 2: requester 1 (demo), addressee 2 (duyet), status 'pending'.
Use the `INSERT INTO connections (id, requester_id, addressee_id, status) SELECT ... WHERE NOT EXISTS (SELECT 1 FROM connections WHERE id = N)` form (mirror 02-migrate-phase2.sql 5b). Add a header comment block explaining Pitfall 1 (db/*.sql only run on fresh volume) and the plain-.sql/no-envsubst rationale, exactly as 02 does.
  </action>
  <verify>
    <automated>grep -q "USE proconnect_connection" db/03-migrate-phase3.sql && grep -q "CREATE TABLE IF NOT EXISTS connections" db/03-migrate-phase3.sql && grep -q "uq_conn_pair (requester_id, addressee_id)" db/03-migrate-phase3.sql && grep -q "SELECT 1, 2, 3, 'accepted'" db/03-migrate-phase3.sql && grep -q "SELECT 2, 1, 2, 'pending'" db/03-migrate-phase3.sql && ! grep -qiE "DROP TABLE|ALTER TABLE .* DROP|envsubst|\\$\\{" db/03-migrate-phase3.sql && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep "USE proconnect_connection" db/03-migrate-phase3.sql` matches (correct DB).
    - `grep "CREATE TABLE IF NOT EXISTS connections" db/03-migrate-phase3.sql` matches.
    - `grep "uq_conn_pair (requester_id, addressee_id)" db/03-migrate-phase3.sql` matches (UNIQUE backstop present).
    - `grep "ENUM('pending','accepted')" db/03-migrate-phase3.sql` matches.
    - `grep "SELECT 1, 2, 3, 'accepted'"` and `grep "SELECT 2, 1, 2, 'pending'"` both match (demo seed).
    - `grep -iE "DROP TABLE|envsubst|\${"` returns NOTHING (non-destructive, plain .sql).
  </acceptance_criteria>
  <done>db/03-migrate-phase3.sql exists, idempotent, non-destructive, demo seed present, no envsubst/DROP.</done>
</task>

<task type="auto">
  <name>Task 2: db/01-schema-connection.sql (fresh-volume convergence)</name>
  <read_first>db/00-init.sh (creates proconnect_connection DB + connection_svc grant on a fresh volume); db/01-schema-profile.sql (the fresh-volume schema-file pattern that USEs its own DB); 03-RESEARCH.md Pitfall 1 (filename ordering note: 01-schema-connection.sql sorts after 00-init.sh; harmless alongside 01-schema-profile.sql since each USEs its own DB).</read_first>
  <files>db/01-schema-connection.sql</files>
  <action>
Create db/01-schema-connection.sql so a from-scratch `docker compose up` (fresh volume)
converges to the same schema. Top line `USE proconnect_connection;`. Contains the SAME
`CREATE TABLE IF NOT EXISTS connections (...)` DDL as Task 1 (copy it verbatim). Do NOT
include the demo seed here (seed belongs in the migration + 99-seed flow; keep the
fresh-volume schema file structural-only, mirroring how 01-schema-profile.sql is
structure-only). Add a one-line header: this is the fresh-volume schema; the live VPS
volume gets the same table via db/03-migrate-phase3.sql (Pitfall 1).
Confirm ordering is safe: `ls db/` shows 00-init.sh sorts before 01-schema-*.sql.
  </action>
  <verify>
    <automated>ls db/ | sort | grep -n "01-schema-connection.sql" && grep -q "USE proconnect_connection" db/01-schema-connection.sql && grep -q "CREATE TABLE IF NOT EXISTS connections" db/01-schema-connection.sql && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `db/01-schema-connection.sql` exists with `USE proconnect_connection;` + the connections DDL.
    - `ls db/ | sort` shows `00-init.sh` before `01-schema-connection.sql` (fresh-volume order safe).
    - File does NOT contain the demo seed INSERTs (structure-only; `grep -c "INSERT" db/01-schema-connection.sql` is 0).
  </acceptance_criteria>
  <done>Fresh-volume schema file exists, ordered correctly, structure-only.</done>
</task>

<task type="auto">
  <name>Task 3: wire migration into deploy.sh (step 7c, blocking) + create smoke-phase3.sh harness</name>
  <read_first>scripts/deploy.sh lines 126-141 (step 7b phase-2 migration + step 8 full-topology up — step 7c slots BETWEEN them); scripts/smoke-phase2.sh (the FULL smoke scaffold to clone: set -euo pipefail, GW/PW/FAILURES, pass/fail, trap restore EXIT, login token extraction, final verdict); 03-VALIDATION.md §"Per-Requirement Verification Map" (the assertions to implement).</read_first>
  <files>scripts/deploy.sh, scripts/smoke-phase3.sh</files>
  <action>
PART A — deploy.sh: Insert step 7c IMMEDIATELY after step 7b (after line 134
`echo "[deploy] phase-2 additive migration applied"`), BEFORE step 8 full-topology up.
Plain .sql, no envsubst, no `|| true` (a failure must surface). Mirror 7b exactly:
```bash
# 7c) PHASE-3 ADDITIVE MIGRATION (idempotent; RESEARCH Pitfall 1) -------------
# Adds the connections table + demo graph seed to the live proconnect_connection
# volume. Non-destructive (CREATE IF NOT EXISTS + guarded seed), safe to re-run.
echo "[deploy] applying db/03-migrate-phase3.sql (additive)"
docker compose exec -T mariadb mysql -uroot -p"$DB_ROOT_PASSWORD" proconnect_connection < db/03-migrate-phase3.sql
echo "[deploy] phase-3 additive migration applied"
```

PART B — scripts/smoke-phase3.sh: Clone smoke-phase2.sh scaffold (`set -euo pipefail`,
`GW="${GW:-http://127.0.0.1:8000}"`, `PW='demo@123**'`, `FAILURES=0`, `pass()`/`fail()`
using a `[smoke3]` prefix, login helper, final `ALL PASS`/`N FAILURES` verdict).
Make it executable (`chmod +x`). NON-DESTRUCTIVE: any edge the script creates it must
DELETE (cancel/reject) in cleanup; it MUST NOT delete the demo-seed fixtures
(duyet<->long accepted, demo->duyet pending) — or must recreate them. Register a
`trap` cleanup BEFORE creating any edge.

Implement these assertions (D-04 routes from the gateway — these go live in Plan 03;
the harness is written now and the assertions run on the deployed stack in Plan 05):
1. login duyet (2), long (3), diep (4) → tokens.
2. CONN-07 self-invite: `POST /api/connections/requests {target_id:2}` as duyet → 400.
3. CONN-07 missing user: `POST .../requests {target_id:999999}` as duyet → 404 (PROFILE_NOT_FOUND).
4. CONN-07 already-connected: duyet invites long (already accepted via seed) → 409.
5. CONN-01 send: diep invites tai? No — use a clean pair: diep(4) invites tai(5) → 201/200; appears in diep outgoing; clean up after (cancel).
6. CONN-07 duplicate: repeat step 5 → 409 (REQUEST_EXISTS).
7. CONN-04 lists: `GET /api/connections/requests?direction=incoming` for duyet contains demo(1) (seed pending demo->duyet); `?direction=outgoing` for demo contains duyet(2).
8. CONN-02 accept then connected: have a clean requester invite an addressee, addressee accepts → `GET /api/connections/status/{requester}` shows connected for both; then remove to restore.
9. CONN-02 reject/cancel gone: invite → reject (addressee) → not in pending.
10. CONN-03 connections list enriched: `GET /api/connections` as duyet contains long's display_name "Đinh Ngọc Long"; assert NO `@`/`"email"` in body.
11. CONN-06 suggestions: `GET /api/connections/suggestions` as duyet does NOT include long (connected) nor duyet (self); assert NO `@`/`"email"` in body.
12. CONN-05 status values: `/api/connections/status/3` (long) for duyet → connected; for an un-edged pair → none.
13. PAYOFF (D-05): as duyet token, `GET /api/profiles/3/full` → body contains `"connection_status":"connected"` (NOT "none", NOT null) AND no `"degraded":true` for the connection part.
14. Ownership: diep (4) tries `POST /api/connections/requests/{demo->duyet pending id}/accept` → 404/403 (only addressee duyet may accept).
15. Security: combined assert no `@`/`"email"` across /connections, /suggestions, /requests bodies.

Use `grep -qE` for body assertions. For codes use `curl -s -o /dev/null -w '%{http_code}'`.
Where an assertion depends on a request id, capture it via `grep -o '"id":[0-9]*' | head -1 | cut -d: -f2`.
  </action>
  <verify>
    <automated>grep -q "03-migrate-phase3.sql" scripts/deploy.sh && grep -q "proconnect_connection < db/03-migrate-phase3.sql" scripts/deploy.sh && bash -n scripts/smoke-phase3.sh && test -x scripts/smoke-phase3.sh && grep -q "connection_status.*connected" scripts/smoke-phase3.sh && grep -qE 'email|@' scripts/smoke-phase3.sh && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep "03-migrate-phase3.sql" scripts/deploy.sh` matches and the line uses `proconnect_connection` and has NO `|| true`.
    - The 7c block appears AFTER the 7b `phase-2 additive migration applied` echo and BEFORE `docker compose up -d --remove-orphans`.
    - `bash -n scripts/smoke-phase3.sh` exits 0 (valid syntax).
    - `test -x scripts/smoke-phase3.sh` (executable).
    - `grep "connection_status.*connected" scripts/smoke-phase3.sh` matches (D-05 payoff assertion present).
    - smoke-phase3.sh greps for `@`/`email` to guard PII leak.
    - smoke-phase3.sh registers a cleanup `trap` and does not delete the demo-seed fixtures.
  </acceptance_criteria>
  <done>deploy.sh applies the phase-3 migration blocking after 7b; smoke-phase3.sh is a syntactically-valid, executable, non-destructive harness covering CONN-01..07 + payoff + PII + ownership.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| deploy host → mariadb container | Migration SQL piped into the running DB as root; must be non-destructive |
| fresh-volume initdb glob → schema | db/*.sql run alphabetically only on a fresh volume |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering/Denial | db/03-migrate-phase3.sql | mitigate | Only CREATE IF NOT EXISTS + guarded INSERT; NO DROP/ALTER-DROP; idempotent so re-runs are no-ops (grep guard in verify). |
| T-03-02 | Denial | deploy.sh step 7c | mitigate | No `|| true` — a migration failure surfaces and blocks deploy before full-topology up, so services never boot against a missing table. |
| T-03-03 | Information disclosure | smoke-phase3.sh | mitigate | The harness itself asserts no email/@ leaks in card bodies — turning a security control into a CI gate. |
| T-03-04 | Tampering | fresh-volume ordering | accept | 01-schema-connection.sql sorts after 00-init.sh; both 01-* files USE their own DB so inter-order is irrelevant. Low risk. |
</threat_model>

<verification>
- `grep` asserts on the three SQL/script artifacts (see per-task automated checks).
- `bash -n scripts/smoke-phase3.sh` for syntax.
- Runtime verification of the migration + smoke assertions happens in Plan 05 (live cutover) against the deployed VPS stack — Docker is not available locally (03-RESEARCH §Environment Availability).
</verification>

<success_criteria>
- connections table will exist on the live volume after deploy (migration wired blocking).
- Migration is idempotent + non-destructive + carries the demo graph seed.
- smoke-phase3.sh is a valid, executable, non-destructive harness referenced by every later wave.
</success_criteria>

<output>
After completion, create `.planning/phases/03-k-t-n-i-social-graph/03-01-SUMMARY.md`
</output>
